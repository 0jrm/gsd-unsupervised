---
phase: 03-agent-integration
plan: 01
type: execute
depends_on: ["02-02"]
files_modified:
  - src/agent-runner.ts
  - src/cursor-agent.ts
  - src/stream-events.ts
  - src/agent-runner.test.ts
  - src/agent-runner.spawn.test.ts
  - src/cursor-agent.test.ts
  - src/cursor-agent.invoker.test.ts
domain:
  - node
  - cursor-agent
---

<objective>
Implement a robust, configurable Cursor agent invoker that the orchestrator uses to execute GSD commands in YOLO mode, with timeout handling and session logging hooks.

Purpose: Provide a single agent-agnostic seam that spawns the Cursor CLI with correct flags, environment, and timeouts, and exposes a simple AgentInvoker interface.
Output: Production-ready createAgentInvoker/createCursorAgentInvoker implementation and tests for success, timeout, crash, and heartbeat behavior.
</objective>

<execution_context>
.gsd-framework/workflows/execute-plan.md
.gsd-framework/templates/summary.md
.gsd-framework/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-lifecycle/02-01-SUMMARY.md
@.planning/phases/02-lifecycle/02-02-SUMMARY.md
@src/orchestrator.ts
@src/config.ts
@src/config/paths.ts
@src/cursor-agent.ts
@src/agent-runner.ts
@src/session-log.ts
@src/stream-events.ts
@src/logger.ts

**Tech stack:** Node 18+, TypeScript, child_process.spawn, readline, tree-kill, vitest, Zod.
**Patterns:** AgentInvoker seam, NDJSON stream parsing via stream-events, vitest unit tests.
**Decisions:** Agent invocation pluggable by AgentId; runs in non-interactive/YOLO mode; session log and heartbeat for crash detection.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement agent-agnostic runner with stable spawn contract</name>
  <files>src/agent-runner.ts, src/stream-events.ts, src/agent-runner.test.ts, src/agent-runner.spawn.test.ts</files>
  <action>
Ensure runAgent in src/agent-runner.ts is the single spawning seam. It must:
- Accept options: agentPath, workspace, prompt, env, timeoutMs, model, resumeId, onEvent.
- Invoke binary with: -p --force --trust --approve-mcps --workspace &lt;dir&gt; --output-format stream-json.
- Use node:child_process.spawn with stdio ['pipe','pipe','pipe'], wrap stdout in readline, feed each line through parseEvent from stream-events.ts.
- Return RunAgentResult: sessionId (extractSessionId), resultEvent (extractResult), events, exitCode, timedOut, stderr.
- When timeoutMs &gt; 0: schedule timer, call abortAgent (tree-kill), resolve with timedOut: true when process closes. Do NOT reject on timeout.
- Catch spawn errors and reject with clear message including agent path. Never throw from event handlers.
  </action>
  <verify>npm test -- agent-runner.spawn.test.ts agent-runner.test.ts</verify>
  <done>runAgent spawns with documented flags, surfaces typed event stream and result metadata, tests pass for spawn args, env merging, timeout/abort.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Cursor-specific AgentInvoker with session logging and heartbeat</name>
  <files>src/cursor-agent.ts, src/session-log.ts, src/cursor-agent.invoker.test.ts</files>
  <action>
Implement createCursorAgentInvoker and createAgentInvoker:
- createCursorAgentInvoker: accepts CursorAgentConfig (agentPath, defaultTimeoutMs, sessionLogPath, heartbeatPath, heartbeatIntervalMs), wraps runAgent, adapts to AgentInvoker (success, output/error).
- Write "running" SessionLogEntry before starting; "done"/"timeout"/"crashed" on completion.
- If heartbeatPath set: write ISO timestamp immediately and on heartbeatIntervalMs; unlink and clear interval when run finishes.
- createAgentInvoker: factory that returns cursor adapter for cursor, stubs for other AgentId values.
  </action>
  <verify>npm test -- cursor-agent.invoker.test.ts cursor-agent.test.ts</verify>
  <done>createCursorAgentInvoker and createAgentInvoker encapsulate spawn, heartbeat, session-log; tests cover success, timeout, crash, heartbeat.</done>
</task>

<task type="auto">
  <name>Task 3: Verify agent invocation end-to-end</name>
  <files></files>
  <action>
Run full test suite: npm run build && npm test. Confirm agent-runner and cursor-agent suites pass. No manual steps required in YOLO mode.
  </action>
  <verify>npm run build && npm test</verify>
  <done>All tests pass; orchestrator can obtain AgentInvoker and execute GSD commands; no orphaned processes or heartbeat files.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds
- [ ] npm test passes including agent-runner and cursor-agent suites
- [ ] No TypeScript errors
</verification>

<success_criteria>
- All tasks completed and tests pass
- Orchestrator obtains AgentInvoker for configured agent ID
- Agent runs produce session-log entries and respect timeouts
- No orphaned processes or heartbeat files
</success_criteria>

<output>
After completion, create .planning/phases/03-agent-integration/03-01-SUMMARY.md per .gsd-framework/templates/summary.md
</output>
