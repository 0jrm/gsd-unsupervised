---
phase: 03-agent-integration
plan: 01
type: execute
depends_on: []
files_modified:
  - src/agent-runner.ts
  - src/cursor-agent.ts
  - src/agent-runner.spawn.test.ts
  - src/cursor-agent.invoker.test.ts
domain:
  - orchestrator
  - cursor-agent
---

<objective>
Harden the Cursor agent invoker so timeouts and crashes are classified deterministically, with hermetic tests locking down spawn arguments, environment merging, timeout behavior, and session logging + heartbeat lifecycle.

Purpose: Ensure the orchestrator can reliably distinguish success, timeout, and crash outcomes from the Cursor agent to support later crash recovery, resume, and monitoring phases.
Output: Updated `RunAgentResult` shape with explicit timeout signal and a tested `CursorAgentInvoker` that records session log entries and heartbeat behavior across success, timeout, and crash scenarios.
</objective>

<execution_context>
/mnt/c/Users/jrm22n/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/02-lifecycle/02-01-SUMMARY.md
@.planning/phases/02-lifecycle/02-02-SUMMARY.md

@src/agent-runner.ts
@src/cursor-agent.ts
@src/agent-runner.spawn.test.ts
@src/cursor-agent.invoker.test.ts

**Tech stack available:** Node ≥18, TypeScript, Vitest, existing orchestrator and lifecycle tests, subprocess spawning via `child_process`, heartbeat/session logging infrastructure.
**Established patterns:** Lifecycle and orchestrator behavior are validated via focused Vitest unit tests; subprocess behavior is verified with hermetic mocks and explicit classification fields instead of parsing stderr.
**Constraining decisions:**
- [Phase 2]: `GoalStateMachine` focuses on orchestration lifecycle; plan execution loops and agent invocation details live in the orchestrator and invoker layers.
- [Phase 2]: Orchestrator lifecycle tests treat the invoker as a seam, assuming deterministic classification of success vs failure.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make timeouts first-class in RunAgentResult</name>
  <files>src/agent-runner.ts</files>
  <action>
Update the `RunAgentResult` type and `runAgent` implementation so that timeout conditions are represented by an explicit `timedOut: boolean` field rather than inferred from stderr text. Ensure timeout detection is based on actual abort/timeout handling in the subprocess layer, and that crash vs timeout are mutually exclusive outcomes (one boolean for timeout plus existing error/exit information). Preserve existing fields used by callers and keep the shape stable enough for downstream consumers in the orchestrator and invoker.
  </action>
  <verify>Run `npm test src/agent-runner.spawn.test.ts` (or the equivalent Vitest pattern) and ensure all existing tests that depend on `RunAgentResult` still pass.</verify>
  <done>`RunAgentResult` exposes an explicit `timedOut` boolean, timeout paths set it to true, normal completions leave it false, no callers rely on stderr substring parsing, and type-checking passes without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add hermetic tests for spawn args, env passthrough, and timeout abort behavior</name>
  <files>src/agent-runner.spawn.test.ts, src/agent-runner.ts</files>
  <action>
Create or extend `src/agent-runner.spawn.test.ts` with Vitest tests that exercise the `runAgent` subprocess wrapper in a hermetic way using mocks rather than real subprocesses. Lock down the exact spawn arguments (binary path, arguments, cwd) and environment merging behavior (base process env plus agent-specific additions) so regressions are caught. Add tests that simulate timeout/abort scenarios and assert that `RunAgentResult.timedOut` is set correctly, subprocess cleanup is triggered, and no flaky reliance on stderr text remains.
  </action>
  <verify>Run `npm test src/agent-runner.spawn.test.ts` (or `npx vitest src/agent-runner.spawn.test.ts`) and confirm all new and existing tests pass.</verify>
  <done>Spawn argument and env-merging behavior are fully covered by tests, timeout/abort behavior sets `timedOut` correctly, and test output shows green for `src/agent-runner.spawn.test.ts`.</done>
</task>

<task type="auto">
  <name>Task 3: Add invoker-level tests for session logs and heartbeat lifecycle</name>
  <files>src/cursor-agent.invoker.test.ts, src/cursor-agent.ts</files>
  <action>
Implement or extend `src/cursor-agent.invoker.test.ts` to cover the `CursorAgentInvoker` behavior across success, timeout, and crash outcomes, using mocks for the underlying `runAgent` calls. Verify that the invoker writes appropriate entries to the session log for each outcome, including status fields that distinguish success, timeout, and crash, and that heartbeat files are created, updated, and cleaned up according to the contract used by later crash-recovery phases. Ensure the invoker classifies timeout vs crash strictly via explicit fields (e.g., `timedOut`) rather than parsing stderr, in line with the `RunAgentResult` changes.
  </action>
  <verify>Run `npm test src/cursor-agent.invoker.test.ts` (or the equivalent Vitest invocation) and confirm all tests pass without regressions.</verify>
  <done>Invoker tests exist for success, timeout, and crash paths; they assert correct session-log entries and heartbeat lifecycle; all relevant tests pass; and invoker classification relies on explicit `RunAgentResult` fields, not stderr substrings.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `npm test src/agent-runner.spawn.test.ts src/cursor-agent.invoker.test.ts` succeeds without errors.
- [ ] `npm test` (full suite) passes to ensure no regressions in orchestrator or lifecycle behavior.
- [ ] TypeScript compilation (via `npm run build` or equivalent) succeeds with the updated `RunAgentResult` shape.
</verification>

<success_criteria>

- All three tasks are completed with passing tests.
- `RunAgentResult` exposes an explicit `timedOut` boolean used by the invoker instead of stderr parsing.
- Subprocess spawn arguments, environment merging, and timeout behavior are locked down by hermetic tests.
- Invoker behavior for session logs and heartbeat lifecycle is covered by unit tests across success, timeout, and crash scenarios.
  </success_criteria>

<output>
After completion, create `.planning/phases/03-agent-integration/03-01-SUMMARY.md`:

# Phase 3 Plan 1: Agent invoker hardening Summary

**Cursor agent invocation is deterministic and well-covered (spawn args, timeout/abort classification, session logging, heartbeat).**

## Accomplishments

- [Key outcome 1: timeout handling and `RunAgentResult` shape]
- [Key outcome 2: subprocess spawn/env tests]
- [Key outcome 3: invoker session-log and heartbeat lifecycle tests]

## Files Created/Modified

- `src/agent-runner.ts` — Explicit `timedOut` field in `RunAgentResult`.
- `src/cursor-agent.ts` — Invoker classification logic based on explicit fields.
- `src/agent-runner.spawn.test.ts` — Hermetic tests for spawn args/env/timeout.
- `src/cursor-agent.invoker.test.ts` — Tests for session logging and heartbeat lifecycle.

## Decisions Made

- [Any notable decisions or "None".]

## Issues Encountered

- [Issues and resolutions, or "None".]

## Next Step

- Ready for `03-02-PLAN.md` (Stream parsing and completion detection).
</output>

---
phase: 03-agent-integration
plan: 01
type: execute
depends_on: ["02-02"]
files_modified:
  - src/cursor-agent.ts
  - src/agent-runner.ts
  - src/cursor-agent.test.ts
  - src/cursor-agent.invoker.test.ts
  - src/agent-runner.test.ts
  - src/agent-runner.spawn.test.ts
domain:
  - node
  - cursor-agent
---

<objective>
Implement a robust, configurable Cursor agent invoker that the orchestrator can use to execute GSD commands in YOLO mode, with timeout handling and session logging hooks.

Purpose: Provide a single agent-agnostic seam that spawns the Cursor CLI with the correct flags, environment, and timeouts, and exposes a simple `AgentInvoker` interface to the rest of the system.
Output: A production-ready `createAgentInvoker`/`createCursorAgentInvoker` implementation and tests that exercise success, timeout, crash, and heartbeat behavior.
</objective>

<execution_context>
/mnt/c/Users/jrm22n/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
/mnt/c/Users/jrm22n/.cursor/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/02-lifecycle/02-01-SUMMARY.md
@.planning/phases/02-lifecycle/02-02-SUMMARY.md

@src/orchestrator.ts
@src/config.ts
@src/config/paths.ts
@src/cursor-agent.ts
@src/agent-runner.ts
@src/session-log.ts
@src/stream-events.ts
@src/logger.ts

@src/cursor-agent.test.ts
@src/cursor-agent.invoker.test.ts
@src/agent-runner.test.ts
@src/agent-runner.spawn.test.ts

**Tech stack available:** Node 18+, TypeScript, `child_process.spawn`, `readline`, `tree-kill`, vitest, Zod.
**Established patterns:** Functional seams for orchestration (`AgentInvoker`), NDJSON stream parsing via `stream-events`, vitest-based unit and spawn-contract tests.
**Constraining decisions:**
- Agent invocation must be pluggable by agent ID (`AgentId` union).
- Invocations run in non-interactive/YOLO mode with auto-approved tool calls and MCPs.
- Session logging and heartbeat files are the primary crash-detection mechanism.

**Issues being addressed:** None specific; this plan formalizes and tests the agent invocation seam so later phases can rely on it.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement agent-agnostic runner with stable spawn contract</name>
  <files>src/agent-runner.ts, src/stream-events.ts, src/agent-runner.test.ts, src/agent-runner.spawn.test.ts</files>
  <action>
Ensure `runAgent` in `src/agent-runner.ts` is the single spawning seam for the Cursor agent, accepting options for agent path, workspace, prompt, env, timeout, model, and resume ID, and always invoking the binary with:
- `-p --force --trust --approve-mcps` flags for non-interactive, trusted, auto-approved execution.
- `--workspace &lt;dir&gt;` pointing at the autopilot workspace root.
- `--output-format stream-json` so stdout is line-delimited JSON.

Use `node:child_process.spawn` with stdio `['pipe','pipe','pipe']`, wrap stdout in a `readline` interface, and feed each line through `parseEvent` from `src/stream-events.ts`, collecting a list of typed `CursorStreamEvent` values and forwarding them to an optional `onEvent` callback.

Capture stderr into an in-memory string buffer; when the child exits, return an object containing `sessionId` (extracted via `extractSessionId`), the last `result` event (via `extractResult`), the full events array, the exit code, a `timedOut` boolean, and the stderr text (plus timeout diagnostics if applicable).

Add a timeout path that, when `timeoutMs &gt; 0`, schedules a timer to mark the run as timed out and call `abortAgent` (implemented with `tree-kill`), ensuring the promise still resolves with `timedOut: true` once the process actually closes. Do not reject on timeout; callers should treat timeout as a structured error case.

Keep the implementation defensive: catch spawn errors and reject with a clear message that includes the agent path; avoid throwing from event handlers.
  </action>
  <verify>
Run `pnpm test -- agent-runner.spawn.test.ts agent-runner.test.ts` (or equivalent npm test command) and confirm all tests in the agent-runner suite pass, including spawn-argument, env-merging, and timeout/abort behavior.
  </verify>
  <done>
`runAgent` reliably spawns the agent with the documented flags and arguments, surfaces a typed event stream and result metadata, and is covered by tests that verify spawn arguments, env merging, and timeout/abort semantics.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Cursor-specific AgentInvoker with session logging and heartbeat</name>
  <files>src/cursor-agent.ts, src/session-log.ts, src/cursor-agent.invoker.test.ts</files>
  <action>
Implement `createCursorAgentInvoker` and `createAgentInvoker` in `src/cursor-agent.ts` so that:
- `createCursorAgentInvoker` accepts a `CursorAgentConfig` (agent binary path, default timeout, session log path, optional heartbeat path/interval), wraps a call to `runAgent`, and adapts the result into the `AgentInvoker` contract (`success`, `output`/`error` message).
- The invoker writes a "running" `SessionLogEntry` to the configured JSONL log before starting, including goal title, phase and plan numbers, and the full GSD command string.
- While the agent runs, if `heartbeatPath` is configured, it writes an ISO timestamp to that file immediately and then on a fixed interval (`heartbeatIntervalMs`) using `fs.promises.writeFile`, and ensures the heartbeat file is unlinked and the interval cleared when the run finishes or throws.
- On success (exit code 0 with a result event), it appends a "done" entry with duration and session ID; on timeout, it appends a "timeout" entry with duration and an explanatory error; on non-timeout failures or thrown errors, it appends a "crashed" entry with duration and error details, always best-effort.

Implement `createAgentInvoker` as a small factory that chooses between the Cursor implementation and stubbed adapters for other `AgentId` values, using `getCursorBinaryPath(config)` and `config.agentTimeoutMs` to wire the cursor case, and returning fast-resolving stubs (logging-only) for unsupported agents until real adapters exist.
  </action>
  <verify>
Run `pnpm test -- cursor-agent.invoker.test.ts cursor-agent.test.ts` and confirm tests exercise:
- Session-log writes for running/done/timeout/crashed paths.
- Heartbeat write and unlink behavior under fake timers.
- AgentId dispatch in `createAgentInvoker` and the cursor-specific configuration wiring.
  </verify>
  <done>
`createCursorAgentInvoker` and `createAgentInvoker` encapsulate all Cursor-specific spawn details, heartbeat, and session-log behavior behind the `AgentInvoker` seam, and their dedicated tests pass and cover success, timeout, crash, and heartbeat scenarios.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Agent invocation seam (`runAgent` + Cursor-specific `AgentInvoker`) with session logging and heartbeat, plus tests that guard the spawn contract and error/timeout behavior.</what-built>
  <how-to-verify>
    1. Run the full test suite: `pnpm test` (or your project’s configured test command) and confirm all tests pass, paying particular attention to agent-runner and cursor-agent suites.
    2. Temporarily configure a small `agentTimeoutMs` in your local `config.json` and invoke the autopilot against a long-running plan to observe that timeouts produce structured errors and do not hang the process.
    3. With `CURSOR_API_KEY` set and a valid cursor-agent binary installed, run the CLI in a small demo workspace to confirm the agent starts, the heartbeat file is created and removed on completion, and session-log entries are appended as expected.
  </how-to-verify>
  <resume-signal>Type "approved" when the agent invocation behavior, logging, and heartbeat handling look correct in both passing and failure scenarios, or describe any discrepancies that need to be addressed.</resume-signal>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `pnpm test` (or equivalent) passes, including all cursor-agent and agent-runner related tests.
- [ ] Manual smoke-test of invoking a simple `/gsd/execute-plan` command via the CLI confirms the agent runs in YOLO mode and returns structured success or timeout/crash errors.
- [ ] Heartbeat file appears while the agent runs and is removed on completion without lingering timers.
</verification>

<success_criteria>

- All tasks in this plan are completed and passing tests.
- The orchestrator can obtain an `AgentInvoker` for the configured agent ID and use it to execute GSD commands without knowing about spawn details.
- Agent runs produce clear session-log entries and respect configured timeouts without leaving orphaned processes or heartbeat files.
  </success_criteria>

<output>
After completion, create `.planning/phases/03-agent-integration/03-01-SUMMARY.md`:

# Phase 3 Agent Integration — Plan 01: Agent Invoker Summary

**Cursor agent invoker and agent-runner spawn contract implemented and covered by tests.**

## Accomplishments

- Implemented and/or confirmed `runAgent` spawns the Cursor agent with the correct flags, env, timeout, and NDJSON stream wiring.
- Implemented and/or confirmed `createCursorAgentInvoker` and `createAgentInvoker` adapters that wrap `runAgent` with session logging and heartbeat handling.
- Added or updated tests that cover success, timeout, crash, heartbeat, and agent-ID dispatch behavior.

## Files Created/Modified

- `src/agent-runner.ts` and associated tests — spawn contract and timeout/abort logic.
- `src/cursor-agent.ts` and associated tests — Cursor-specific invoker, session logging, and heartbeat.
- Any additional small refactors or test fixtures required to exercise the contract.

## Decisions Made

- Standardized on a single Cursor CLI invocation shape (`-p --force --trust --approve-mcps --workspace &lt;dir&gt; --output-format stream-json`) for all runs.
- Confirmed pluggable agent seam using `AgentId` union and stub adapters for non-cursor agents until they support the same stream contract.

## Issues Encountered

- [Document any flakiness, platform-specific behavior, or edge cases discovered during testing, or state "None".]

## Next Step

- Phase 3, Plan 02: Stream parsing and completion detection, building on the `runAgent` event stream to drive higher-level progress reporting and result handling.
</output>
