---
phase: 03-agent-integration
plan: 01
type: execute
depends_on: []
files_modified: [src/cursor-agent.ts, src/agent-runner.ts, src/cursor-agent.invoker.test.ts, src/agent-runner.spawn.test.ts]
domain: node
---

<objective>
Harden the Cursor agent invoker seam (spawn, timeout/abort, heartbeat, session logging) so orchestrator runs are reliable and failures are classified correctly.

Purpose: Phase 3 is the “hands” of the autopilot—invocation must be deterministic, observable, and robust under timeouts/crashes.
Output: A well-tested `runAgent`/cursor invoker contract with explicit timeout signal, stable spawn args, and predictable session-log outcomes.
</objective>

<execution_context>
/mnt/c/Users/jrm22n/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/ARCHITECTURE.md
@docs/CONTEXT-FOR-MODEL.md
@src/cursor-agent.ts
@src/agent-runner.ts
@src/session-log.ts
@src/orchestrator.ts
@src/config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make timeouts first-class in the runAgent result</name>
  <files>src/agent-runner.ts, src/cursor-agent.ts</files>
  <action>Update `runAgent()` to return an explicit boolean (e.g. `timedOut`) rather than relying on stderr substring checks. Ensure the timeout path: (1) triggers `abortAgent(child)`; (2) resolves with `timedOut: true`; (3) preserves stderr as diagnostic text without being the signal. Then update `createCursorAgentInvoker()` to use this boolean for classification (`timeout` vs `crashed`). Avoid changing orchestrator behavior here—keep scope limited to the invoker seam.</action>
  <verify>npm test passes</verify>
  <done>`createCursorAgentInvoker` logs/returns `timeout` outcomes based on `RunAgentResult.timedOut`, not stderr parsing</done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for spawn args, env passthrough, and timeout abort behavior</name>
  <files>src/agent-runner.spawn.test.ts</files>
  <action>Create a focused Vitest suite that mocks `node:child_process` `spawn` and `tree-kill` to assert: (1) args include `-p --force --trust --approve-mcps --workspace <dir> --output-format stream-json` and the prompt; (2) optional `--model` and `--resume` flags are appended when provided; (3) env merges `process.env` with provided `env`; (4) when `timeoutMs` elapses, `abortAgent` is invoked and result resolves with `timedOut: true`. Keep tests hermetic: do not require `cursor-agent` binary, do not spawn real processes.</action>
  <verify>npm test -- agent-runner.spawn passes</verify>
  <done>Spawn/timeout contract is covered by unit tests and does not rely on real subprocesses</done>
</task>

<task type="auto">
  <name>Task 3: Add invoker-level tests for session log entries and heartbeat lifecycle</name>
  <files>src/cursor-agent.invoker.test.ts, src/cursor-agent.ts</files>
  <action>Write tests that mock `runAgent()` and `fs/promises` to assert `createCursorAgentInvoker()` behavior: (1) appends a `running` session-log entry before invocation; (2) on success writes `done` with `durationMs` and captured `sessionId`; (3) on timeout writes `timeout`; (4) on crash writes `crashed` with an error message. Also verify heartbeat: when `heartbeatPath` is set, it writes an ISO timestamp at least once and unlinks it on completion (success/timeout/crash). Keep assertions resilient: check fields and statuses rather than exact timestamps.</action>
  <verify>npm test -- cursor-agent.invoker passes</verify>
  <done>Session log + heartbeat behavior is tested across success/timeout/crash outcomes</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm test passes
- [ ] Timeout classification uses an explicit boolean, not stderr substring checks
- [ ] New unit tests are hermetic (no real subprocess required)
</verification>

<success_criteria>

- All tasks completed
- Agent invocation failures are classified deterministically (success / timeout / crashed)
- Session log and heartbeat behavior are covered by tests
  </success_criteria>

<output>
After completion, create `.planning/phases/03-agent-integration/03-01-SUMMARY.md`:

# Phase 3 Plan 1: Agent invoker hardening Summary

**Cursor agent invocation is now deterministic and well-covered (spawn args, timeout/abort, session logging, heartbeat).**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `src/agent-runner.ts` — Explicit timeout signal in RunAgentResult
- `src/cursor-agent.ts` — Invoker uses explicit timeout classification; improved testability
- `src/agent-runner.spawn.test.ts` — Hermetic tests for spawn args/env/timeout abort
- `src/cursor-agent.invoker.test.ts` — Tests for session log entries and heartbeat lifecycle

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 03-02-PLAN.md (Stream parsing and completion detection)
</output>
