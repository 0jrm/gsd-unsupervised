---
phase: 03-agent-integration
plan: 02
type: execute
depends_on: ["03-01"]
files_modified: [src/stream-events.ts, src/stream-events.test.ts]
domain: node
---

<objective>
Strengthen NDJSON stream parsing and completion detection so cursor-agent runs are observable and resilient to noisy/partial streams.

Purpose: The invoker must correctly identify session start, tool activity, and final completion/failure from streaming output to support monitoring, timeouts, and future crash recovery.
Output: Improved `stream-events` extraction helpers with coverage for multi-result streams, truncated output, and unknown-but-valid payload fields.
</objective>

<execution_context>
/mnt/c/Users/jrm22n/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/ARCHITECTURE.md
@docs/CONTEXT-FOR-MODEL.md
@src/stream-events.ts
@src/cursor-agent.ts
@src/agent-runner.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix completion extraction to handle multiple result events and prefer the last terminal result</name>
  <files>src/stream-events.ts, src/stream-events.test.ts</files>
  <action>Update `extractResult(events)` to return the last `ResultEvent` in the stream (not the first), since real streams may include multiple result-ish lines or retries. Ensure behavior is deterministic. Add tests covering: (1) single result; (2) multiple results where the last indicates error; (3) multiple results where the last indicates success; (4) no result returns null. Keep `parseEvent` fail-closed (null on invalid/malformed) but tolerant of extra fields via `.passthrough()`.</action>
  <verify>npm test -- stream-events passes</verify>
  <done>`extractResult` is correct for multi-result streams and is fully covered by tests</done>
</task>

<task type="auto">
  <name>Task 2: Expand parsing coverage for noisy streams (blank lines, non-JSON, partial JSON)</name>
  <files>src/stream-events.test.ts</files>
  <action>Add tests ensuring `parseEvent` returns null for: blank/whitespace-only lines, non-JSON text, and truncated JSON. Add a test that a valid JSON event preceded/followed by noise still parses correctly when iterating line-by-line (simulate by calling `parseEvent` repeatedly). Do not change parsing semantics beyond what’s needed for reliability.</action>
  <verify>npm test -- stream-events passes</verify>
  <done>Stream parsing behavior is explicitly specified for common noisy input cases</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm test passes
- [ ] Stream completion detection is correct when multiple result events appear
- [ ] Noise cases are covered by tests (blank, non-JSON, truncated JSON)
</verification>

<success_criteria>

- All tasks completed
- Stream parsing is resilient to noisy input without masking invalid event types
- Completion detection prefers the last terminal result event
  </success_criteria>

<output>
After completion, create `.planning/phases/03-agent-integration/03-02-SUMMARY.md`:

# Phase 3 Plan 2: Stream parsing and completion detection Summary

**NDJSON parsing is more robust and completion detection correctly handles multi-result streams.**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `src/stream-events.ts` — Improved result extraction semantics
- `src/stream-events.test.ts` — Added coverage for noisy streams and multi-result cases

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase 3 complete; ready for Phase 4 (STATE monitoring & phase transitions)
</output>
