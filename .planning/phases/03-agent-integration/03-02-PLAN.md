---
phase: 03-agent-integration
plan: 02
type: execute
depends_on: ["03-01"]
files_modified:
  - src/stream-events.ts
  - src/stream-events.test.ts
  - src/agent-runner.ts
  - src/agent-runner.test.ts
  - src/cursor-agent.ts
  - src/cursor-agent.test.ts
domain:
  - node
  - cursor-agent
---

<objective>
Harden stream parsing, progress reporting, and completion detection for Cursor agent runs so orchestration can reliably determine plan success, failure, or timeout from NDJSON events.

Purpose: Provide a well-tested event decoding layer and result-extraction utilities that turn raw stream lines into typed events, session IDs, and result payloads.
Output: Robust parseEvent, extractSessionId, extractResult helpers plus tests for happy-path, malformed, and edge-case streams.
</objective>

<execution_context>
.gsd-framework/workflows/execute-plan.md
.gsd-framework/templates/summary.md
.gsd-framework/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-agent-integration/03-01-SUMMARY.md
@src/stream-events.ts
@src/agent-runner.ts
@src/cursor-agent.ts
@src/logger.ts

**Tech stack:** TypeScript, Zod, Node readline, vitest.
**Patterns:** Discriminated-union Zod schemas for Cursor NDJSON; extractSessionId, extractResult from event array.
**Decisions:** Cursor CLI emits one JSON per line with type discriminator; parsing tolerates malformed lines; completion = last result event + exit code + timedOut.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Solidify NDJSON event schemas and parsing behavior</name>
  <files>src/stream-events.ts, src/stream-events.test.ts</files>
  <action>
Refine Zod schemas and helpers in src/stream-events.ts:
- CursorStreamEventSchema: discriminated union over type (system, assistant, tool_call, result).
- parseEvent: returns CursorStreamEvent for well-formed lines, null for malformed/unknown; never throws.
- extractSessionId: first system init event session_id or null.
- extractResult: last result event from end of array or null.

Expand stream-events.test.ts:
- Valid system, assistant, tool_call, result lines.
- Invalid JSON, valid JSON with wrong type or missing fields.
- Multiple result events: extractResult returns last one.
  </action>
  <verify>npm test -- stream-events.test.ts</verify>
  <done>Event schemas robust; malformed lines ignored; callers get sessionId and final result reliably.</done>
</task>

<task type="auto">
  <name>Task 2: Propagate result and error semantics through runAgent and CursorAgentInvoker</name>
  <files>src/agent-runner.ts, src/agent-runner.test.ts, src/cursor-agent.ts, src/cursor-agent.test.ts</files>
  <action>
Ensure consistent success/failure semantics:
- runAgent: timedOut: true when timeout fired; append "Agent exited cleanly but produced no result event" to stderr when exit 0 and no result event.
- createCursorAgentInvoker: (exitCode === 0 and resultEvent) = success, return { success: true, output: resultEvent.result }; else compose error from resultEvent.is_error or exitCode + stderr snippet, return { success: false, error }.

Add/update tests:
- Exit 0 without result event path.
- Timeout bubbles to invoker error string mentioning timeout.
- Non-zero exit with parsed events: error includes exit code and available payload.
  </action>
  <verify>npm test -- agent-runner.test.ts cursor-agent.test.ts</verify>
  <done>runAgent and createCursorAgentInvoker use event helpers consistently; callers see clear success/failure; tests cover edge cases.</done>
</task>

<task type="auto">
  <name>Task 3: Verify stream parsing end-to-end</name>
  <files></files>
  <action>
Run full test suite: npm run build && npm test. Confirm stream-events, agent-runner, cursor-agent tests pass. No unhandled exceptions on malformed Cursor output.
  </action>
  <verify>npm run build && npm test</verify>
  <done>All tests pass; NDJSON parsing and completion detection stable; error messages deterministic and informative.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds
- [ ] All stream-events, agent-runner, cursor-agent tests pass
- [ ] No unhandled exceptions on malformed lines
</verification>

<success_criteria>
- NDJSON events parsed into typed discriminated union; malformed lines safely ignored
- Orchestrator determines plan success/timeout/failure from exit code, timedOut, and final result event
- Error messages deterministic, short, and debuggable
</success_criteria>

<output>
After completion, create .planning/phases/03-agent-integration/03-02-SUMMARY.md per .gsd-framework/templates/summary.md
</output>
