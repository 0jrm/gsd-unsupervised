---
phase: 01-foundation
plan: 03
type: execute
depends_on: ["01-01", "01-02"]
files_modified:
  - src/roadmap-parser.ts
  - .planning/ROADMAP.md
  - .planning/phases/
domain: roadmap-discovery
---

<objective>
Implement roadmap and phase/plan discovery from `.planning/ROADMAP.md` and `.planning/phases/` so the daemon can locate executable plans.

Purpose: Provide a single source of truth for phase metadata (numbers, names, completion state) and a filesystem-backed registry of PLAN/SUMMARY pairs for orchestration.
Output: A `roadmap-parser` module that parses the roadmap markdown into `PhaseInfo` records, locates phase directories, discovers PLAN/SUMMARY files, and reports completion status.
</objective>

<execution_context>
/mnt/c/Users/jrm22n/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@src/roadmap-parser.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parse .planning/ROADMAP.md into PhaseInfo records</name>
  <files>.planning/ROADMAP.md, src/roadmap-parser.ts</files>
  <action>
    Implement `parseRoadmap(roadmapPath: string): Promise&lt;PhaseInfo[]&gt;` that:
    - Reads the markdown file from `roadmapPath` and throws a clear error if it does not exist.
    - Ignores empty files by returning an empty array.
    - Scans lines for phase entries matching the pattern:
      `- [ ] **Phase N(.M): Name** — Description` or `- [x] **Phase N(.M): Name** — Description`.
    - Extracts:
      - `number`: numeric phase identifier (supports decimals for inserted phases).
      - `name`: the human-readable phase name.
      - `description`: the free-text description following the em dash.
      - `complete`: true when the checkbox is checked (`[x]` / `[X]`), false otherwise.
    - Derives a filesystem-safe `dirName` from the phase number and name:
      - Use a helper `phaseNumberToPrefix` that renders integers as zero-padded (e.g., `1` → `01`) and decimals with the decimal part preserved (e.g., `2.1` → `02.1`).
      - Slugify the name to lowercase, replace non-alphanumeric characters with `-`, and trim leading/trailing dashes.
      - Concatenate as `${prefix}-${slug}` (e.g., `01-foundation`).
    - Returns the resulting `PhaseInfo[]` sorted by numeric phase number ascending, so decimal insertions naturally order between integers.
  </action>
  <verify>
    - Add or update tests in `roadmap-parser.test.ts` to cover:
      - Basic integer phases matching the current `ROADMAP.md`.
      - Decimal phases (e.g., 2.1) and directory-name generation.
      - Completion flag handling for checked vs. unchecked items.
    - Run `npm test -- roadmap-parser.test.ts` (or `npm test`) and confirm all roadmap tests pass.
  </verify>
  <done>
    - `parseRoadmap` correctly returns seven phases for the current `ROADMAP.md` with matching numbers, names, descriptions, and completion flags.
    - `dirName` values match the existing `.planning/phases/*` directory names.
  </done>
</task>

<task type="auto">
  <name>Task 2: Discover phase directories and PLAN/SUMMARY files</name>
  <files>.planning/phases/, src/roadmap-parser.ts</files>
  <action>
    Implement filesystem discovery helpers to locate phase directories and PLAN/SUMMARY pairs:
    - `findPhaseDir(phasesRoot: string, phaseNumber: number): string | null`:
      - Return `null` if `phasesRoot` does not exist or cannot be read.
      - Use the same `phaseNumberToPrefix` logic as `parseRoadmap` to derive a directory prefix (e.g., `01-` or `02.1-`), then search `phasesRoot` for a directory whose name starts with that prefix.
      - Return the joined absolute path when a directory is found, or `null` otherwise.
    - `discoverPlans(phaseDir: string): Promise&lt;PlanInfo[]&gt;`:
      - Read the directory entries and match files against the pattern `^(\d+(?:\.\d+)?)-(\d+)-PLAN\.md$`.
      - For each match, derive:
        - `phaseNumber` from the first capture group (parsed as float).
        - `planNumber` from the second capture group (parsed as integer).
        - `planPath` as the full path to the PLAN file.
        - `summaryPath` by replacing `-PLAN.md` with `-SUMMARY.md`.
        - `executed` as a boolean indicating whether the summary file exists.
      - Sort the resulting `PlanInfo[]` by `planNumber` ascending.
    - `getNextUnexecutedPlan(plans: PlanInfo[]): PlanInfo | null` that returns the lowest-numbered plan with `executed === false`, or `null` if all are executed.
    - `isPhaseComplete(plans: PlanInfo[]): boolean` that returns true only when at least one plan exists and all of them have `executed === true`.
    - `isPlanCompleted(phaseDir: string, planNumber: number): boolean` that checks whether there is a `*-SUMMARY.md` file in `phaseDir` whose numeric plan suffix matches `planNumber`.

    Ensure all helpers are tolerant of missing directories and files: they should throw only when a genuinely required directory is missing (e.g., the phase directory passed to `discoverPlans`), and otherwise return `null` or empty arrays as appropriate.
  </action>
  <verify>
    - Extend `roadmap-parser.test.ts` or add a dedicated test file to cover:
      - `findPhaseDir` returning the correct `01-foundation` directory for phase 1.
      - `discoverPlans` returning three plans for `01-foundation`, each with `executed === true` when the corresponding `01-0N-SUMMARY.md` files exist.
      - `getNextUnexecutedPlan` returning `null` when all summaries exist and the first missing plan when one is absent.
      - `isPhaseComplete` and `isPlanCompleted` behaviors for complete and partial phases.
    - Run `npm test` and confirm all roadmap/plan discovery tests pass.
  </verify>
  <done>
    - The autopilot can call into `roadmap-parser` to determine which phase/plan to execute next using the existing `.planning` layout.
    - Discovery functions behave predictably even when new decimal phases or partially executed phases are introduced.
  </done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `npm test` passes with coverage for roadmap parsing and plan discovery helpers.
- [ ] For the current `.planning` tree, calling `parseRoadmap` and `discoverPlans` in a small script returns phase and plan metadata that matches the roadmap and SUMMARY files.
- [ ] No roadmap or plan-discovery logic is duplicated elsewhere; orchestrator code relies on `roadmap-parser` as the single source of truth.
</verification>

<success_criteria>

- The roadmap markdown is parsed into a sorted list of `PhaseInfo` records with correct directory naming.
- Phase directories and PLAN/SUMMARY files are discovered reliably from `.planning/phases/`.
- Callers can ask for the next unexecuted plan and phase completion status using a single module.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`:

# Phase 1 Plan 03: Roadmap and Phase Discovery Summary

**Roadmap parsing and PLAN/SUMMARY discovery implemented for the GSD autopilot.**

## Accomplishments

- Implemented `parseRoadmap`, `findPhaseDir`, `discoverPlans`, `getNextUnexecutedPlan`, `isPhaseComplete`, and `isPlanCompleted`.
- Verified that the helpers correctly interpret the existing `.planning/ROADMAP.md` and `.planning/phases/` tree.

## Files Created/Modified

- `src/roadmap-parser.ts` – Roadmap and plan-discovery helpers.
- `.planning/ROADMAP.md` – Source of phase structure and completion flags.
- `.planning/phases/*` – Phase directories housing PLAN/SUMMARY files.

## Decisions Made

- Roadmap parsing uses a strict markdown pattern so phases remain machine-readable while human-friendly.
- Phase and plan completion are determined solely by the presence of `*-SUMMARY.md` files.

## Issues Encountered

- [Document any inconsistencies between roadmap and filesystem discovered, or "None".]

## Next Step

- Phase 1 foundation is fully planned; proceed to Phase 2 lifecycle plans or re-run `/gsd/execute-phase 1` to execute these plans if needed.
</output>
