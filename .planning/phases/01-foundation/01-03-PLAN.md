---
phase: 01-foundation
plan: 03
type: execute
depends_on: ["01-02"]
files_modified: [src/roadmap-parser.ts, src/cli.ts]
domain:
---

<objective>
Discover roadmap and phases from .planning/: read ROADMAP.md, list phase directories, discover PLAN.md (and optionally SUMMARY) files per phase.

Purpose: Orchestrator needs to know phases and plans to drive execute-plan in order; this plan provides the discovery API.
Output: parseRoadmap(roadmapPath), phase directory resolution, plan discovery (PLAN.md paths per phase); CLI or API surface that demonstrates discovery.
</objective>

<execution_context>
/mnt/c/Users/jrm22n/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/roadmap-parser.ts
@src/cli.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parse ROADMAP.md and resolve phase directories</name>
  <files>src/roadmap-parser.ts</files>
  <action>Implement roadmap parsing: read ROADMAP.md (path parameter, e.g. .planning/ROADMAP.md). Parse phase lines matching format like "- [ ] **Phase N: Name** — description" or "- [x] **Phase N: Name**". Extract phase number (integer or decimal), name, description, completion. Derive phase directory name (e.g. 01-foundation) from phase number and name slug. Export parseRoadmap(roadmapPath): Promise<PhaseInfo[]> with number, name, description, dirName, complete. Provide findPhaseDir(phasesRoot, phaseNumber) returning path to phase dir or null. Phases root is typically .planning/phases. Sort phases by number.</action>
  <verify>Unit test or script: parseRoadmap('.planning/ROADMAP.md') returns phases; findPhaseDir('.planning/phases', 1) returns path to 01-* dir; decimal phases (e.g. 2.1) have correct prefix (02.1-*)</verify>
  <done>Roadmap parsed; phase list and dir resolution work for integer and decimal phases</done>
</task>

<task type="auto">
  <name>Task 2: Discover PLAN.md (and SUMMARY) per phase</name>
  <files>src/roadmap-parser.ts</files>
  <action>Implement plan discovery: given a phase directory path, list files matching pattern *-PLAN.md (e.g. 01-01-PLAN.md, 01-02-PLAN.md). Parse phase and plan number from filename; optionally resolve corresponding *-SUMMARY.md. Export getPlansInPhase(phaseDirPath): PlanInfo[] or equivalent with phaseNumber, planNumber, planPath, summaryPath (if exists), executed (e.g. derived from SUMMARY existence). Order by plan number. Handle missing or empty phase dir without throwing.</action>
  <verify>Unit test or script: getPlansInPhase('.planning/phases/01-foundation') returns plan paths in order; SUMMARY paths correct when present</verify>
  <done>PLAN.md discovery per phase; ordered list of plans with paths</done>
</task>

<task type="auto">
  <name>Task 3: Expose discovery in CLI or API</name>
  <files>src/cli.ts</files>
  <action>Wire roadmap and phase discovery to CLI: add option or subcommand (e.g. --roadmap path or 'status' / 'discover') that uses parseRoadmap and getPlansInPhase for .planning/. Print or return phase list and plan counts (or first incomplete phase/plan). Default roadmap path from config or .planning/ROADMAP.md. Do not implement plan execution—only discovery and display.</action>
  <verify>Run CLI with discovery option against this repo: shows Phase 1 and its plans (01-01, 01-02, 01-03) or equivalent</verify>
  <done>CLI can report roadmap and phase/plan discovery; foundation complete for lifecycle</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds
- [ ] parseRoadmap and findPhaseDir work for .planning/ROADMAP.md
- [ ] getPlansInPhase returns correct PLAN.md list for 01-foundation
- [ ] CLI discovery option shows phases and plans
</verification>

<success_criteria>

- All tasks completed
- Roadmap and phase/plan discovery ready for Phase 2 (lifecycle)
- No new linter or type errors
- Phase 1 Foundation complete
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`:

# Phase 1 Plan 3: Roadmap and phase discovery Summary

**[Substantive one-liner - e.g. ROADMAP.md and PLAN.md discovery from .planning/ in place]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `src/roadmap-parser.ts` - Description
- `src/cli.ts` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase 1 complete, ready for next phase (Phase 2: Lifecycle)
</output>
