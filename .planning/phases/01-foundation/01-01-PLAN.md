---
phase: 01-foundation
plan: 01
type: execute
depends_on: []
files_modified: [package.json, src/cli.ts, bin/unsupervised-gsd]
domain:
---

<objective>
Establish CLI entrypoint with Commander and ESM so the orchestrator can be invoked from the shell.

Purpose: Foundation for all later phases; config, goals, and discovery will be wired to this CLI.
Output: Working CLI binary (e.g. unsupervised-gsd) built from TypeScript, ESM, with Commander; --help and version work.
</objective>

<execution_context>
/mnt/c/Users/jrm22n/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ensure package.json is ESM and defines CLI bin</name>
  <files>package.json</files>
  <action>Ensure package.json has "type": "module", "main" pointing to built CLI (e.g. dist/cli.js), and "bin" with an entry (e.g. "unsupervised-gsd": "bin/unsupervised-gsd"). Use Node ≥18. Do not add CommonJS or "require" usage.</action>
  <verify>node -e "import('./package.json', { assert: { type: 'json' } }).then(() => console.log('ok'))" succeeds; package.json has type, main, bin</verify>
  <done>ESM and bin defined; npm run build can produce dist output</done>
</task>

<task type="auto">
  <name>Task 2: Ensure CLI entrypoint with Commander</name>
  <files>src/cli.ts</files>
  <action>Ensure a single CLI entrypoint (e.g. src/cli.ts) that uses Commander: create a Command, set name/description/version, register at least one default or primary action so the program runs. Use ESM (import from 'commander', export nothing or minimal). Version may be read from package.json at runtime. Do not add agent invocation or daemon logic here—only scaffold and option parsing if needed for next plans.</action>
  <verify>npm run build && node dist/cli.js --help (or equivalent) exits 0 and prints usage/version</verify>
  <done>CLI runs; --help and --version work; build succeeds</done>
</task>

<task type="auto">
  <name>Task 3: Ensure bin launcher invokes built CLI</name>
  <files>bin/unsupervised-gsd</files>
  <action>Ensure a bin script (e.g. bin/unsupervised-gsd) that invokes the built CLI (e.g. node dist/cli.js or node path relative to package root). Use shebang #!/usr/bin/env node. Script must be executable or runnable via node. Prefer resolving path relative to package root so it works from any cwd when run as unsupervised-gsd.</action>
  <verify>node bin/unsupervised-gsd --help (or npm run start -- --help) exits 0</verify>
  <done>Bin launcher runs the built CLI; help works when invoked via bin</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds with no TypeScript errors
- [ ] node dist/cli.js --help exits 0 and shows usage
- [ ] node dist/cli.js --version (or equivalent) shows version
- [ ] Bin launcher (node bin/unsupervised-gsd --help) works
</verification>

<success_criteria>

- All tasks completed
- Build and help/version checks pass
- No new linter or type errors
- CLI scaffold ready for config and goals wiring in 01-02
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`:

# Phase 1 Plan 1: CLI scaffold Summary

**[Substantive one-liner - e.g. CLI entrypoint with Commander and ESM in place]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `package.json` - Description
- `src/cli.ts` - Description
- `bin/unsupervised-gsd` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 01-02-PLAN.md (Config and goals)
</output>
