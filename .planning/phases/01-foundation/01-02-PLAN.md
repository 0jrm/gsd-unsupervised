---
phase: 01-foundation
plan: 02
type: execute
depends_on: ["01-01"]
files_modified: [src/config.ts, src/goals.ts, src/cli.ts, package.json, .autopilot/config.json]
domain:
---

<objective>
Load configuration from JSON (goals path, agent, timeouts, gates) with CLI overrides, and parse goals from a markdown file (pending / in progress / done).

Purpose: Config and goals drive the orchestrator; later phases depend on these being stable and validated.
Output: loadConfig() with Zod schema and CLI override support; parseGoals/loadGoals/getPendingGoals for goals markdown; CLI wired to use config and goals path.
</objective>

<execution_context>
/mnt/c/Users/jrm22n/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/config.ts
@src/goals.ts
@src/cli.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config schema and loader with CLI overrides</name>
  <files>src/config.ts</files>
  <action>Implement autopilot config as a JSON file (path from CLI option or default `./.autopilot/config.json`) with a Zod schema. Include at least: goalsPath (string), agent (supported agent id), cursorAgentPath (string), agentTimeoutMs (int ms), sessionLogPath (string), requireCleanGitBeforePlan (bool), autoCheckpoint (bool), statusServerPort (optional int). Merge CLI overrides over file values; CLI wins. Validate with safeParse; on failure throw Error with readable message. Keep this separate from `.planning/config.json` (that file belongs to the GSD planning workflow, not the daemon). Do not add agent invocation here—only config loading/validation.</action>
  <verify>Unit test or node -e: loadConfig({ configPath, cliOverrides: { goalsPath: 'x.md' } }) returns config with goalsPath 'x.md'; invalid JSON or invalid field fails validation with readable message</verify>
  <done>Config loaded from file; overrides applied; validation errors are clear</done>
</task>

<task type="auto">
  <name>Task 2: Goals markdown parsing (pending / in progress / done)</name>
  <files>src/goals.ts</files>
  <action>Implement goals parsing: read markdown file (path from config). Parse sections "## pending", "## in progress", "## done" and list items - [ ] or - [x] as goals with title and status. Export parseGoals(markdown), loadGoals(filePath), getPendingGoals(goals). Handle missing file with clear error. Empty file or no pending goals may log warning but must not throw. Return Goal[] with { title, status, raw } or equivalent.</action>
  <verify>Unit test or script: parseGoals with sample markdown returns correct status per section; getPendingGoals filters only pending; loadGoals(absent) throws with ENOENT message</verify>
  <done>Goals parsed from markdown; pending/in progress/done respected; API stable for orchestrator</done>
</task>

<task type="auto">
  <name>Task 3: Wire CLI to config and goals path</name>
  <files>src/cli.ts</files>
  <action>In CLI entrypoint, add options for --config and --goals (or equivalent). Default --config to `./.autopilot/config.json` and ensure the file can be absent (loader should fall back to schema defaults when config file path does not exist). Call loadConfig with config path and CLI overrides (goals path from option). Ensure --dry-run can load goals and show queue summary without invoking any agent/daemon.</action>
  <verify>node dist/cli.js --dry-run (with default config path) runs; node dist/cli.js --config ./.autopilot/config.json --goals ./goals.md --dry-run uses those paths</verify>
  <done>CLI uses config and goals path; dry-run or equivalent demonstrates wiring</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds
- [ ] Config load (file + overrides) and validation tested or manually verified
- [ ] Goals parsing and getPendingGoals verified with sample markdown
- [ ] CLI accepts --config and --goals and uses them (e.g. dry-run)
</verification>

<success_criteria>

- All tasks completed
- Config and goals APIs ready for lifecycle and agent phases
- No new linter or type errors
- Ready for 01-03 (roadmap and phase discovery)
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`:

# Phase 1 Plan 2: Config and goals Summary

**[Substantive one-liner - e.g. Config loader and goals markdown parsing in place]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `src/config.ts` - Description
- `src/goals.ts` - Description
- `src/cli.ts` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 01-03-PLAN.md (Roadmap and phase discovery)
</output>
