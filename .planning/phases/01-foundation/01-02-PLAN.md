---
phase: 01-foundation
plan: 02
type: execute
depends_on: ["01-01"]
files_modified:
  - src/config.ts
  - .planning/config.json
  - src/goals.ts
  - goals.md
domain: config-and-goals
---

<objective>
Define validated configuration loading and markdown-driven goals parsing for the GSD autopilot.

Purpose: Centralize daemon runtime configuration (paths, parallelism, resource guards, agent settings) and derive an execution queue from a human-editable `goals.md`.
Output: A Zod-validated `AutopilotConfig` loaded from JSON + `.planning/config.json` overrides, plus robust markdown goals parsing and execution-plan building.
</objective>

<execution_context>
/mnt/c/Users/jrm22n/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/config.json
@goals.md
@src/config.ts
@src/goals.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement validated autopilot config loading with .planning overrides</name>
  <files>src/config.ts, .planning/config.json</files>
  <action>
    Implement and maintain a Zod-based `AutopilotConfigSchema` that covers all runtime settings needed by the daemon:
    - Goals and parallelization: `goalsPath`, `parallel`, `maxConcurrent`.
    - Resource guards: `maxCpuFraction`, `maxMemoryFraction`.
    - Logging and workspace: `verbose`, `logLevel`, `workspaceRoot`.
    - Agent configuration: `agent` (enum over supported IDs), `cursorAgentPath`, `agentTimeoutMs`.
    - Operational paths and state: `sessionLogPath`, `stateWatchDebounceMs`.
    - Git/plan gating: `requireCleanGitBeforePlan`, `autoCheckpoint`.
    - Optional `statusServerPort` for the HTTP status server.

    In `loadConfig`:
    - Read `configPath` JSON if present, otherwise default to an empty object.
    - Derive `workspaceRoot` from CLI overrides, config file, or `process.cwd()` in that order.
    - Read `.planning/config.json` via `readPlanningOverrides(workspaceRoot)` and allow only a small, safe subset of overrides (`autoCheckpoint`, `maxConcurrent`, `maxCpuFraction`, `maxMemoryFraction`) to flow into the runtime config.
    - Merge file values, planning overrides, and CLI overrides (with CLI taking precedence), stripping any `undefined` values.
    - Validate the merged config with `AutopilotConfigSchema.safeParse`; if validation fails, throw an error that includes a human-readable bullet list of issues.

    Keep `readPlanningOverrides` resilient:
    - If `.planning/config.json` is missing or invalid JSON, return an empty override object rather than failing config loading.
    - Only attach fields that are of the correct primitive types; ignore anything else.
  </action>
  <verify>
    - Run `npm test -- config.test.ts` (or `npm test` if the suite is small) and confirm all config tests pass.
    - Manually test a few combinations via Node REPL or a small script:
      - `loadConfig({ configPath: undefined, cliOverrides: {} })` returns defaults and respects `.planning/config.json`.
      - Passing an invalid `agent` value triggers a validation error that lists the offending field.
  </verify>
  <done>
    - Config loads successfully when only `.planning/config.json` is present.
    - CLI overrides take precedence over both config file and planning overrides.
    - Invalid config values fail fast with clear, actionable error messages.
  </done>
</task>

<task type="auto">
  <name>Task 2: Parse goals markdown into an execution plan</name>
  <files>src/goals.ts, goals.md</files>
  <action>
    Implement resilient goals parsing and execution-plan building:
    - Treat `goals.md` as a structured queue with three sections: `## pending`, `## in progress`, and `## done`.
    - For each section, parse markdown checklist items of the form `- [ ] Title` or `- [x] Title` and map them to a `Goal` object with `title`, `status`, and `raw` line.
    - Normalize newlines and ignore any content before the first recognized section header.

    Add lightweight annotations extracted from square-bracket tokens embedded in the line:
    - `[group:alpha]` → `parallelGroup: "alpha"`.
    - `[after:beta,gamma]` → `dependsOn: ["beta","gamma"]`.
    - `[priority:1]` → numeric `priority: 1`.
    Strip these tokens from the human-facing `title` but preserve them in the structured fields.

    Implement helpers:
    - `parseGoals(markdown: string): Goal[]` that handles normalization and annotation parsing.
    - `loadGoals(filePath: string)` that reads the file via `fs/promises`, throws a clear error if missing, tolerates empty files by returning an empty array, and warns when there are no pending goals.
    - `getPendingGoals(goals)` to filter by `status === 'pending'`.
    - `buildExecutionPlan(goals)` that sorts goals into an `ExecutionPlan` by:
      - Primary key: explicit numeric `priority` (ascending; missing priority maps to `Number.MAX_SAFE_INTEGER`).
      - Secondary key: original order in the file (stable sort).
      Attach `parallelGroup` and `dependsOn` metadata to plan items without enforcing any scheduling semantics yet.
  </action>
  <verify>
    - Add or update tests in `src/goals.test.ts` (or equivalent) to cover:
      - Section-based status assignment and basic checkbox parsing.
      - Annotation parsing for `group`, `after`, and `priority` tokens.
      - Execution-plan ordering for mixed-priority goals.
    - Run `npm test` and confirm all goals-related tests pass.
    - Run `npm run build` followed by `node dist/cli.js --dry-run --goals ./goals.md` and confirm it prints a queue that matches the parsed `goals.md`.
  </verify>
  <done>
    - Goals markdown in `goals.md` round-trips through `loadGoals` and `buildExecutionPlan` with correct statuses and ordering.
    - Missing or empty `goals.md` produces a clear error or an empty queue without crashing the daemon.
    - Dry-run CLI output reflects the parsed goals accurately, including pending count.
  </done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `npm test` (or at minimum config/goals test suites) passes successfully.
- [ ] `npm run build` succeeds and `node dist/cli.js --dry-run --goals ./goals.md` prints a correct summary of the current goals queue.
- [ ] Modifying `.planning/config.json` with allowed override fields (e.g. `maxCpuFraction`, `maxConcurrent`) changes runtime behavior without breaking validation.
</verification>

<success_criteria>

- Autopilot configuration is fully validated and can be driven by a combination of CLI flags, a JSON config file, and `.planning/config.json` overrides.
- Goals are derived solely from `goals.md` using a predictable structure and lightweight annotations for parallelization hints.
- Dry-run mode and daemon startup both rely on the same config and goals parsing, avoiding duplicated logic or divergent behavior.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`:

# Phase 1 Plan 02: Config and Goals Summary

**Validated config loading and markdown-based goals queue established for the autopilot.**

## Accomplishments

- Implemented `AutopilotConfigSchema` and `loadConfig` with `.planning/config.json` override support.
- Implemented `parseGoals`, `loadGoals`, `getPendingGoals`, and `buildExecutionPlan` to drive execution from `goals.md`.

## Files Created/Modified

- `src/config.ts` – Zod schema, config loading, and planning overrides.
- `.planning/config.json` – Project-level planning and parallelization defaults.
- `src/goals.ts` – Goals parsing and execution-plan utilities.
- `goals.md` – Structured goals queue used during development and tests.

## Decisions Made

- `.planning/config.json` is permitted to override only a safe subset of runtime flags.
- Goal annotations (`group`, `after`, `priority`) provide non-invasive hints for future parallel scheduling.

## Issues Encountered

- [Document any edge cases in parsing or validation, or "None".]

## Next Step

- Ready for `01-03-PLAN.md` to wire roadmap and phase/plan discovery against `.planning/ROADMAP.md` and `.planning/phases/`.
</output>
