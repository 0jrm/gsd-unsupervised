---
phase: 08-cn-continue-cli
plan: 03
type: execute
depends_on: ["08-02"]
files_modified:
  - src/cn-output.test.ts
  - src/cursor-agent.cn.test.ts
  - src/agent-runner.test.ts
  - src/config.test.ts
  - README.md
domain: infra
---

<objective>
Add unit tests for cn adapter and parseCnOutput, update agent-runner and config tests for cn, and add README documentation for Using cn (Continue CLI).

Purpose: Ensure cn integration is well-tested and documented. Regression tests for SUPPORTED_AGENTS, isSupportedAgent, config validation.
Output: cn-output.test.ts, cursor-agent.cn.test.ts, updated agent-runner.test.ts and config.test.ts, README "Using cn" section.
</objective>

<execution_context>
/mnt/c/Users/jrm22n/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cn-continue-cli/08-01-SUMMARY.md
@.planning/phases/08-cn-continue-cli/08-02-SUMMARY.md
@src/agent-runner.spawn.test.ts
@src/cursor-agent.invoker.test.ts
@src/agent-runner.test.ts
@src/config.test.ts
@src/stream-events.test.ts
@README.md

**Tech stack available:** Vitest, vi.mock for spawn/tree-kill
**Established patterns:** agent-runner.spawn.test.ts mocks spawn, creates PassThrough for stdout/stderr, emits close; config.test.ts tests agent enum acceptance and rejection
**Constraining decisions:** All existing tests must pass; do not break cursor
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit tests for cn adapter and parseCnOutput</name>
  <files>src/cn-output.test.ts, src/cursor-agent.cn.test.ts</files>
  <action>
- Create src/cn-output.test.ts: Test parseCnOutput with (1) empty string → hasError false, summary "No output" or similar, (2) output containing "Error:" → hasError true, (3) output containing "Failed" → hasError true, (4) normal output → hasError false, summary is truncated. Follow stream-events.test.ts style.
- Create src/cursor-agent.cn.test.ts (or add to cursor-agent.test.ts as describe block): Mock spawn and tree-kill like agent-runner.spawn.test.ts. Test runContinueCli: (1) exit 0 → success, resultEvent.result from stdout, (2) exit 1 → crashed, (3) timeout → timedOut true, treeKill called. Use same createMockChildProcess pattern. Mock getCnBinaryPath if needed to avoid path resolution in tests.
  </action>
  <verify>npm test runs cn-output.test.ts and cursor-agent.cn.test.ts; all pass.</verify>
  <done>parseCnOutput and runContinueCli have unit tests covering empty, error, normal, timeout.</done>
</task>

<task type="auto">
  <name>Task 2: Update agent-runner.test.ts and config.test.ts</name>
  <files>src/agent-runner.test.ts, src/config.test.ts</files>
  <action>
- agent-runner.test.ts: Update SUPPORTED_AGENTS test — expect length 5 (cursor, claude-code, gemini-cli, codex, cn), add expect(SUPPORTED_AGENTS).toContain('cn'). Update isSupportedAgent test — add expect(isSupportedAgent('cn')).toBe(true).
- config.test.ts: Add it('accepts agent cn', ...) with loadConfig({ cliOverrides: { agent: 'cn' } }), expect config.agent to be 'cn'. Update the "rejects invalid agent" test — the expected error message will now include 'cn' in the enum list; ensure the regex or assertion still passes (rejects 'bogus-agent').
  </action>
  <verify>npm test for agent-runner.test.ts and config.test.ts; SUPPORTED_AGENTS includes cn, config accepts cn, rejects bogus-agent.</verify>
  <done>agent-runner and config tests updated for cn.</done>
</task>

<task type="auto">
  <name>Task 3: Add README "Using cn (Continue CLI)" section</name>
  <files>README.md</files>
  <action>
Add a new section "Using cn (Continue CLI)" after the Cursor/prerequisites section (or in a logical place). Cover:

- Install: curl install script or npm (from docs.continue.dev/guides/cli)
- Config: Fill in model in .continue/config.yaml (placeholder exists; user adds their model)
- Set agent: --agent cn or "agent": "cn" in .planning/config.json
- CONTINUE_API_KEY env var for CI/headless use
- Binary path: GSD_CN_BIN env or continueCliPath in config
- Note: cn uses plain-text output; GSD rules load from .continue/config.yaml which references .cursor/rules/
  </action>
  <verify>README contains the section; user can follow it to use cn.</verify>
  <done>README documents cn setup and usage.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] npm run build succeeds
- [ ] npm test passes (all tests including new cn tests)
- [ ] README "Using cn" section is complete
- [ ] No regressions in cursor agent tests
</verification>

<success_criteria>

- parseCnOutput: unit test for empty, error-containing, normal output
- runContinueCli: unit test for exit 0 success, nonzero crashed, timeout
- SUPPORTED_AGENTS includes 'cn', isSupportedAgent('cn') true
- config accepts agent 'cn', rejects unknown agents
- README has "Using cn (Continue CLI)" section
- All existing tests pass
  </success_criteria>

<output>
After completion, create `.planning/phases/08-cn-continue-cli/08-03-SUMMARY.md`:

# Phase 8 Plan 3: Tests and documentation Summary

**[Substantive one-liner]**

## Accomplishments

## Files Created/Modified

## Decisions Made

## Issues Encountered

## Next Step

Phase 8 complete. cn is first-class supported agent alongside cursor.
</output>
