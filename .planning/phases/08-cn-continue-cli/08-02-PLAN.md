---
phase: 08-cn-continue-cli
plan: 02
type: execute
depends_on: ["08-01"]
files_modified:
  - src/agent-runner.ts
  - src/config.ts
  - src/cursor-agent.ts
  - src/cli.ts
  - .continue/config.yaml
domain: infra
---

<objective>
Wire cn into the agent registry, create createContinueCliInvoker and add case 'cn' in createAgentInvoker, create .continue/config.yaml for GSD rules, and add CONTINUE_API_KEY validation when agent is cn.

Purpose: cn must be selectable via --agent cn or "agent": "cn" in .planning/config.json. GSD rules must load for cn via .continue/config.yaml. API key validation must run for cn like cursor.
Output: SUPPORTED_AGENTS includes 'cn', createAgentInvoker returns cn invoker, .continue/config.yaml exists, validateContinueApiKey when agent is cn.
</objective>

<execution_context>
/mnt/c/Users/jrm22n/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cn-continue-cli/08-01-SUMMARY.md
@src/agent-runner.ts
@src/config.ts
@src/cursor-agent.ts
@src/cli.ts

**Tech stack available:** runContinueCli, parseCnOutput, getCnBinaryPath, continueCliPath
**Established patterns:** createCursorAgentInvoker structure; validateCursorApiKey in cli.ts before daemon; AgentIdSchema from SUPPORTED_AGENTS
**Constraining decisions:** Do not break cursor; cn as optional peer; if binary missing, clear error
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cn to agent registry and createAgentInvoker</name>
  <files>src/agent-runner.ts, src/config.ts, src/cursor-agent.ts</files>
  <action>
- In agent-runner.ts: Add 'cn' to AgentId type and SUPPORTED_AGENTS array. isSupportedAgent will automatically include 'cn' via the array.
- In config.ts: AgentIdSchema uses SUPPORTED_AGENTS, so it will accept 'cn' after agent-runner update. Add agent to readPlanningOverrides: if parsed.agent is a string and isSupportedAgent(parsed.agent), set overrides.agent so .planning/config.json can set "agent": "cn".
- In cursor-agent.ts: Add createContinueCliInvoker(agentConfig, callbacks) modeled on createCursorAgentInvoker. It uses runContinueCli (from 08-01), same heartbeat/session-log pattern, maps RunAgentResult-like to AgentResult. Add case 'cn' in createAgentInvoker: return createContinueCliInvoker with agentPath from getCnBinaryPath, defaultTimeoutMs, sessionLogPath, heartbeatPath, etc. Remove 'cn' from stub list (claude-code, gemini-cli, codex remain stubs).
  </action>
  <verify>createAgentInvoker('cn', config) returns invoker that calls runContinueCli; loadConfig with agent 'cn' succeeds; isSupportedAgent('cn') is true.</verify>
  <done>cn in SUPPORTED_AGENTS, createAgentInvoker handles 'cn', .planning/config.json agent override works.</done>
</task>

<task type="auto">
  <name>Task 2: Create .continue/config.yaml and wire --config</name>
  <files>.continue/config.yaml, src/cursor-agent.ts</files>
  <action>
- Create .continue/config.yaml at repo root. Use Continue config format (name, version, schema: v1). In rules section, add entries that load GSD rules from .cursor/rules/. Continue supports file:// paths. Use relative paths like file://.cursor/rules/gsd-system.mdc or list each rule file. Document in comments: "GSD rules for cn. Fill in models section with your model." Leave models as placeholder (user fills in).
- In createContinueCliInvoker / runContinueCli: Before spawning, check if path.join(workspaceRoot, '.continue', 'config.yaml') exists. If so, add --config &lt;absolutePath&gt; to cn args. This ensures GSD rules are active when cn runs in a GSD project.
  </action>
  <verify>.continue/config.yaml exists, has rules pointing to .cursor/rules; runContinueCli includes --config when file exists.</verify>
  <done>.continue/config.yaml loads GSD rules; cn adapter passes --config when file exists.</done>
</task>

<task type="auto">
  <name>Task 3: Add validateContinueApiKey and wire in cli.ts</name>
  <files>src/cursor-agent.ts, src/cli.ts</files>
  <action>
- In cursor-agent.ts: Add validateContinueApiKey(): void. Check process.env.CONTINUE_API_KEY; if missing or empty, throw Error with message directing user to continue.dev/settings/api-keys and CONTINUE_API_KEY env var for CI/headless.
- In cli.ts: Extend the API key check block. Currently: if !dryRun && config.agent === 'cursor', validateCursorApiKey(). Add: if !dryRun && config.agent === 'cn', validateContinueApiKey(). Update --agent option description to include cn.
  </action>
  <verify>With agent cn and no CONTINUE_API_KEY, cli exits 1 with clear message. With CONTINUE_API_KEY set, proceeds.</verify>
  <done>validateContinueApiKey exists; cli validates CONTINUE_API_KEY when agent is cn.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] npm run build succeeds
- [ ] npm test passes
- [ ] createAgentInvoker('cn', config) returns working invoker
- [ ] .continue/config.yaml valid YAML, rules reference .cursor/rules
</verification>

<success_criteria>

- SUPPORTED_AGENTS includes 'cn'
- isSupportedAgent('cn') is true
- createAgentInvoker('cn') returns createContinueCliInvoker
- .continue/config.yaml exists with GSD rules
- cn adapter passes --config when .continue/config.yaml exists
- validateContinueApiKey runs when agent is cn
- All existing tests pass
  </success_criteria>

<output>
After completion, create `.planning/phases/08-cn-continue-cli/08-02-SUMMARY.md`:

# Phase 8 Plan 2: Agent registry and GSD rules Summary

**[Substantive one-liner]**

## Accomplishments

## Files Created/Modified

## Decisions Made

## Issues Encountered

## Next Step

Ready for 08-03-PLAN.md (Tests and documentation)
</output>
