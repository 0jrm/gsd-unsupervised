---
phase: 08-cn-continue-cli
plan: 01
type: execute
depends_on: []
files_modified:
  - src/cn-output.ts
  - src/cursor-agent.ts
  - src/config.ts
  - src/config/paths.ts
domain: infra
---

<objective>
Implement the core cn (Continue CLI) adapter: runContinueCli function, parseCnOutput for plain-text completion detection, and config fields for binary path.

Purpose: cn outputs plain text (not NDJSON). We need a dedicated spawn path, timeout/abort handling, and lightweight output parsing for session logging.
Output: runContinueCli in cursor-agent.ts, parseCnOutput in cn-output.ts, continueCliPath config, getCnBinaryPath in paths.ts.
</objective>

<execution_context>
/mnt/c/Users/jrm22n/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-agent-integration/03-01-SUMMARY.md
@.planning/phases/03-agent-integration/03-02-SUMMARY.md
@src/cursor-agent.ts
@src/agent-runner.ts
@src/stream-events.ts
@src/config.ts
@src/config/paths.ts

**Tech stack available:** tree-kill, spawn, explicit timedOut field, session log, heartbeat
**Established patterns:** Timeout signaled by RunAgentResult.timedOut; invoker uses appendSessionLog; getCursorBinaryPath uses GSD_CURSOR_BIN env, then config.cursorAgentPath
**Constraining decisions:** Do not break cursor (default agent); cn is optional peer — if binary missing, fail with clear error
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add parseCnOutput in src/cn-output.ts</name>
  <files>src/cn-output.ts</files>
  <action>
Create src/cn-output.ts with parseCnOutput(stdout: string): { hasError: boolean; summary: string }.

- hasError: true if output contains "Error:", "Failed", or similar error indicators (case-insensitive substring match). Do NOT try to parse NDJSON — cn outputs plain text.
- summary: first ~200 chars of stdout trimmed, or "No output" if empty. Used for session log.
- Export the function. Keep it lightweight — no complex parsing.
  </action>
  <verify>Import parseCnOutput from cn-output.ts and call with empty string, "Error: something", and normal output — assert hasError and summary values.</verify>
  <done>parseCnOutput exists, handles empty/error/normal cases, exported.</done>
</task>

<task type="auto">
  <name>Task 2: Add runContinueCli in cursor-agent.ts</name>
  <files>src/cursor-agent.ts</files>
  <action>
Add runContinueCli function in cursor-agent.ts (or a new cn-agent.ts if cleaner — user said cursor-agent.ts). Model it on runAgentOnce in agent-runner.ts:

- Spawn: cn -p "&lt;prompt&gt;" --allow Write() --allow Bash() --allow Read()
- Add --config &lt;path&gt; when config path is provided (handled in 08-02; for now accept optional configPath param, pass through if set).
- Binary path from getCnBinaryPath(config) — add getCnBinaryPath in config/paths.ts: GSD_CN_BIN env, then config.continueCliPath, default 'cn'.
- Respect agentTimeoutMs: same abort pattern as agent-runner (setTimeout → treeKill on child).
- Collect stdout and stderr. On close: exitCode, timedOut flag.
- Return RunAgentResult-like: { sessionId: null, resultEvent: synthetic from parseCnOutput(stdout) for session log compatibility, exitCode, timedOut, stderr }. For success (exit 0): resultEvent = { result: stdout, is_error: false }. For failure: resultEvent from parseCnOutput or null.
- Do NOT use runAgentWithRetry — cn has different retry semantics; use single attempt for now (retry can be added later if needed).
- If spawn fails (binary not found), throw clear error: "cn binary not found. Install via: curl ... or npm. Set GSD_CN_BIN or continueCliPath if installed elsewhere."
  </action>
  <verify>Unit test mocks spawn; runContinueCli returns success on exit 0, crashed on nonzero, timedOut on abort. Same pattern as agent-runner.spawn.test.ts.</verify>
  <done>runContinueCli spawns cn with correct args, respects timeout, returns RunAgentResult-compatible shape.</done>
</task>

<task type="auto">
  <name>Task 3: Add continueCliPath config and getCnBinaryPath</name>
  <files>src/config.ts, src/config/paths.ts</files>
  <action>
- In config.ts: Add continueCliPath: z.string().default('cn') to AutopilotConfigSchema, same pattern as cursorAgentPath.
- In config/paths.ts: Add getCnBinaryPath(config: AutopilotConfig): string. Use GSD_CN_BIN env if set and non-empty, else config.continueCliPath ?? 'cn'. No WSL path mapping for cn (simpler; can add later if needed).
  </action>
  <verify>loadConfig with continueCliPath override; getCnBinaryPath returns env override when GSD_CN_BIN set.</verify>
  <done>continueCliPath in schema, getCnBinaryPath in paths.ts.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] npm run build succeeds
- [ ] npm test passes (including new parseCnOutput and runContinueCli tests)
- [ ] No changes to cursor agent behavior
</verification>

<success_criteria>

- parseCnOutput handles empty, error-containing, and normal output
- runContinueCli spawns cn with -p, --allow Write(), --allow Bash(), --allow Read()
- runContinueCli respects timeout and tree-kill abort
- continueCliPath config and getCnBinaryPath exist
- All existing tests pass
  </success_criteria>

<output>
After completion, create `.planning/phases/08-cn-continue-cli/08-01-SUMMARY.md`:

# Phase 8 Plan 1: cn adapter core Summary

**[Substantive one-liner]**

## Accomplishments

## Files Created/Modified

## Decisions Made

## Issues Encountered

## Next Step

Ready for 08-02-PLAN.md (Agent registry, createAgentInvoker, GSD rules)
</output>
