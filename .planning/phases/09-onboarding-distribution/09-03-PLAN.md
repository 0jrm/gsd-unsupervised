---
phase: 09-onboarding-distribution
plan: 03
type: execute
depends_on: ["09-01", "09-02"]
files_modified:
  - docs/launch-post-draft.md
  - README.md
  - package.json
  - CHANGELOG.md
domain: node
---

<objective>
Finalize the launch post, verify npm publish readiness, and ensure the full first-run
experience (clone → setup → run → SMS) is described end-to-end in both the launch post
and README.

This plan does NOT require publishing to npm — it prepares everything so publishing
is a single `npm publish` command with confidence.

Output:
- docs/launch-post-draft.md — publish-ready, no placeholder links, concrete demo steps
- README.md — accurate agent table, WSL section, cn section, quick-start all consistent
- package.json — version bumped to 1.1.0, files array includes .gsd-framework/
- CHANGELOG.md — [1.1.0] entry written
- A self-contained npm pack dry-run that shows no missing files
</objective>

<execution_context>
/gsd/execute-plan
.planning/STATE.md
</execution_context>

<context>
@.planning/STATE.md
@docs/launch-post-draft.md
@README.md
@package.json
@CHANGELOG.md
@.planning/OPPORTUNITY.md
</context>

<tasks>

<task type="auto">
  <n>Task 1: Rewrite launch post to be publish-ready</n>
  <files>docs/launch-post-draft.md</files>
  <action>
    Read the current docs/launch-post-draft.md. It's a solid draft but has:
    - "[link to your waitlist or paid tier]" placeholder
    - No concrete "here's how to run it right now" steps
    - The demo flow is vague

    Rewrite it as a complete, publishable HN/dev.to post:

    1. Keep the hook: "I built a daemon that runs my AI coding agent overnight while I sleep"
    2. Add a concrete "Try it in 2 minutes" block immediately after the intro:
       ```
       git clone https://github.com/0jrm/gsd-unsupervised
       cd your-project && ./setup.sh
       echo "- [ ] Add auth to my app" >> goals.md
       ./run
       ```
    3. Replace the paid tier placeholder with:
       "If you want a hosted version where goals keep running even when your laptop sleeps,
       reply to this post or open an issue — I'm gauging interest."
    4. Add a "What's working right now" section listing concrete features:
       - Multi-agent support: Cursor, Claude Code, Continue (cn), Gemini CLI, Codex
       - SMS notifications via Twilio (goal started / complete / crashed)
       - Crash recovery: resumes from last known-good plan on restart
       - Status server at localhost:3000
       - Configurable via goals.md (plain text queue) and .planning/config.json
    5. Keep it under 800 words total — the current draft is about right in length.

    Commit: "docs(09-03): finalize launch post — publish-ready, concrete demo steps"
  </action>
  <verify>No placeholder text like "[link to..." remains. Post is under 800 words.</verify>
  <done>Launch post is publish-ready with no TODOs or placeholders</done>
</task>

<task type="auto">
  <n>Task 2: Audit README for consistency with current state</n>
  <files>README.md</files>
  <action>
    Read the full README.md. Verify and fix these specific sections:

    1. Agent table — should list all 5 agents: cursor, cn, claude-code, gemini-cli, codex.
       For cn it should show: install via `npm install -g @continuedev/cli`, set CONTINUE_API_KEY.
       For claude-code/gemini-cli/codex mark as "stub (coming soon)" if they're still stubs.

    2. Configuration table — should include `continueCliPath` (path to cn binary, default "cn").

    3. WSL section — should reference `scripts/bootstrap-wsl.sh` as the entry point.

    4. Quick-start — should match the setup.sh flow from 09-02 exactly.

    5. SMS/Twilio section — should explain the 3 message types (started/complete/crashed)
       and that setup.sh will prompt for Twilio credentials.

    6. Remove any references to commands or flags that don't exist in dist/cli.js.
       Run: node dist/cli.js --help and compare against documented commands.

    Fix any inconsistencies found. Don't add new sections — only fix what's wrong.
    Commit: "docs(09-03): sync README with current feature state"
  </action>
  <verify>
    Every command shown in README exists in node dist/cli.js --help output.
    Agent table lists all 5 agents.
    No broken internal links (grep for .md references and verify they exist).
  </verify>
  <done>README accurately reflects what ships in the current version</done>
</task>

<task type="auto">
  <n>Task 3: Bump version to 1.1.0 and write CHANGELOG entry</n>
  <files>package.json, CHANGELOG.md</files>
  <action>
    1. In package.json, bump version from 1.0.3 to 1.1.0.

    2. Verify package.json "files" array includes:
       "dist", "bin", ".gsd-framework", "setup.sh"
       Add any that are missing.

    3. In CHANGELOG.md, add a [1.1.0] entry above [1.0.2]:

    ## [1.1.0] — <today's date>

    ### Summary
    Developer onboarding overhaul: GSD framework bundled into repo (no more local path
    dependencies), interactive setup.sh wizard, fixed init/run CLI commands, and
    updated documentation.

    ### Added
    - `.gsd-framework/` — GSD workflows, templates, and references bundled into the repo.
      Any developer who clones can now use all GSD commands without external dependencies.
    - `setup.sh` — interactive first-run wizard (agent type, goals path, port, optional Twilio).
    - `gsd-unsupervised init` CLI command for non-interactive initialization.

    ### Fixed
    - All `.cursor/rules/*.mdc` files previously referenced hardcoded local paths
      (`/mnt/c/Users/jrm22n/...`). Replaced with repo-relative `.gsd-framework/` paths.
    - `gsd-unsupervised run` now prints an actionable error when not yet initialized
      instead of crashing.

    ### Changed
    - README quick-start rewritten to match the setup.sh → run flow.
    - npm package now includes `.gsd-framework/` and `setup.sh`.

    Commit: "chore(09-03): bump version to 1.1.0, update CHANGELOG"
  </action>
  <verify>
    package.json version is 1.1.0.
    package.json files array includes .gsd-framework and setup.sh.
    CHANGELOG has [1.1.0] entry.
  </verify>
  <done>Version bumped and CHANGELOG written for 1.1.0 release</done>
</task>

<task type="auto">
  <n>Task 4: npm pack dry-run and final checklist</n>
  <files>— (verification only)</files>
  <action>
    Run: npm run build && npm test
    Run: npm pack --dry-run 2>&1 | head -60

    Verify the dry-run output includes:
      - dist/cli.js and other dist files
      - bin/gsd-unsupervised
      - .gsd-framework/ files
      - setup.sh
      - README.md
      - package.json

    If anything is missing from the pack, fix package.json "files" array and re-run.

    Then update STATE.md:
      Phase: 9 of 9 (Onboarding & Distribution)
      Plan: 3 of 3 in current phase
      Status: Phase complete
      Progress: Phase 9 complete

    Final commit: "docs(09-03): complete phase 9 — onboarding and distribution ready"

    Print a summary of what to do to actually publish:
      npm publish --access public
      git tag v1.1.0 && git push origin v1.1.0
      Post docs/launch-post-draft.md to HN / dev.to
  </action>
  <verify>
    npm run build passes.
    npm test passes.
    npm pack --dry-run shows .gsd-framework/ files.
    STATE.md shows Phase 9 complete.
  </verify>
  <done>
    Project is publish-ready. npm publish and the launch post are the only remaining steps.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build passes
- [ ] npm test passes
- [ ] npm pack --dry-run includes .gsd-framework/ and setup.sh
- [ ] No placeholder text in docs/launch-post-draft.md
- [ ] CHANGELOG has [1.1.0] entry
- [ ] package.json version is 1.1.0
- [ ] STATE.md shows Phase 9 complete
</verification>

<success_criteria>
- Publishing is a single `npm publish` command
- Launch post is ready to copy-paste to HN or dev.to
- README and launch post are consistent with each other
- npm test passes (no regressions)
</success_criteria>

<o>
After completion, create .planning/phases/09-onboarding-distribution/09-03-SUMMARY.md.
Update STATE.md to Phase 9 of 9, Plan 3 of 3 complete. Phase 9 complete.
</o>
