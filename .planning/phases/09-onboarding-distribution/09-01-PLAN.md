---
phase: 09-onboarding-distribution
plan: 01
type: execute
depends_on: []
files_modified:
  - .cursor/rules/*.mdc
  - .gsd-framework/workflows/
  - .gsd-framework/templates/
  - .gsd-framework/references/
domain: node
---

<objective>
Bundle the GSD framework files (workflows, templates, references) into the repo and rewrite
all .cursor/rules/*.mdc files to use repo-relative paths instead of
/mnt/c/Users/jrm22n/.cursor/get-shit-done/... hardcoded paths.

Purpose: Right now every rule references files that only exist on the author's machine.
Nobody else can onboard. Fix this so the repo is self-contained.

Output:
- .gsd-framework/ directory with all workflow/template/reference markdown files
- All .cursor/rules/*.mdc files updated to use file://.gsd-framework/... paths
- README section explaining the framework directory
</objective>

<execution_context>
/gsd/execute-plan
.planning/STATE.md
.planning/config.json
</execution_context>

<context>
@.planning/STATE.md
@.planning/config.json
@.cursor/rules/gsd-system.mdc
@.cursor/rules/gsd-execute-plan.mdc
@.cursor/rules/gsd-plan-phase.mdc
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit all hardcoded paths in .cursor/rules/</name>
  <files>.cursor/rules/*.mdc</files>
  <action>
    Run: grep -r "/mnt/c/Users/jrm22n/.cursor/get-shit-done" .cursor/rules/ --include="*.mdc" -l
    to get the full list of affected files. Then for each file, list every hardcoded path
    referenced (Read file: ..., @path, uses: file://...). Produce a mapping:
      hardcoded path → new repo-relative path under .gsd-framework/
    e.g.:
      /mnt/c/Users/jrm22n/.cursor/get-shit-done/workflows/execute-plan.md
        → .gsd-framework/workflows/execute-plan.md
    Write this mapping to .planning/phases/09-onboarding-distribution/path-mapping.json
    so it can be referenced by subsequent tasks.
  </action>
  <verify>path-mapping.json exists and has at least 10 entries</verify>
  <done>Full list of files and paths that need updating is captured</done>
</task>

<task type="auto">
  <name>Task 2: Create .gsd-framework/ directory with stub files</name>
  <files>.gsd-framework/**</files>
  <action>
    Using path-mapping.json from Task 1, create the .gsd-framework/ directory tree
    and populate each file. For files that can be reconstructed from the rules that
    reference them (the rules describe what they contain), write the content now.
    For workflows, create each .md file with the workflow steps inferred from the
    corresponding .cursor/rules/*.mdc that references it — the rules contain the
    process steps inline which is sufficient to reconstruct the workflow content.

    Minimum required files (infer from rules):
      .gsd-framework/workflows/execute-plan.md
      .gsd-framework/workflows/resume-project.md
      .gsd-framework/workflows/plan-phase.md
      .gsd-framework/workflows/create-roadmap.md
      .gsd-framework/workflows/debug.md
      .gsd-framework/workflows/verify-work.md
      .gsd-framework/workflows/discuss-phase.md
      .gsd-framework/workflows/research-phase.md
      .gsd-framework/templates/summary.md
      .gsd-framework/templates/context.md
      .gsd-framework/templates/phase-prompt.md
      .gsd-framework/templates/roadmap.md
      .gsd-framework/templates/state.md
      .gsd-framework/templates/DEBUG.md
      .gsd-framework/references/plan-format.md
      .gsd-framework/references/scope-estimation.md
      .gsd-framework/references/checkpoints.md
      .gsd-framework/references/tdd.md
      .gsd-framework/references/git-integration.md

    Each file should contain the process/template content extracted from the rules.
    Add a comment at the top: "# Auto-extracted from .cursor/rules/ — expand as needed"
    Also create .gsd-framework/README.md explaining the directory.
  </action>
  <verify>find .gsd-framework -name "*.md" | wc -l shows >= 18 files</verify>
  <done>.gsd-framework/ exists and is populated with workflow/template/reference files</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite .cursor/rules/*.mdc to use repo-relative paths</name>
  <files>.cursor/rules/*.mdc</files>
  <action>
    For each .mdc file identified in Task 1, replace every hardcoded
    /mnt/c/Users/jrm22n/.cursor/get-shit-done/... reference with the
    corresponding .gsd-framework/... path using the mapping from Task 1.

    Replace patterns:
      "Read file: `/mnt/c/Users/jrm22n/.cursor/get-shit-done/X`"
        → "Read file: `.gsd-framework/X`"
      "uses: file:///mnt/c/Users/jrm22n/.cursor/get-shit-done/X"
        → "uses: file://.gsd-framework/X"
      "@/mnt/c/Users/..." style references similarly

    Also update .continue/config.yaml which has:
      uses: file://../.cursor/rules/gsd-execute-phase.mdc  (file doesn't exist — remove or stub)
      uses: file://../.cursor/rules/gsd-help.mdc           (file doesn't exist — remove or stub)
    Create .cursor/rules/gsd-execute-phase.mdc and gsd-help.mdc as minimal stubs
    referencing the appropriate .gsd-framework workflows so the cn config doesn't error.

    Commit: "fix(09-01): replace hardcoded paths in .cursor/rules with repo-relative .gsd-framework/ paths"
  </action>
  <verify>
    grep -r "/mnt/c/Users/jrm22n" .cursor/rules/ should return 0 results.
    grep -r "/mnt/c/Users/jrm22n" .continue/ should return 0 results.
  </verify>
  <done>All rules use .gsd-framework/ paths; zero hardcoded user paths remain</done>
</task>

<task type="auto">
  <name>Task 4: Add .gsd-framework/ to .gitignore exclusions and commit everything</name>
  <files>.gitignore, .gsd-framework/**</files>
  <action>
    1. Verify .gitignore does NOT exclude .gsd-framework/ (it shouldn't, but confirm).
    2. Add .gsd-framework/ entry to package.json "files" array so it's included in npm publish.
    3. Git add .gsd-framework/ and commit:
       "feat(09-01): add .gsd-framework/ — bundled GSD workflows, templates, references"
    4. Git add .cursor/rules/ .continue/config.yaml and commit:
       "fix(09-01): replace hardcoded local paths with repo-relative .gsd-framework/ paths"
    5. Update STATE.md: Phase 9 of 9, Plan 1 of 3, Status: Executed plan 1
  </action>
  <verify>
    git log --oneline -3 shows both commits.
    A fresh clone would have all .gsd-framework/ files available.
    npm pack --dry-run shows .gsd-framework/ files included.
  </verify>
  <done>.gsd-framework/ is committed and included in npm package</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] grep -r "/mnt/c/Users/jrm22n" .cursor/ returns 0 results
- [ ] .gsd-framework/ has >= 18 .md files
- [ ] npm pack --dry-run includes .gsd-framework/
- [ ] npm test passes
</verification>

<success_criteria>
- Zero hardcoded user paths in .cursor/rules/ or .continue/config.yaml
- .gsd-framework/ directory committed and npm-publishable
- Any developer who clones the repo has all framework files available
- npm test passes (no regressions)
</success_criteria>

<output>
After completion, create .planning/phases/09-onboarding-distribution/09-01-SUMMARY.md
following the standard summary template. Update STATE.md to Phase 9 of 9, Plan 1 of 3 complete.
</output>
