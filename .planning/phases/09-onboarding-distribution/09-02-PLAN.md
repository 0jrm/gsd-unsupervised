---
phase: 09-onboarding-distribution
plan: 02
type: execute
depends_on: ["09-01"]
files_modified:
  - setup.sh
  - src/cli.ts
  - .gsd/state.json (generated, not committed)
domain: node
---

<objective>
Replace the broken "npx gsd-unsupervised init" flow with a working ./setup.sh that
gets any developer from zero to a running daemon in under 5 minutes.

Current state: the `run` script bails with "No state found. Run: npx gsd-unsupervised init"
before anything happens. The init command either doesn't exist or doesn't write the state
correctly, so users are stuck before they start.

Output:
- setup.sh — interactive 3-question setup that creates .gsd/state.json and goals.md
- An `init` CLI command wired properly in src/cli.ts
- A working goals.md template dropped into the project root on first run
- Updated README quick-start section (replace the broken npx init instructions)
</objective>

<execution_context>
/gsd/execute-plan
.planning/STATE.md
</execution_context>

<context>
@.planning/STATE.md
@src/cli.ts (read first — understand existing command structure)
@run
@package.json
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <n>Task 1: Audit the current init/run flow and identify exact failure point</n>
  <files>run, src/cli.ts, bin/gsd-unsupervised</files>
  <action>
    Read `run` script, `src/cli.ts`, and the current CLI command list.
    Identify:
      1. Does an `init` command exist in cli.ts? If not, that's the root cause.
      2. What does .gsd/state.json need to contain for `run` to proceed?
         (read the node -e lines in `run` — it reads mode, statusServerPort, goalsPath)
      3. Does `npx gsd-unsupervised run` exist as a CLI command? Or is it only `start`?
    Document findings as a comment block at the top of the new setup.sh.
    No code changes in this task — discovery only.
  </action>
  <verify>Understanding of exact failure path is documented</verify>
  <done>Root cause of broken init flow identified and documented</done>
</task>

<task type="auto">
  <n>Task 2: Create setup.sh — interactive first-run wizard</n>
  <files>setup.sh</files>
  <action>
    Create setup.sh at project root. It should:

    1. Check if .gsd/state.json already exists → if yes, print "Already initialized.
       Edit .gsd/state.json to change settings." and exit 0.

    2. Ask 3 questions with sensible defaults shown in brackets:
         Agent type [cursor]: (cursor / cn / claude-code / gemini-cli / codex)
         Goals file path [./goals.md]:
         Status server port [3000]:

    3. Optionally ask:
         Twilio SMS notifications? (y/N):
         → if y, prompt for TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN, TWILIO_FROM, TWILIO_TO
           and write them to .env (with a note: "never commit .env")

    4. Write .gsd/state.json:
       {
         "mode": "self",
         "agent": "<chosen>",
         "goalsPath": "<chosen>",
         "statusServerPort": <chosen>,
         "workspaceRoot": ".",
         "createdAt": "<iso timestamp>"
       }

    5. If goals.md doesn't exist, create it with this template:
       # Goals

       ## Pending
       - [ ] My first goal — describe what you want to build

       ## In Progress

       ## Done

    6. Run `npm run build` to ensure dist/cli.js exists.

    7. Print success message:
       ✓ Initialized! Next steps:
         1. Edit goals.md and add your first goal
         2. Run ./run to start the daemon
         3. Run: tmux attach -t gsd-self  to watch it work
         Docs: https://github.com/0jrm/gsd-unsupervised

    Make the script POSIX-compatible (works on macOS and Linux/WSL).
    chmod +x setup.sh.
    Commit: "feat(09-02): add setup.sh — interactive first-run wizard"
  </action>
  <verify>
    bash setup.sh runs without error on a clean dir (mock the inputs with echo piping).
    .gsd/state.json is written with correct shape.
    goals.md is created when absent.
  </verify>
  <done>setup.sh creates valid .gsd/state.json and goals.md in under 30 seconds</done>
</task>

<task type="auto">
  <n>Task 3: Wire `gsd-unsupervised init` CLI command and fix `gsd-unsupervised run`</n>
  <files>src/cli.ts</files>
  <action>
    In src/cli.ts:

    1. Add an `init` command that replicates setup.sh logic programmatically
       (non-interactive version: accepts --agent, --goals, --port flags with the same
       defaults; writes .gsd/state.json and goals.md). This way both
       `./setup.sh` (interactive) and `npx gsd-unsupervised init` (flags-based) work.

    2. Find the `run` command (or add it if missing). It should:
       - Read .gsd/state.json
       - If missing, print: "Not initialized. Run ./setup.sh or npx gsd-unsupervised init"
         and exit 1 (not the current cryptic node -e crash).
       - Otherwise start the daemon with the stored config.

    3. Ensure `npm run build` compiles cleanly after changes.

    Commit: "feat(09-02): add init CLI command; improve run command error message"
  </action>
  <verify>
    npm run build passes.
    node dist/cli.js --help shows `init` and `run` commands.
    node dist/cli.js init --agent cursor writes .gsd/state.json without crashing.
  </verify>
  <done>npx gsd-unsupervised init works; run gives actionable error when not initialized</done>
</task>

<task type="auto">
  <n>Task 4: Update README quick-start to match the new setup flow</n>
  <files>README.md</files>
  <action>
    Find the Quick Start / Getting Started section in README.md.
    Replace whatever is there with:

    ## Quick Start

    ```bash
    # 1. Install
    npm install -g gsd-unsupervised
    # or clone and use locally:
    git clone https://github.com/0jrm/gsd-unsupervised && cd gsd-unsupervised && npm install

    # 2. Initialize in your project
    cd your-project
    ./setup.sh
    # or non-interactively:
    npx gsd-unsupervised init --agent cursor --goals ./goals.md

    # 3. Add a goal
    echo "- [ ] Add dark mode to the dashboard" >> goals.md

    # 4. Start the daemon
    ./run
    # Attach to watch: tmux attach -t gsd-self
    ```

    That's it. The daemon will read goals.md, invoke your agent, and SMS you when done
    (if Twilio is configured).

    Also add a "Requirements" line near the top:
    Node >= 18, tmux, and one of: Cursor, Claude Code, Continue (cn), Gemini CLI, or Codex.

    Commit: "docs(09-02): rewrite README quick-start for setup.sh flow"
  </action>
  <verify>README quick-start section is accurate and matches actual setup.sh behavior</verify>
  <done>README quick-start is accurate, minimal, and matches real behavior</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build passes
- [ ] npm test passes
- [ ] ./setup.sh runs end-to-end and produces valid .gsd/state.json
- [ ] node dist/cli.js init --agent cursor produces valid .gsd/state.json
- [ ] node dist/cli.js run (without state) prints actionable error not a crash
- [ ] README quick-start matches the actual 4-step flow
</verification>

<success_criteria>
- A developer can go from `git clone` to running daemon in under 5 minutes
- setup.sh and `npx gsd-unsupervised init` both work
- No cryptic crash on first run
- npm test passes (no regressions)
</success_criteria>

<o>
After completion, create .planning/phases/09-onboarding-distribution/09-02-SUMMARY.md.
Update STATE.md to Phase 9 of 9, Plan 2 of 3 complete.
</o>
