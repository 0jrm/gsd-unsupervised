---
phase: 05-crash-detection-recovery
plan: 01
type: execute
depends_on: []
files_modified: [src/session-log.ts, src/cursor-agent.ts, src/orchestrator.ts]
---

<objective>
Define the session-log schema, implement atomic append, and enrich entries with goal/position for crash recovery. Session log remains append-only and machine-friendly at project root (sessionLogPath, default ./session-log.jsonl). Add inspectForCrashedSessions() for startup detection and extend the agent invoker to persist goalTitle, phaseNumber, planNumber.

Purpose: Phase 5 must detect "cursor-agent died mid-phase" and "resume from exactly that point." The session log is the audit trail: one JSON object per line, append-only. It needs a defined schema, atomic writes, goal identity, and numeric position so the daemon can compute resume point deterministically.
Output: Documented SessionLogEntry schema; atomic appendSessionLog; inspectForCrashedSessions(); AgentInvoker extended with optional log context; cursor-agent writes start/done/crashed (and timeout) entries with full context.
</objective>

<execution_context>
/mnt/c/Users/jrm22n/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-state-monitoring-phase-transitions/04-01-SUMMARY.md
@.planning/phases/04-state-monitoring-phase-transitions/04-03-SUMMARY.md
@src/session-log.ts
@src/cursor-agent.ts
@src/orchestrator.ts
@src/daemon.ts
@src/lifecycle.ts

**Tech stack available:** TypeScript, chokidar, state-parser (readStateMd, StateSnapshot), session-log (appendSessionLog, readSessionLog, getLastRunningSession).
**Established patterns:** Null-on-failure for parsing; optional callbacks (onProgress); session log append on running/done/crashed/timeout in cursor-agent.
**Constraining decisions:** Session log stays append-only, machine-friendly, at project root (config.sessionLogPath). No heuristics that silently skip work; recovery must be deterministic and auditable. Schema and behavior will be documented in README/ARCHITECTURE (05-04).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define session-log schema and implement atomic append</name>
  <files>src/session-log.ts</files>
  <action>
    Define SessionLogEntry schema in code (JSDoc or top-of-file comment): timestamp (ISO string), goalTitle (string), phase (string, command label), phaseNumber (optional number), planNumber (optional number), sessionId (string | null), command (string), status ('running' | 'done' | 'crashed' | 'timeout'), durationMs (optional), error (optional). One JSON object per line; append-only; no in-place edits. Implement atomic append: appendSessionLog must write exactly one newline-terminated line (JSON.stringify(entry) + '\n') with appendFile(..., { flag: 'a' }). Do not read-modify-write. Add inspectForCrashedSessions(logPath: string): Promise<SessionLogEntry | null> that returns the last entry with status === 'running' or status === 'crashed' (scan from end). Skip malformed lines as readSessionLog does. Keep readSessionLog and getLastRunningSession; getLastRunningSession can remain for backward compatibility or be documented as legacy alias of inspectForCrashedSessions for "running" only.
  </action>
  <verify>Unit test: appendSessionLog appends one line; readSessionLog returns entries; inspectForCrashedSessions returns null for empty or when last is 'done'/'timeout'; returns last entry when last is 'running' or 'crashed'.</verify>
  <done>Schema documented; append is atomic (single-line append); inspectForCrashedSessions() exists and returns last crashed/running entry or null.</done>
</task>

<task type="auto">
  <name>Task 2: Write session start/done/crashed entries in cursor-agent invoker</name>
  <files>src/orchestrator.ts, src/cursor-agent.ts</files>
  <action>
    Define SessionLogContext { goalTitle: string; phaseNumber?: number; planNumber?: number }. Extend AgentInvoker to accept an optional 4th parameter: logContext?: SessionLogContext. In orchestrator, pass logContext at every agent() call with goal.title and current phase/plan (phaseNum and nextPlan.phaseNumber/planNumber when in plan loop). In createCursorAgentInvoker: accept 4th param; build baseEntry with goalTitle (logContext?.goalTitle ?? ''), phaseNumber, planNumber from logContext; keep 'phase' as command string. Write session start (status 'running') before runAgent; write 'done' on success, 'crashed' on non-zero exit or throw, 'timeout' on timeout. All appendSessionLog calls must use the same enriched baseEntry so every entry is auditable and machine-friendly.
  </action>
  <verify>Run daemon once; inspect session-log.jsonl at project root; confirm each entry has goalTitle, phaseNumber, planNumber when in phase/plan loop.</verify>
  <done>Session start/done/crashed (and timeout) entries written with full context; AgentInvoker extended; orchestrator passes logContext.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Session log schema documented; append is single-line atomic
- [ ] inspectForCrashedSessions() returns last running/crashed entry or null
- [ ] Session start/done/crashed entries include goalTitle, phaseNumber, planNumber
- [ ] Log remains append-only at project root (sessionLogPath)
- [ ] npm run build passes
</verification>

<success_criteria>

- All tasks completed
- Session log schema defined; atomic append; inspectForCrashedSessions(); invoker writes full context
- No regression in session log read/write or orchestrator flow
  </success_criteria>

<output>
After completion, create `.planning/phases/05-crash-detection-recovery/05-01-SUMMARY.md` with: Phase 5 Plan 1 summary, accomplishments, files created/modified, decisions, issues, next step (Ready for 05-02-PLAN.md).
</output>
