---
phase: 05-crash-detection-recovery
plan: 01
type: execute
depends_on: []
files_modified: [src/session-log.ts, src/cursor-agent.ts, src/orchestrator.ts]
---

<objective>
Enrich session log with goal and position so crash recovery can resume from the exact phase/plan. Add optional invocation context (goalTitle, phaseNumber, planNumber) to the agent invoker and persist it in session-log; add getLastCrashedOrRunningSession for startup detection.

Purpose: Phase 5 must detect "cursor-agent died mid-phase" and "resume from exactly that point." The session log already records status (running/done/crashed/timeout); it needs goal identity and numeric position so the daemon can match the first pending goal and pass resumeFrom to the orchestrator.
Output: SessionLogEntry with optional goalTitle, phaseNumber, planNumber; AgentInvoker extended to accept optional context; getLastCrashedOrRunningSession in session-log.ts.
</objective>

<execution_context>
/mnt/c/Users/jrm22n/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-state-monitoring-phase-transitions/04-01-SUMMARY.md
@.planning/phases/04-state-monitoring-phase-transitions/04-03-SUMMARY.md
@src/session-log.ts
@src/cursor-agent.ts
@src/orchestrator.ts
@src/daemon.ts
@src/lifecycle.ts

**Tech stack available:** TypeScript, chokidar, state-parser (readStateMd, StateSnapshot), session-log (appendSessionLog, readSessionLog, getLastRunningSession).
**Established patterns:** Null-on-failure for parsing; optional callbacks (onProgress); session log append on running/done/crashed/timeout in cursor-agent.
**Constraining decisions:** Session log format already has status running|done|crashed|timeout; cursor-agent writes entries with goalTitle: '' and phase: command string — we need goalTitle and numeric phase/plan for resume.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend SessionLogEntry and add getLastCrashedOrRunningSession</name>
  <files>src/session-log.ts</files>
  <action>
    Add optional fields to SessionLogEntry: goalTitle (string, can be '' for backward compatibility), phaseNumber?: number, planNumber?: number. Keep existing fields (timestamp, phase as string for command label, sessionId, command, status, durationMs, error). Add function getLastCrashedOrRunningSession(logPath: string): Promise<SessionLogEntry | null> that returns the last entry with status === 'running' or status === 'crashed' (scan entries from end). Skip malformed lines as readSessionLog does. Do not change readSessionLog or getLastRunningSession behavior.
  </action>
  <verify>Unit test or manual: readSessionLog still returns entries; getLastCrashedOrRunningSession returns null for empty log or when last entry is 'done'; returns last entry when last is 'running' or 'crashed'.</verify>
  <done>SessionLogEntry has optional goalTitle, phaseNumber, planNumber; getLastCrashedOrRunningSession exists and returns last running/crashed entry or null.</done>
</task>

<task type="auto">
  <name>Task 2: Extend AgentInvoker with optional log context and pass from orchestrator</name>
  <files>src/orchestrator.ts, src/cursor-agent.ts</files>
  <action>
    Define a type for log context, e.g. SessionLogContext { goalTitle: string; phaseNumber?: number; planNumber?: number }. Extend AgentInvoker signature to accept an optional 4th parameter: (command, workspaceDir, logger, logContext?: SessionLogContext) => Promise<AgentResult>. In orchestrator, when calling agent(), pass logContext with goal.title and current phase/plan: before each agent() call, set phaseNumber and planNumber from the orchestrator's current loop state (phaseNum for phase loop; for plan loop use the nextPlan's phaseNumber and planNumber). Pass goalTitle from goal.title. In createCursorAgentInvoker, accept the 4th parameter; use it in baseEntry for appendSessionLog: goalTitle from logContext?.goalTitle ?? '', phaseNumber and planNumber from logContext. Keep 'phase' as the command string (e.g. command.command) for readability in logs. Ensure all appendSessionLog calls in cursor-agent use the enriched baseEntry (running, done, crashed, timeout, and catch block).
  </action>
  <verify>Run daemon (or integration test) once; inspect session-log.jsonl and confirm entries include goalTitle, phaseNumber, planNumber when provided.</verify>
  <done>AgentInvoker has optional 4th param; orchestrator passes goal title and phase/plan at each call; cursor-agent persists them in every session log entry.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Session log entries written during a run include goalTitle, phaseNumber, planNumber where applicable
- [ ] getLastCrashedOrRunningSession returns last running/crashed entry or null
- [ ] Existing getLastRunningSession and readSessionLog behavior unchanged for existing fields
- [ ] npm run build (or tsc) passes
</verification>

<success_criteria>

- All tasks completed
- Session log enriched with goal and position; getLastCrashedOrRunningSession available
- No regression in session log read/write or orchestrator flow
  </success_criteria>

<output>
After completion, create `.planning/phases/05-crash-detection-recovery/05-01-SUMMARY.md` with: Phase 5 Plan 1 summary, accomplishments, files created/modified, decisions, issues, next step (Ready for 05-02-PLAN.md).
</output>
