---
phase: 05-crash-detection-recovery
plan: 04
type: execute
depends_on: ["05-03"]
files_modified: [src/session-log.test.ts, docs/ARCHITECTURE.md, README.md, CHANGELOG.md or release notes]
---

<objective>
Add unit tests for resume logic and plan completion detection, integration test simulating agent crash (CI job), and update documentation: ARCHITECTURE.md (recovery, config, failure modes), README.md (recovery behavior, example session-log and STATE.md mapping), release notes, and "how to recover manually" for users.

Purpose: Ship Phase 5 with test coverage and clear, copyable docs so recovery is auditable and users can recover manually when needed.
Output: Unit tests for inspectForCrashedSessions, computeResumePoint, isPlanCompleted; integration test that simulates crash and resume; ARCHITECTURE.md and README updated; example session-log + STATE.md; release notes and manual recovery steps.
</objective>

<execution_context>
/mnt/c/Users/jrm22n/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
/mnt/c/Users/jrm22n/.cursor/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-crash-detection-recovery/05-01-SUMMARY.md
@.planning/phases/05-crash-detection-recovery/05-02-SUMMARY.md
@.planning/phases/05-crash-detection-recovery/05-03-SUMMARY.md
@src/session-log.ts
@src/state-parser.ts
@src/roadmap-parser.ts
@README.md
@docs/ARCHITECTURE.md

**Decisions:** Session log append-only at project root; recovery deterministic (no silent skip); clean git by default, checkpoint opt-in. Expose recovery in README with example.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit tests for resume logic and plan completion detection</name>
  <files>src/session-log.test.ts, test files for recovery/plan completion</files>
  <action>
    Add unit tests for: (1) inspectForCrashedSessions — empty log returns null; last entry 'done'/'timeout' returns null; last entry 'running' or 'crashed' returns that entry; malformed lines skipped. (2) computeResumePoint — null when no crashed session; null when goalTitle mismatch; null when STATE.md null and log entry missing phase/plan; returns ResumeFrom when STATE.md valid and goal match; returns ResumeFrom from log fallback when STATE.md null but entry has phaseNumber and planNumber. (3) isPlanCompleted (or equivalent) — returns true when SUMMARY exists for plan, false otherwise. Use Vitest; follow existing test layout (e.g. state-parser.test.ts). Mock or temp files for session log and STATE.md where needed.
  </action>
  <verify>npm test passes; coverage includes session-log and computeResumePoint and plan completion paths.</verify>
  <done>Unit tests for resume logic and plan completion detection pass.</done>
</task>

<task type="auto">
  <name>Task 2: Integration test simulating agent crash (CI job)</name>
  <files>test or tests/ directory, package.json script</files>
  <action>
    Add an integration test that simulates an agent crash and verifies resume behavior: e.g. write a fixture session-log.jsonl with last entry status 'crashed' and goalTitle matching a fixture goal; write a fixture STATE.md with Current Position at phase 2 plan 1; run daemon (or the resume path only) and assert that orchestrateGoal is called with resumeFrom { phaseNumber: 2, planNumber: 1 }, or that the next executed command is execute-plan for that plan. Prefer a small, fast test (mock agent if needed) so it can run in CI. Add npm script e.g. "test:integration" and document in README; ensure CI job runs it if applicable.
  </action>
  <verify>Integration test passes locally; CI runs it (or instructions for running it).</verify>
  <done>Integration test for crash-and-resume scenario added and runnable in CI.</done>
</task>

<task type="auto">
  <name>Task 3: Update docs — ARCHITECTURE.md, README recovery, example, release notes, manual recovery</name>
  <files>docs/ARCHITECTURE.md, README.md, CHANGELOG or release notes</files>
  <action>
    (1) ARCHITECTURE.md: Add section on crash detection and recovery (session log, STATE.md, inspectForCrashedSessions, computeResumePoint, resume path in orchestrator). Document config: requireCleanGitBeforePlan, autoCheckpoint, sessionLogPath. Describe failure modes: agent crash, timeout, dirty git, ambiguous resume. (2) README.md: Add "Crash detection and recovery" section. Explain that session-log.jsonl is append-only at project root; link to schema or example. Show a minimal example of session-log.jsonl (2–3 lines) and the corresponding STATE.md "Current Position" block so users see the mapping. Document requireCleanGitBeforePlan and autoCheckpoint. (3) Add "How to recover manually" subsection: steps to inspect session-log.jsonl, read STATE.md, optionally run with same goal and let daemon resume, or run /gsd/execute-plan for the failed plan path. (4) Release notes (CHANGELOG.md or similar): Phase 5 shipped; crash detection and recovery; config options; link to README recovery section.
  </action>
  <verify>README and ARCHITECTURE describe recovery, config, and failure modes; example session-log and STATE.md are shown; manual recovery steps are copyable.</verify>
  <done>Docs updated; recovery exposed in README with example; release notes and manual recovery steps added.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Unit tests for resume and plan completion pass
- [ ] Integration test for crash/resume passes (or documented for CI)
- [ ] ARCHITECTURE.md describes recovery, config, failure modes
- [ ] README has recovery section with example session-log and STATE.md mapping
- [ ] "How to recover manually" steps are clear and copyable
- [ ] Release notes mention Phase 5 and recovery
- [ ] npm run build and npm test pass
</verification>

<success_criteria>

- All tasks completed
- Phase 5 complete: tests and docs ship with recovery; users can recover manually and understand behavior
  </success_criteria>

<output>
After completion, create `.planning/phases/05-crash-detection-recovery/05-04-SUMMARY.md` with: Phase 5 Plan 4 summary, accomplishments, files modified, decisions, issues, next step (Phase complete, ready for Phase 6).
</output>
