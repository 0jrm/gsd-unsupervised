---
phase: 05-crash-detection-recovery
plan: 03
type: execute
depends_on: ["05-02"]
files_modified: [src/orchestrator.ts, src/config.ts, src/roadmap-parser.ts]
---

<objective>
Implement the orchestrator resume path, require clean git working tree by default before executing plans (configurable), and add an opt-in git checkpoint helper. Expose isPlanCompleted() so resume and plan discovery use a single, auditable heuristic (e.g. SUMMARY.md exists). When daemon passes resumeFrom, fast-forward to that phase/plan, retry the plan, then continue. Recovery remains deterministic — no silent skip.

Purpose: When the daemon passes resumeFrom (05-02), the orchestrator jumps to the exact phase/plan and re-runs that plan. Requiring a clean git tree by default gives a reliable anchor for resuming; auto-checkpointing is opt-in so users are not surprised by automatic commits.
Output: orchestrateGoal accepts resumeFrom and fast-forwards then retries; config requireCleanGitBeforePlan (default true), autoCheckpoint (default false); git checkpoint helper (configurable); isPlanCompleted(phaseDir, planNumber) or equivalent for auditable plan completion.
</objective>

<execution_context>
/mnt/c/Users/jrm22n/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-crash-detection-recovery/05-01-SUMMARY.md
@.planning/phases/05-crash-detection-recovery/05-02-SUMMARY.md
@src/orchestrator.ts
@src/lifecycle.ts
@src/roadmap-parser.ts
@src/state-parser.ts

**Tech stack available:** parseRoadmap, findPhaseDir, discoverPlans, getNextUnexecutedPlan, GoalStateMachine, readStateMd.
**Constraining decisions:** getNextCommand() returns null for planning_phase/executing_plan — orchestrator discovers plans externally. Recovery deterministic: prefer re-run after checkpoint over skip. Clean git by default; checkpoint opt-in.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Require clean git and optional checkpoint (config)</name>
  <files>src/config.ts, src/orchestrator.ts</files>
  <action>
    Add config (Zod): requireCleanGitBeforePlan (boolean, default true), autoCheckpoint (boolean, default false). When requireCleanGitBeforePlan is true, before the first agent call that runs execute-plan (or before each execute-plan, as specified), check that git working tree is clean (e.g. run `git status --porcelain` and fail or warn if non-empty). When autoCheckpoint is true and tree is dirty, optionally create a checkpoint: run `git add -A && git commit -m "chore(autopilot): checkpoint before plan"` (or similar), then proceed. If requireCleanGitBeforePlan is true and tree is dirty and autoCheckpoint is false, abort the execute-plan step (or the goal) with a clear error so the user can commit or stash. Implement a small git helper (e.g. isWorkingTreeClean(workspaceRoot), optional createCheckpoint(workspaceRoot, message)) using child_process or a library; call it from the orchestrator before running execute-plan. Document behavior in code comments.
  </action>
  <verify>With requireCleanGitBeforePlan true and dirty tree, orchestrator aborts or refuses execute-plan with clear message; with autoCheckpoint true, checkpoint commit is created and plan runs.</verify>
  <done>Config requireCleanGitBeforePlan (default true) and autoCheckpoint (default false); git helper; orchestrator checks before execute-plan.</done>
</task>

<task type="auto">
  <name>Task 2: Add resumeFrom to orchestrateGoal and fast-forward to phase/plan</name>
  <files>src/orchestrator.ts</files>
  <action>
    Add optional parameter to orchestrateGoal options: resumeFrom?: { phaseNumber: number; planNumber: number }. If resumeFrom is absent, run the existing flow unchanged. If resumeFrom is present: (1) Parse roadmap (roadmapPath) and validate resumeFrom.phaseNumber is in [1, totalPhases]; (2) Fast-forward: do not call agent for new-project, create-roadmap, or any plan-phase / execute-plan until we reach phase resumeFrom.phaseNumber and the plan whose planNumber === resumeFrom.planNumber. To do this: advance the state machine through InitializingProject, CreatingRoadmap, then loop phases 1..resumeFrom.phaseNumber-1 with no agent calls (set PhaseInfo, setPlanInfo, advance to PhaseComplete as needed); for phase resumeFrom.phaseNumber, find phase dir, discoverPlans, find the plan with planNumber === resumeFrom.planNumber (not getNextUnexecutedPlan — the plan we're retrying may already be the "current" one). (3) Run one agent call: execute-plan for that plan's planPath. (4) After that call, if success: reportProgress, setPlanInfo(nextPlan.planNumber + 1, plans.length), re-discover plans, set nextPlan = getNextUnexecutedPlan(plans), and continue the existing while (nextPlan) loop for the rest of the phase, then phase_complete and continue for remaining phases. If the retry fails, sm.fail() and return as today. Shutdown checks (isShuttingDown) should remain in place during fast-forward and during the retry. Ensure the state machine is advanced correctly so phase/plan numbers and lifecycle phase stay consistent (e.g. sm.advance(GoalLifecyclePhase.PlanningPhase), sm.setPhaseInfo, sm.setPlanInfo before the phase loop; during fast-forward set phase/plan info without calling agent).
  </action>
  <verify>With resumeFrom { phaseNumber: 2, planNumber: 1 }, orchestrator skips new-project, create-roadmap, plan-phase 1; skips to phase 2, runs execute-plan for plan 1 only once, then continues with plan 2, 3, ... and subsequent phases. Manual or integration test.</verify>
  <done>orchestrateGoal(resumeFrom) fast-forwards without agent calls to the given phase/plan, runs one execute-plan (retry), then continues normal loop.</done>
</task>

<task type="auto">
  <name>Task 3: Handle edge cases and implement isPlanCompleted()</name>
  <files>src/orchestrator.ts, src/roadmap-parser.ts</files>
  <action>
    Edge cases for resume: resume at phase 1 plan 1 skips new-project/create-roadmap and runs only execute-plan for that plan. If the plan list doesn't contain the target planNumber, log error and fail (do not silently skip). Ensure setLastCommand is called for the retry; after retry success, re-discover plans and continue. Implement isPlanCompleted(phaseDir: string, planNumber: number): Promise<boolean> (or sync with existsSync) that returns true iff the plan's SUMMARY file exists (same heuristic as discoverPlans: PLAN file N-M-PLAN.md → N-M-SUMMARY.md). Export from roadmap-parser or a small recovery/plan helper. Use it where "plan executed" is needed so completion is auditable and consistent.
  </action>
  <verify>Resume at (1,1) and (2,2) works; missing plan fails loudly; isPlanCompleted(phaseDir, n) matches existence of *-n-SUMMARY.md.</verify>
  <done>Edge cases handled; isPlanCompleted() implemented and used for auditable plan completion.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] requireCleanGitBeforePlan (default true) blocks execute-plan when tree is dirty unless autoCheckpoint
- [ ] autoCheckpoint (default false) creates checkpoint commit when enabled and tree dirty
- [ ] Resume at (1,1), (2,1), (2,2) produces exactly one execute-plan for that plan then continues
- [ ] isPlanCompleted() matches SUMMARY.md existence; no silent skip on missing plan
- [ ] npm run build passes
</verification>

<success_criteria>

- All tasks completed
- Clean git required by default; opt-in checkpoint; resume path and isPlanCompleted(); recovery deterministic
- Phase 5 plan 3 complete (orchestrator + git + plan completion)
  </success_criteria>

<output>
After completion, create `.planning/phases/05-crash-detection-recovery/05-03-SUMMARY.md` with: Phase 5 Plan 3 summary, accomplishments, files modified, decisions, issues, next step (Ready for 05-04-PLAN.md).
</output>
