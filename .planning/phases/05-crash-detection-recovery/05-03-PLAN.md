---
phase: 05-crash-detection-recovery
plan: 03
type: execute
depends_on: ["05-02"]
files_modified: [src/orchestrator.ts]
---

<objective>
Implement the orchestrator resume path: accept optional resumeFrom { phaseNumber, planNumber }, fast-forward to that phase/plan without invoking the agent, then retry the plan at that position (one agent call), then continue the normal loop so subsequent plans and phases run as usual.

Purpose: When the daemon passes resumeFrom (05-02), the orchestrator must jump to the exact phase/plan and re-run that plan (crash retry), then advance. This delivers "resume from exactly that point with no lost work."
Output: orchestrateGoal(options) accepts resumeFrom?: { phaseNumber: number; planNumber: number }; when set, skip all agent calls until we are at that phase and plan, run one agent call for that plan (execute-plan), then continue the existing phase/plan loop.
</objective>

<execution_context>
/mnt/c/Users/jrm22n/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-crash-detection-recovery/05-01-SUMMARY.md
@.planning/phases/05-crash-detection-recovery/05-02-SUMMARY.md
@src/orchestrator.ts
@src/lifecycle.ts
@src/roadmap-parser.ts
@src/state-parser.ts

**Tech stack available:** parseRoadmap, findPhaseDir, discoverPlans, getNextUnexecutedPlan, GoalStateMachine, readStateMd.
**Constraining decisions:** getNextCommand() returns null for planning_phase/executing_plan — orchestrator discovers plans externally. Fast-forward means: set sm state and skip agent invocations until we reach phaseNumber and the plan index that corresponds to planNumber; then run execute-plan for that plan (retry), then re-discover plans and continue the while (nextPlan) loop.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add resumeFrom to orchestrateGoal and fast-forward to phase/plan</name>
  <files>src/orchestrator.ts</files>
  <action>
    Add optional parameter to orchestrateGoal options: resumeFrom?: { phaseNumber: number; planNumber: number }. If resumeFrom is absent, run the existing flow unchanged. If resumeFrom is present: (1) Parse roadmap (roadmapPath) and validate resumeFrom.phaseNumber is in [1, totalPhases]; (2) Fast-forward: do not call agent for new-project, create-roadmap, or any plan-phase / execute-plan until we reach phase resumeFrom.phaseNumber and the plan whose planNumber === resumeFrom.planNumber. To do this: advance the state machine through InitializingProject, CreatingRoadmap, then loop phases 1..resumeFrom.phaseNumber-1 with no agent calls (set PhaseInfo, setPlanInfo, advance to PhaseComplete as needed); for phase resumeFrom.phaseNumber, find phase dir, discoverPlans, find the plan with planNumber === resumeFrom.planNumber (not getNextUnexecutedPlan — the plan we're retrying may already be the "current" one). (3) Run one agent call: execute-plan for that plan's planPath. (4) After that call, if success: reportProgress, setPlanInfo(nextPlan.planNumber + 1, plans.length), re-discover plans, set nextPlan = getNextUnexecutedPlan(plans), and continue the existing while (nextPlan) loop for the rest of the phase, then phase_complete and continue for remaining phases. If the retry fails, sm.fail() and return as today. Shutdown checks (isShuttingDown) should remain in place during fast-forward and during the retry. Ensure the state machine is advanced correctly so phase/plan numbers and lifecycle phase stay consistent (e.g. sm.advance(GoalLifecyclePhase.PlanningPhase), sm.setPhaseInfo, sm.setPlanInfo before the phase loop; during fast-forward set phase/plan info without calling agent).
  </action>
  <verify>With resumeFrom { phaseNumber: 2, planNumber: 1 }, orchestrator skips new-project, create-roadmap, plan-phase 1; skips to phase 2, runs execute-plan for plan 1 only once, then continues with plan 2, 3, ... and subsequent phases. Manual or integration test.</verify>
  <done>orchestrateGoal(resumeFrom) fast-forwards without agent calls to the given phase/plan, runs one execute-plan (retry), then continues normal loop.</done>
</task>

<task type="auto">
  <name>Task 2: Handle edge cases and align state machine with resume</name>
  <files>src/orchestrator.ts</files>
  <action>
    If resumeFrom.phaseNumber is 1 and resumeFrom.planNumber is 1: we are resuming at "plan 1 of phase 1". Fast-forward: after new-project and create-roadmap we enter phase loop at i=0 (phase 1), discover plans, find plan with planNumber 1, run it. So we must NOT run new-project and create-roadmap again when resuming — fast-forward means we skip those and also skip plan-phase 1 (we assume roadmap exists). So fast-forward: advance sm to CreatingRoadmap (or to PlanningPhase), set phase info to (resumeFrom.phaseNumber, totalPhases), then for phases from 1 to resumeFrom.phaseNumber-1 do nothing (or advance through phase complete); for phase resumeFrom.phaseNumber discover plans, locate plan with planNumber === resumeFrom.planNumber, run that execute-plan. If the plan list doesn't contain that plan number, log warning and fail or skip. Ensure setLastCommand is called for the retry command so session log and sm stay consistent. After retry success, setPlanInfo and re-discover so nextPlan is the next unexecuted plan (or null if phase complete).
  </action>
  <verify>Resume at phase 1 plan 1 skips init commands and runs only execute-plan for phase 1 plan 1; resume at phase 2 plan 2 runs only execute-plan for phase 2 plan 2 after fast-forward; build passes.</verify>
  <done>Edge cases handled; state machine and plan discovery consistent with resume; no double-execution of init or wrong plan.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Resume at (1,1), (2,1), (2,2) each produces exactly one execute-plan call for that plan then continues
- [ ] Normal run without resumeFrom unchanged
- [ ] isShuttingDown() respected during fast-forward and retry
- [ ] npm run build passes
</verification>

<success_criteria>

- All tasks completed
- Orchestrator resume path implemented; crash recovery end-to-end: detect → resumeFrom → fast-forward → retry → continue
- Phase 5 complete: crash detection and recovery with no lost work from exact failure point
  </success_criteria>

<output>
After completion, create `.planning/phases/05-crash-detection-recovery/05-03-SUMMARY.md` with: Phase 5 Plan 3 summary, accomplishments, files modified, decisions, issues, next step (Phase complete, ready for Phase 6).
</output>
