---
phase: 05-crash-detection-recovery
plan: 02
type: execute
depends_on: ["05-01"]
files_modified: [src/daemon.ts, src/session-log.ts]
---

<objective>
Implement inspectForCrashedSessions() usage and computeResumePoint(): on daemon startup, detect an interrupted session and compute a deterministic resume point. Recovery must be auditable — no heuristics that silently skip work; if there is ambiguity, do not resume (start from scratch) or re-run after checkpoint rather than skip.

Purpose: Crash recovery requires the daemon to recognize "we crashed on this goal" and pass a clear resumeFrom to the orchestrator. computeResumePoint() centralizes the logic so it is testable and deterministic.
Output: computeResumePoint(sessionLogPath, stateMdPath, firstPendingGoalTitle) returning ResumeFrom | null; daemon calls it and passes resumeFrom to orchestrateGoal only when unambiguous.
</objective>

<execution_context>
/mnt/c/Users/jrm22n/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-crash-detection-recovery/05-01-SUMMARY.md
@src/daemon.ts
@src/session-log.ts
@src/state-parser.ts
@src/orchestrator.ts
@src/goals.ts

**Tech stack available:** readStateMd, StateSnapshot (phaseNumber, planNumber), inspectForCrashedSessions, loadGoals, getPendingGoals.
**Constraining decisions:** Recovery must be deterministic and auditable. If position is ambiguous (e.g. STATE.md null and log entry missing phase/plan), do NOT resume — start from scratch. Prefer re-running after checkpoint over silently skipping. Resume only when goalTitle matches first pending goal.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement computeResumePoint() (deterministic, no silent skip)</name>
  <files>src/session-log.ts</files>
  <action>
    Implement computeResumePoint(sessionLogPath: string, stateMdPath: string, firstPendingGoalTitle: string): Promise<ResumeFrom | null>. Use inspectForCrashedSessions(sessionLogPath). If null or pending goal title is empty, return null. If the returned entry's goalTitle does not match firstPendingGoalTitle (trim both, exact or normalized match), return null. Otherwise determine position: (1) Prefer STATE.md: readStateMd(stateMdPath). If snapshot is non-null and snapshot.phaseNumber >= 1 and snapshot.planNumber >= 1, return { phaseNumber: snapshot.phaseNumber, planNumber: snapshot.planNumber }. (2) If STATE.md is null or unparseable, fall back to entry.phaseNumber and entry.planNumber only if both are defined and >= 1; otherwise return null (ambiguous — do not resume). Export type ResumeFrom { phaseNumber: number; planNumber: number }. No heuristics that skip work; when in doubt, return null so the run starts from scratch.
  </action>
  <verify>Unit test: computeResumePoint returns null for empty log, goal mismatch, or ambiguous position; returns correct ResumeFrom when STATE.md and goal match.</verify>
  <done>computeResumePoint() implemented; deterministic; returns null on ambiguity.</done>
</task>

<task type="auto">
  <name>Task 2: Daemon uses inspectForCrashedSessions and computeResumePoint</name>
  <files>src/daemon.ts</files>
  <action>
    After loading pending goals and before the goal loop: call computeResumePoint(config.sessionLogPath, stateMdPath, pending[0]?.title ?? ''). stateMdPath = path.join(config.workspaceRoot, '.planning', 'STATE.md'). If result is null or pending.length === 0, pass resumeFrom: null to orchestrateGoal for all goals. If result is non-null, pass resumeFrom only for the first goal (i === 0); for all other goals (i >= 1) pass resumeFrom: null. Import ResumeFrom from the module that exports computeResumePoint (session-log or recovery).
  </action>
  <verify>With session-log last entry crashed and goalTitle matching first pending, and STATE.md valid, daemon passes resumeFrom to orchestrateGoal for first goal only.</verify>
  <done>Daemon calls computeResumePoint and passes resumeFrom to orchestrateGoal for the first goal when resume point is unambiguous.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] computeResumePoint() returns null on ambiguity (no silent skip)
- [ ] resumeFrom passed to orchestrateGoal only when position is unambiguous
- [ ] Only first pending goal receives resumeFrom when resuming
- [ ] Unit tests for computeResumePoint (and/or inspectForCrashedSessions) cover match, mismatch, null STATE
- [ ] npm run build passes
</verification>

<success_criteria>

- All tasks completed
- computeResumePoint() and daemon wiring implemented; recovery deterministic and auditable
- No regression in normal (no-resume) startup
  </success_criteria>

<output>
After completion, create `.planning/phases/05-crash-detection-recovery/05-02-SUMMARY.md` with: Phase 5 Plan 2 summary, accomplishments, files modified, decisions, issues, next step (Ready for 05-03-PLAN.md).
</output>
