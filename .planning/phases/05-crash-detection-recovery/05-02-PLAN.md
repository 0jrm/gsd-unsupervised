---
phase: 05-crash-detection-recovery
plan: 02
type: execute
depends_on: ["05-01"]
files_modified: [src/daemon.ts]
---

<objective>
On daemon startup, detect an interrupted session (last session log entry running or crashed). If it matches the first pending goal, read STATE.md for last position and pass resumeFrom into orchestrateGoal so the orchestrator can resume from that phase/plan instead of from scratch.

Purpose: Crash recovery requires the daemon to recognize "we crashed on this goal" and tell the orchestrator where to resume. This plan implements the startup detection and resumeFrom wiring; the orchestrator resume path is 05-03.
Output: Daemon calls getLastCrashedOrRunningSession before processing goals; when last entry is running/crashed and goalTitle matches first pending goal, compute resumeFrom from readStateMd(stateMdPath) and pass to orchestrateGoal.
</objective>

<execution_context>
/mnt/c/Users/jrm22n/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-crash-detection-recovery/05-01-SUMMARY.md
@src/daemon.ts
@src/session-log.ts
@src/state-parser.ts
@src/orchestrator.ts
@src/goals.ts

**Tech stack available:** readStateMd, StateSnapshot (phaseNumber, planNumber), getLastCrashedOrRunningSession, loadGoals, getPendingGoals.
**Constraining decisions:** Resume only when last crashed/running entry's goalTitle matches the first pending goal (single workspace, sequential goals). Use STATE.md for position when available; if STATE.md is null, optionally fall back to session log entry's phaseNumber/planNumber if present, otherwise do not resume (start from scratch).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compute resumeFrom on startup when last session was interrupted</name>
  <files>src/daemon.ts</files>
  <action>
    After loading pending goals and before the goal loop: read session log via getLastCrashedOrRunningSession(config.sessionLogPath). If result is null or pending.length === 0, set resumeFrom = null. If result exists and pending.length > 0, compare result.goalTitle with pending[0].title (trim/normalize if needed). If they match, read STATE.md via readStateMd(stateMdPath) for the workspace (stateMdPath = path.join(config.workspaceRoot, '.planning', 'STATE.md')). If snapshot is non-null and snapshot.phaseNumber >= 1 and snapshot.planNumber >= 1, set resumeFrom = { phaseNumber: snapshot.phaseNumber, planNumber: snapshot.planNumber }. If snapshot is null, optionally use result.phaseNumber and result.planNumber if both are defined and >= 1, else set resumeFrom = null. If goalTitle does not match, set resumeFrom = null. Pass resumeFrom only for the first goal (the one we're resuming); when processing the first goal, call orchestrateGoal(..., resumeFrom). For subsequent goals (i >= 1), pass resumeFrom: null. Define a type or interface ResumeFrom { phaseNumber: number; planNumber: number } in orchestrator or a shared types file and use it in daemon and orchestrator.
  </action>
  <verify>With an existing session-log.jsonl whose last entry is status 'crashed' and goalTitle matching first pending goal, and STATE.md present with phase 2 plan 1, daemon should pass resumeFrom { phaseNumber: 2, planNumber: 1 } to orchestrateGoal for that goal.</verify>
  <done>Daemon computes resumeFrom from session log + STATE.md (or log fallback) and passes it to orchestrateGoal for the first goal when matching interrupted session.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] resumeFrom passed to orchestrateGoal when last session crashed/running and goal matches
- [ ] resumeFrom null when no interrupted session or goal mismatch or STATE.md unparseable
- [ ] Only first pending goal receives resumeFrom; others receive null
- [ ] npm run build passes
</verification>

<success_criteria>

- All tasks completed
- Startup resume detection implemented; orchestrateGoal receives resumeFrom when appropriate (orchestrator handling is 05-03)
- No regression in normal (no-resume) startup
  </success_criteria>

<output>
After completion, create `.planning/phases/05-crash-detection-recovery/05-02-SUMMARY.md` with: Phase 5 Plan 2 summary, accomplishments, files modified, decisions, issues, next step (Ready for 05-03-PLAN.md).
</output>
