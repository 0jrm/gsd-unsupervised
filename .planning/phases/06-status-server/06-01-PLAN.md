---
phase: 06-status-server
plan: 01
type: execute
depends_on: ["05-02"]
files_modified:
  - src/status-server.ts
  - src/daemon.ts
  - src/orchestrator.ts
  - src/state-parser.ts
  - src/session-log.ts
  - src/git.ts
  - src/config.ts
  - src/resource-governor.ts
  - .planning/config.json
  - docs/ARCHITECTURE.md
  - docs/CONTEXT-FOR-MODEL.md
domain: infra
---

<objective>
Expose the GSD daemon's progress and health via an HTTP status server with a small, zero-build dashboard and ensure it integrates cleanly with the existing daemon/orchestrator lifecycle.

Purpose: Provide a simple way to see what the autopilot is doing (goal, phase/plan, resume status, recent activity, and system load) without reading logs or STATE.md by hand.
Output: An Express-based status server wired into the daemon that serves a rich JSON API and dashboard HTML at a configurable port, with togglable parallelization mode persisted to .planning/config.json and graceful shutdown semantics.
</objective>

<execution_context>
/mnt/c/Users/jrm22n/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
/mnt/c/Users/jrm22n/.cursor/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phases that this status server builds on:
@.planning/phases/03-agent-integration/03-01-SUMMARY.md
@.planning/phases/04-state-monitoring-and-phase-transitions/04-02-SUMMARY.md
@.planning/phases/05-crash-detection-recovery/05-02-SUMMARY.md

# Relevant source files:
@src/status-server.ts
@src/daemon.ts
@src/orchestrator.ts
@src/config.ts
@src/resource-governor.ts

**Tech stack available:** Node 18+, TypeScript, Express, simple-git, pino, existing daemon/orchestrator/lifecycle modules.
**Established patterns:** Pure helpers for state/SESSION log parsing, explicit timeout and resume-pointer classification, structured logging and non-fatal mismatch reporting via STATE.md, resource governor for CPU-aware behavior.
**Constraining decisions:**
- [Phase 03-01] Timeout classification comes from explicit fields, not stderr parsing.
- [Phase 04-02] STATE.md mismatches are warnings for observability, not hard failures.
- [Phase 05-02] Resume pointer computation is side-effect free and owned by dedicated helpers; daemon/orchestrator act on its result.

**Issues being addressed:** Make daemon/phase progress observable without attaching a debugger or tailing logs, and expose config knobs (parallelization mode) safely through the status server.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize status server API shape and integration with daemon/orchestrator</name>
  <files>src/status-server.ts, src/daemon.ts, src/orchestrator.ts, src/state-parser.ts, src/session-log.ts, src/git.ts</files>
  <action>
Ensure `createStatusServer` in `src/status-server.ts` exposes a stable surface:
- Keep legacy-compatible JSON at GET `/status` (and `/` when no options are provided) using the existing `StatusPayload` shape.
- Serve dashboard HTML at GET `/` when options are passed, and a rich JSON payload at GET `/api/status` that includes current STATE snapshot, recent session-log entries, git feed, token/cost placeholders, and system load via `currentLoadInfo`.
Wire the status server into the daemon/orchestrator lifecycle:
- In `src/daemon.ts`, start the status server when the daemon starts (using workspace root, STATE.md path, and session-log path), and ensure `close` is awaited on shutdown so the Express server does not leak file descriptors.
- In `src/orchestrator.ts` (or equivalent lifecycle module), ensure the status payload used by `getStatus` reflects the current goal, phase, plan, and last heartbeat consistently with the existing lifecycle state machine and resume-pointer behavior from Phase 5.
Respect existing observability decisions:
- Do not turn STATE mismatches or missing STATE.md into hard errors; surface them as null/undefined fields in `DashboardStatusPayload` while keeping the daemon running.
  </action>
  <verify>
Run the unit tests for status and lifecycle integration (or the closest equivalents) and smoke-test the server:
- npm test -- status-server
- npm test -- lifecycle
- npm test -- state-parser
Then start the daemon in a local workspace and hit:
- curl http://localhost:&lt;status-port&gt;/status
- curl http://localhost:&lt;status-port&gt;/api/status
Confirm that both endpoints respond with JSON and that /status remains compatible with earlier minimal tooling.
  </verify>
  <done>
Status server starts and stops with the daemon without unhandled rejections; GET `/status` returns the legacy minimal JSON, GET `/api/status` returns a rich payload (including state snapshot and git feed) even when STATE.md or the session log are partially populated; all related tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire planning config toggles and guardrails into /api/config</name>
  <files>src/status-server.ts, src/config.ts, .planning/config.json, docs/ARCHITECTURE.md, docs/CONTEXT-FOR-MODEL.md</files>
  <action>
Finish and harden the planning config read/write path used by the dashboard toggle:
- Ensure `readPlanningConfig` in `src/status-server.ts` uses `DEFAULT_PLANNING_CONFIG` as a base and merges values from `.planning/config.json` so new fields have sane defaults while preserving user overrides.
- Confirm POST `/api/config` validates the shape of the `parallelization` object (rejecting non-objects or non-boolean `enabled`) and performs a shallow merge onto the existing config instead of overwriting unrelated fields.
Connect this API to the actual planner configuration:
- In `src/config.ts`, make sure `.planning/config.json` is the single source of truth for parallelization flags used by phase/plan execution, so toggling from the dashboard reliably switches between sequential and parallel-aware behavior when the rest of the system supports it.
Document the behavior briefly:
- Update `docs/ARCHITECTURE.md` or `docs/CONTEXT-FOR-MODEL.md` to mention the status server, `/api/config` endpoint, and how parallelization settings are loaded and applied so future phases (like WSL bootstrap) do not need to rediscover this behavior.
  </action>
  <verify>
From a clean workspace, run:
- npm test -- status-server
- npm test -- config
Then:
1. Start the daemon with the status server enabled.
2. Visit `/` in a browser and use the toggle in the "Execution mode" card.
3. Confirm that `.planning/config.json` is updated (only within the `parallelization` slice) and that refreshing `/` reflects the new mode.
4. Restart the daemon and confirm that the in-memory configuration now matches the persisted parallelization setting.
  </verify>
  <done>
The `/api/config` endpoint returns a merged planning config with sensible defaults when the file is missing or malformed, rejects invalid payloads with clear JSON errors, persists parallelization changes to `.planning/config.json` without clobbering other settings, and the daemon's execution mode follows those settings after restart.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Express-based status server with a small dashboard at GET `/` that shows current goal, phase/plan progress, recent git commits, and a working execution-mode toggle backed by `.planning/config.json`.
  </what-built>
  <how-to-verify>
    1. Start the GSD daemon locally with the status server enabled on a known port (for example, 4173 or another unoccupied port).
    2. Visit `http://localhost:&lt;status-port&gt;/` in a desktop browser and confirm:
       - The "Current goal" card shows the expected goal title (or `—` when idle).
       - The progress bar moves as phases/plans advance.
       - The "Recent commits" list shows the most recent git commits for this repo with hashes, messages, and timestamps.
    3. Toggle the "Execution mode" control:
       - Verify that the label switches between "Sequential" and "Parallel".
       - Open `.planning/config.json` and confirm that `parallelization.enabled` flips accordingly without unrelated keys changing.
    4. Stop the daemon cleanly and confirm that the HTTP server shuts down (reloading the page should fail rather than hanging indefinitely).
  </how-to-verify>
  <resume-signal>
Type "approved" once the dashboard looks correct, the toggle works end-to-end, and shutdown behavior is clean; otherwise, describe any discrepancies between the UI, API responses, and actual daemon behavior to iterate on.
  </resume-signal>
</task>

</tasks>

<verification>
Before declaring Phase 6 complete for this plan:
- [ ] `npm test -- status-server` and any related lifecycle/config tests pass.
- [ ] The status server starts and stops with the daemon without unhandled promise rejections or open handles.
- [ ] GET `/status` continues to provide the legacy JSON shape for existing tooling.
- [ ] GET `/api/status` returns a rich payload (including state snapshot, session log window, git feed, token/cost placeholders, and system load) even when STATE.md or the session log are partially populated.
- [ ] The `/api/config` endpoint round-trips valid updates to `.planning/config.json` while rejecting malformed payloads with clear error messages.
</verification>

<success_criteria>

- All tasks above are implemented and verified in tests and by a manual dashboard smoke test.
- The status server is the canonical way to observe daemon progress and health without tailing logs.
- Parallelization configuration can be safely toggled via the dashboard and persists across daemon restarts without corrupting `.planning/config.json`.
- No new TypeScript, lint, or runtime errors are introduced, and existing CLI/daemon workflows continue to behave as before when the status server is disabled.
  </success_criteria>

<output>
After completion, create `.planning/phases/06-status-server/06-01-SUMMARY.md`:

# Phase 6 Status Server · Plan 01 Summary

**Status server and dashboard wired into daemon with observable progress and configurable execution mode.**

## Accomplishments

- Implemented and wired Express-based status server into the daemon/orchestrator lifecycle with graceful shutdown.
- Exposed rich `/api/status` and `/api/config` endpoints and connected them to STATE.md, the session log, git history, and `.planning/config.json`.

## Files Created/Modified

- `src/status-server.ts` — Status server implementation and planning config helpers.
- `src/daemon.ts`, `src/orchestrator.ts` — Status-server lifecycle integration.
- `src/config.ts` — Configuration loading/wiring for planning parallelization.
- `.planning/config.json` — Updated structure for parallelization mode persisted via `/api/config`.
- `docs/ARCHITECTURE.md`, `docs/CONTEXT-FOR-MODEL.md` — Documentation of status server and config behavior.

## Decisions Made

- Confirmed that STATE.md mismatches remain non-fatal observability signals; the status server surfaces them without crashing the daemon.
- Established `/status` as the legacy JSON endpoint and `/api/status` as the richer dashboard API, with `/api/config` as the single knob for toggling parallelization from the UI.

## Issues Encountered

- [Document any test flakiness, port conflicts, or edge cases discovered while wiring the server; or state "None".]

## Next Step

- Phase 6 is complete after this plan; proceed to Phase 7 (WSL Bootstrap) once the summary is committed and the status server has been exercised in a real daemon run.
</output>
