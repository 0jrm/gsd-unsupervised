---
phase: 04-state-monitoring-phase-transitions
plan: 02
type: execute
depends_on: []
files_modified: [src/state-watcher.ts, src/daemon.ts, docs/ARCHITECTURE.md, src/state-watcher.test.ts]
domain: node
---

<objective>
Harden state monitoring: StateWatcher integration, event emission verification, and STATE.md contract documentation.

Purpose: Ensure STATE.md is watched reliably, progress events are emitted as specified, and the STATE.md format is documented for GSD and downstream consumers (status server, Phase 5 resume).
Output: StateWatcher tests, documented STATE.md format, any minor fixes to daemon/watcher integration.
</objective>

<execution_context>
/mnt/c/Users/jrm22n/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/ARCHITECTURE.md
@docs/CONTEXT-FOR-MODEL.md
@src/state-watcher.ts
@src/state-parser.ts
@src/daemon.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add StateWatcher unit tests</name>
  <files>src/state-watcher.test.ts</files>
  <action>Create state-watcher.test.ts (or add to existing test file if present). Test: (1) start() watches stateMdPath and on first valid STATE.md content emits 'ready' with snapshot; (2) when content changes and phase number increases, emit 'phase_advanced' with correct fromPhase/toPhase/phaseName; (3) when same phase but plan number increases, emit 'plan_advanced'; (4) when status indicates phase complete, emit 'phase_completed'; (5) when phaseNumber === totalPhases and status complete, emit 'goal_completed' once. Use temporary STATE.md file and write content updates; debounce may require small delays (e.g. 50ms) in tests. Use Vitest. Do not use chokidar mock if it complicates; use real file writes and assert events. If state-watcher.test.ts already exists, extend it to cover the above; otherwise create it.</action>
  <verify>npm test -- state-watcher passes with new or updated tests</verify>
  <done>StateWatcher event emission (ready, phase_advanced, plan_advanced, phase_completed, goal_completed) covered by tests</done>
</task>

<task type="auto">
  <name>Task 2: Document STATE.md format and state-parser contract</name>
  <files>docs/ARCHITECTURE.md or docs/STATE.md</files>
  <action>Document the "## Current Position" section format required by state-parser: exact line patterns for Phase (X of Y (Name)), Plan (A of B in current phase), Status, Last activity, Progress (N%). Note that readStateMd returns null on missing file or parse failure. Add a short "STATE.md contract" subsection under StateWatcher/state-parser in ARCHITECTURE.md, or create docs/STATE.md and link from ARCHITECTURE. Ensure GSD and status server consumers know the expected format.</action>
  <verify>Grep docs for "Current Position" or "STATE.md" shows the new documentation; format description matches state-parser.ts regexes</verify>
  <done>STATE.md format and parser contract documented for consumers</done>
</task>

<task type="auto">
  <name>Task 3: Verify daemon StateWatcher lifecycle</name>
  <files>src/daemon.ts</files>
  <action>Confirm daemon starts StateWatcher before orchestrateGoal and stops it in finally (watcher.stop()). Ensure watcher is only created when stateMdPath is valid (daemon already uses stateMdPath from config). If any edge case exists (e.g. STATE.md missing at start), ensure watcher failure is non-fatal (log warning, continue). No functional change required if current code is correct; otherwise add minimal fix and a brief comment. Optionally add an integration test in tests/ or daemon.test that runs one goal with a stub agent and asserts StateWatcher received at least one state_changed (if test infrastructure allows).</action>
  <verify>npm test passes; daemon code shows clear start/stop and error handling for StateWatcher</verify>
  <done>Daemon StateWatcher lifecycle verified and documented or fixed</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] npm test passes (including state-watcher tests)
- [ ] STATE.md format is documented
- [ ] Daemon starts/stops StateWatcher correctly
</verification>

<success_criteria>

- All tasks completed
- StateWatcher events covered by tests
- STATE.md contract documented
- No regressions in existing tests
  </success_criteria>

<output>
After completion, create `.planning/phases/04-state-monitoring-phase-transitions/04-02-SUMMARY.md`:

# Phase 4 Plan 2: State Monitoring Summary

**[Substantive one-liner — what shipped]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `path/to/file` — Description

## Decisions Made

[Key decisions or "None"]

## Issues Encountered

[Problems and resolutions or "None"]

## Next Step

Ready for 04-03-PLAN.md (Phase transitions and progress reporting)
</output>
