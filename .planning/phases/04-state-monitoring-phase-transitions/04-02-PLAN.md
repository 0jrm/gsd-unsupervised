---
phase: 04-state-monitoring-phase-transitions
plan: 02
type: execute
depends_on: ["04-01"]
files_modified: [src/orchestrator.ts, tests/orchestrator.progress.test.ts, docs/ARCHITECTURE.md, docs/CONTEXT-FOR-MODEL.md]
domain: node
---

<objective>
Phase/plan advancement correctness: orchestrator `reportProgress` mismatch logging + tests + documentation alignment.

Purpose: Ensure the orchestrator’s expected phase tracking matches what STATE.md reports, and when it doesn’t, the system emits a clear warning without failing the run. This tightens the Phase 4 “monitoring + transitions” story and sets Phase 5 resume up for success.
Output: `reportProgress` mismatch logging with correct expectedPhase values, a focused test for progress/mismatch behavior, and updated docs describing ownership and transition flow.
</objective>

<execution_context>
/mnt/c/Users/jrm22n/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-state-monitoring-phase-transitions/04-01-SUMMARY.md
@docs/ARCHITECTURE.md
@docs/CONTEXT-FOR-MODEL.md
@src/state-parser.ts
@src/daemon.ts
@src/orchestrator.ts
@src/lifecycle.ts
@tests/orchestrator.lifecycle.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement `reportProgress` mismatch warning and correct expectedPhase usage</name>
  <files>src/orchestrator.ts</files>
  <action>
Update `reportProgress({ expectedPhase, expectedSummaryPath? })` to:
- Read `STATE.md` via `readStateMd` as today; if null, do nothing (non-fatal).
- When snapshot exists and `snapshot.phaseNumber !== expectedPhase`, log a warning that includes `{ expectedPhase, actualPhase: snapshot.phaseNumber, actualPhaseName: snapshot.phaseName, plan: snapshot.planNumber, status: snapshot.status }`.
- Ensure calls to `reportProgress` pass the correct `expectedPhase` for the orchestrator step.

Important: today the daemon writes STATE.md with phaseNumber 0 for “Initialized project” and “Created roadmap” steps; if we keep calling `reportProgress({ expectedPhase: 1 })` for those, mismatch warnings will be noisy. Fix by passing `expectedPhase: 0` for the initialization + roadmap steps, and `expectedPhase: phaseNum` for within the per-phase loop.

Keep mismatch non-fatal (warn only), and preserve existing SUMMARY-file presence warning behavior.
  </action>
  <verify>npm test</verify>
  <done>`reportProgress` warns on actual/expected phase mismatch, and expectedPhase values avoid spurious warnings during pre-phase steps</done>
</task>

<task type="auto">
  <name>Task 2: Add focused test coverage for orchestrator progress/mismatch behavior</name>
  <files>tests/orchestrator.progress.test.ts</files>
  <action>
Add `tests/orchestrator.progress.test.ts` that validates the new mismatch behavior without depending on log output from pino:
- Create a temp workspace with `.planning/STATE.md`, `.planning/ROADMAP.md`, and minimal phase directory + plan file.
- Use a stub AgentInvoker that writes deterministic STATE.md content at key points (especially after initialization/roadmap and during phase loop) so `readStateMd` returns snapshots.
- Pass a minimal “fake” logger (or a pino logger with a stubbed `.warn`) to `orchestrateGoal` so the test can assert whether a mismatch warning happened (e.g. capture warn calls into an array).

Test cases:
1) When snapshot.phaseNumber equals expectedPhase, no mismatch warning is emitted.
2) When snapshot.phaseNumber differs (e.g. expected 1 but state says 2), exactly one mismatch warning is emitted with expected/actual fields.

Prefer to keep this test hermetic and fast (no chokidar). You only need orchestrator + filesystem.
  </action>
  <verify>npm test</verify>
  <done>There is a deterministic test that asserts mismatch warnings and validates that expectedPhase plumbing is correct</done>
</task>

<task type="auto">
  <name>Task 3: Align docs with implemented progress/mismatch behavior and ownership</name>
  <files>docs/ARCHITECTURE.md, docs/CONTEXT-FOR-MODEL.md</files>
  <action>
Update docs to match reality after Task 1:
- In `docs/ARCHITECTURE.md`, ensure the data-flow step describing `reportProgress(expectedPhase)` matches actual behavior (warn on mismatch, non-fatal; expectedPhase = 0 during init/roadmap, else phase loop number).
- In `docs/CONTEXT-FOR-MODEL.md`, ensure “Orchestrator progress” section matches the above (warn-only mismatch, correct expectedPhase semantics).
- Re-assert the ownership model: the agent writes STATE.md, orchestrator/daemon read it; StateWatcher emits events from file changes.
  </action>
  <verify>npm test</verify>
  <done>Docs accurately describe reportProgress mismatch warning behavior, expectedPhase semantics, and STATE.md ownership</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm test` passes
- [ ] `reportProgress` emits warn-only mismatch when STATE.md phase differs
- [ ] Docs match the implemented behavior and expectedPhase semantics
</verification>

<success_criteria>

- All tasks completed
- Mismatch detection exists and is non-fatal (warn only)
- Tests cover progress/mismatch behavior deterministically
- Docs accurately describe monitoring + transition behavior and STATE.md ownership
- No regressions in existing tests
  </success_criteria>

<output>
After completion, create `.planning/phases/04-state-monitoring-phase-transitions/04-02-SUMMARY.md`:

# Phase 4 Plan 2: Orchestrator progress + transitions Summary

**[Substantive one-liner — what shipped]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `path/to/file` — Description

## Decisions Made

[Key decisions or "None"]

## Issues Encountered

[Problems and resolutions or "None"]

## Next Step

Phase 4 complete; ready for Phase 5 (Crash Detection & Recovery)
</output>
