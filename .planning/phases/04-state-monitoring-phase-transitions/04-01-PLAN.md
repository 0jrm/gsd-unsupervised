---
phase: 04-state-monitoring-phase-transitions
plan: 01
type: execute
depends_on: []
files_modified: [src/state-watcher.test.ts, docs/ARCHITECTURE.md]
domain: node
---

<objective>
Add reliable STATE.md monitoring validation: StateWatcher unit tests + clear STATE.md contract documentation.

Purpose: Ensure `.planning/STATE.md` can be watched and parsed reliably, and that downstream consumers (daemon, status server, Phase 5 resume) have a clear contract for what STATE.md must contain.
Output: Unit tests covering StateWatcher event emission, plus documentation for the STATE.md “## Current Position” contract and parser expectations.
</objective>

<execution_context>
/mnt/c/Users/jrm22n/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-lifecycle/02-02-SUMMARY.md
@.planning/phases/03-agent-integration/03-01-SUMMARY.md
@.planning/phases/03-agent-integration/03-02-SUMMARY.md
@docs/ARCHITECTURE.md
@docs/CONTEXT-FOR-MODEL.md
@src/state-watcher.ts
@src/state-parser.ts
@src/daemon.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add StateWatcher unit tests for progress events</name>
  <files>src/state-watcher.test.ts</files>
  <action>
Create `src/state-watcher.test.ts` using Vitest that validates StateWatcher behavior using a real temp file (avoid heavy mocking of chokidar).

Coverage targets:
- On first successful parse, emits `ready` exactly once with the snapshot.
- Always emits `state_changed` with `{ previous, current }` where `previous` starts as null then becomes last snapshot.
- Emits `phase_advanced` when `phaseNumber` increases (payload includes from/to and `phaseName` from the new snapshot).
- Emits `plan_advanced` when same phase and `planNumber` increases.
- Emits `phase_completed` when status transitions to something containing "complete" (case-insensitive) and either phase changed or previous status was not complete.
- Emits `goal_completed` only once when `phaseNumber === totalPhases` and status is complete; uses `progressPercent ?? 100`.

Implementation notes:
- Use `mkdtempSync(tmpdir())` + `path.join(tempDir, 'STATE.md')`.
- Use a very small debounce (e.g. 10-25ms) in watcher options and add a helper `await` to wait for events after writes (e.g. polling until received or a small timeout).
- Ensure `watcher.stop()` is always called (afterEach) to avoid open handles.
- Build STATE.md content using the exact line formats expected by `parseStateMd()` (Phase line, Plan line, Status, Last activity; include Progress line for the goal_completed case).
  </action>
  <verify>npm test</verify>
  <done>StateWatcher has tests that assert all progress events and their payloads, and the full test suite passes</done>
</task>

<task type="auto">
  <name>Task 2: Document the STATE.md “## Current Position” contract and parser expectations</name>
  <files>docs/ARCHITECTURE.md</files>
  <action>
Update `docs/ARCHITECTURE.md` with a short, explicit “STATE.md contract” subsection under the StateWatcher/state-parser discussion.

Document:
- The exact lines required by `src/state-parser.ts` (Phase/Plan/Status; Last activity optional; Progress optional).
- That `readStateMd(path)` returns `null` on missing file or parse failure (no throw).
- That StateWatcher ignores missing/unparseable state (debug log, no events beyond file error warnings).
- Any important regex constraints (e.g., Phase line must include `X of Y (Name)` and Plan line must include `in current phase`).

Keep it short and factual; this is a contract for Phase 5 resume and Phase 6 status server.
  </action>
  <verify>npm test</verify>
  <done>Architecture docs clearly specify the required STATE.md “Current Position” format and state-parser behavior</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test` passes
- [ ] `src/state-watcher.test.ts` covers all StateWatcher progress events
- [ ] `docs/ARCHITECTURE.md` includes an explicit STATE.md contract section that matches `src/state-parser.ts`
</verification>

<success_criteria>

- StateWatcher has reliable unit coverage for event emission and "emit once" semantics
- STATE.md format contract is documented for downstream consumers
- No regressions in existing test suite
  </success_criteria>

<output>
After completion, create `.planning/phases/04-state-monitoring-phase-transitions/04-01-SUMMARY.md`:

# Phase 4 Plan 1: STATE watcher and contract Summary

**[Substantive one-liner — what shipped]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `src/state-watcher.test.ts` — Added event-emission coverage for StateWatcher
- `docs/ARCHITECTURE.md` — Documented STATE.md parsing contract

## Decisions Made

[Key decisions or "None"]

## Issues Encountered

[Problems and resolutions or "None"]

## Next Step

Ready for 04-02-PLAN.md (orchestrator progress/mismatch logging + phase transition docs/tests)
</output>

