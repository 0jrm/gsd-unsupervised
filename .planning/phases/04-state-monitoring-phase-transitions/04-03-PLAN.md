---
phase: 04-state-monitoring-phase-transitions
plan: 03
type: execute
depends_on: ["04-02"]
files_modified: [src/orchestrator.ts, src/lifecycle.ts, src/orchestrator.test.ts or tests/]
domain: node
---

<objective>
Verify phase transitions and progress reporting: orchestrator reportProgress, phase/plan advancement consistency with STATE.md, and integration test for state/phase alignment.

Purpose: Ensure the orchestrator's expected phase matches STATE.md after each step, mismatch is logged, and lifecycle transitions produce the correct progress events when STATE.md is updated by the agent.
Output: Verified or updated reportProgress behavior, integration test (or unit test) that phase advancement and state snapshot stay aligned.
</objective>

<execution_context>
/mnt/c/Users/jrm22n/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-state-monitoring-phase-transitions/04-02-SUMMARY.md
@src/orchestrator.ts
@src/lifecycle.ts
@src/state-parser.ts
@docs/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify reportProgress and mismatch logging</name>
  <files>src/orchestrator.ts</files>
  <action>Confirm that after each successful agent call the orchestrator calls reportProgress(expectedPhase). When readStateMd returns a snapshot and snapshot.phaseNumber !== expectedPhase, a warning is logged (existing behavior). Ensure expectedPhase is correct at each call site (after new-project, after create-roadmap, after each plan-phase, after each execute-plan). No change required if already correct; otherwise fix and add a test that stub agent plus mock STATE.md content produces expected warning when phase mismatches.</action>
  <verify>Grep orchestrator for reportProgress and confirm each agent success path calls it with correct phase number; npm test passes</verify>
  <done>reportProgress called after every agent success with correct expectedPhase; mismatch logs warning</done>
</task>

<task type="auto">
  <name>Task 2: Add test for phase/plan advancement and state alignment</name>
  <files>src/orchestrator.test.ts or equivalent</files>
  <action>Add a test that simulates multi-phase flow: (1) Stub agent that returns success and optionally writes a temporary STATE.md with increasing phase/plan; (2) Orchestrator runs until phase complete or N steps; (3) Assert onProgress was called with snapshots that match expected phase/plan progression, or assert reportProgress read StateSnapshot with expected phase. Alternatively, test that when STATE.md is updated (e.g. by test writing file), readStateMd in reportProgress returns the updated snapshot. Use existing orchestrator test patterns (stub agent, temp dir for workspaceRoot).</action>
  <verify>npm test -- orchestrator (or equivalent) passes with new test</verify>
  <done>Test exists that verifies progress reporting or state alignment during orchestration</done>
</task>

<task type="auto">
  <name>Task 3: Document phase transition flow</name>
  <files>docs/ARCHITECTURE.md or docs/CONTEXT-FOR-MODEL.md</files>
  <action>Add a short subsection describing how phase/plan advancement works: orchestrator advances GoalStateMachine (PlanningPhase → ExecutingPlan → PhaseComplete), calls reportProgress(phaseNum) after each agent success, and StateWatcher (when running) emits phase_advanced/plan_advanced when STATE.md content changes. Note that STATE.md is written by the GSD agent (cursor-agent), not by the orchestrator; the orchestrator only reads it. This clarifies ownership and helps Phase 5 resume logic.</action>
  <verify>Docs mention orchestrator reads STATE.md, agent writes it; phase transition flow is clear</verify>
  <done>Phase transition and STATE.md ownership documented</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] npm test passes
- [ ] reportProgress behavior verified or tests added
- [ ] Phase transition flow documented
</verification>

<success_criteria>

- All tasks completed
- reportProgress and mismatch handling verified
- At least one test covers progress/state alignment
- Documentation updated for phase transitions and STATE.md ownership
  </success_criteria>

<output>
After completion, create `.planning/phases/04-state-monitoring-phase-transitions/04-03-SUMMARY.md`:

# Phase 4 Plan 3: Phase Transitions Summary

**[Substantive one-liner]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `path/to/file` — Description

## Decisions Made

[Key decisions or "None"]

## Issues Encountered

[Problems and resolutions or "None"]

## Next Step

Phase 4 complete; ready for Phase 5 (Crash Detection & Recovery)
</output>
