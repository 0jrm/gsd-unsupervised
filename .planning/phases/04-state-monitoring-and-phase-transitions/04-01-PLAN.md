---
phase: 04-state-monitoring-and-phase-transitions
plan: 01
type: execute
depends_on: ["03-01", "03-02"]
files_modified:
  - src/state-parser.ts
  - src/state-types.ts
  - src/state-index.ts
  - src/state-watcher.ts
domain: node-cli
---

<objective>
Provide a robust STATE.md watcher and parser that convert file changes into a typed in-memory representation and debounced progress events for the daemon.

Purpose: Enable the daemon to observe GSD state in real time by reacting to .planning/STATE.md edits.
Output: Typed StateSnapshot model, resilient parser, readStateFile/parseStateFile API, and a chokidar-based StateWatcher that emits state_changed and lifecycle progress events.
</objective>

<execution_context>
/mnt/c/Users/jrm22n/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/config/index.ts
@src/logger/index.ts
@.planning/phases/03-agent-integration/03-01-SUMMARY.md
@.planning/phases/03-agent-integration/03-02-SUMMARY.md

**Tech stack:** Node 18+, TypeScript, chokidar for file watching, pino for logging.
**Established patterns:** Centralized config, pino structured logging, small focused modules.
**Constraining decisions:** Phase 1–3 established CLI, lifecycle, and agent invocation; do not change their public contracts.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Typed state model and parser for STATE.md</name>
  <files>src/state-types.ts, src/state-parser.ts, src/state-index.ts</files>
  <action>
  Ensure a TypeScript model in src/state-types.ts represents the current GSD state (phase number/name, total phases, current plan, total plans, status, lastActivity, gitSha, progressPercent as in .planning/STATE.md).
  Ensure src/state-parser.ts parses the "## Current Position" (and optional "## Progress") section: accepts raw STATE.md string and returns a typed StateSnapshot; tolerates extra sections and whitespace; returns null and does not throw on malformed content.
  Ensure src/state-index.ts exports parseStateFile(contents, logger?) and readStateFile(filePath, logger?) that return StateSnapshot | null, logging parse/read failures via the shared logger. Re-export StateSnapshot from state-types.
  Do not hard-code formatting assumptions that break when extra sections are added; parse headings and key-value lines defensively.
  </action>
  <verify>Run project test command; state-parser tests and readStateMd/parseStateMd behavior cover normal, partial, and malformed STATE.md inputs.</verify>
  <done>parseStateFile/readStateFile return a typed snapshot for valid .planning/STATE.md; malformed content yields null and logged warning; no unhandled throws or build errors.</done>
</task>

<task type="auto">
  <name>Task 2: STATE.md file watcher with debounced change notifications</name>
  <files>src/state-watcher.ts, src/state-index.ts</files>
  <action>
  Ensure a StateWatcher in src/state-watcher.ts uses chokidar to watch the STATE.md path (provided via options, e.g. from config workspaceRoot + '.planning/STATE.md'). Debounce rapid change events (e.g. 100–500 ms). On startup and on each debounced change, read the file via readStateFile from state-index; on success emit an initial 'ready' (first snapshot) and subsequent 'state_changed' with { previous, current }. On parse/read failure log and skip emission; do not crash. Expose start() and stop() for daemon lifecycle; on error event from chokidar log and do not unhandled-reject. Optionally derive and emit phase_advanced, plan_advanced, phase_completed, goal_completed from snapshot diffs for progress visibility.
  </action>
  <verify>With .planning/STATE.md present, start the watcher, edit STATE.md (phase/status), and confirm one debounced state_changed (and any progress events) per logical change; corrupt STATE.md and confirm process stays up and errors are logged.</verify>
  <done>Watcher emits initial snapshot and debounced updates on change; parse/FS errors are logged and do not terminate the process; stop() cleans up chokidar and timers.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] TypeScript/build succeeds.
- [ ] Watcher emits at least one snapshot when .planning/STATE.md is edited.
- [ ] Malformed or missing STATE.md is handled without unhandled exceptions.
</verification>

<success_criteria>

- StateSnapshot type and parser exist and are exported via state-index (parseStateFile, readStateFile).
- StateWatcher watches .planning/STATE.md and delivers debounced snapshots and progress events.
- Other code can depend on readStateFile and StateWatcher without touching parser/watcher internals.
  </success_criteria>

<output>
After completion, create `.planning/phases/04-state-monitoring-and-phase-transitions/04-01-SUMMARY.md`:

# Phase 4 Plan 1: STATE.md Watcher and Parser Summary

**[One-line: robust STATE.md parsing and watching with typed snapshot and debounced events.]**

## Accomplishments

- Typed state model and parser for .planning/STATE.md.
- Debounced file watcher emitting state_changed and progress events.

## Files Created/Modified

- src/state-types.ts, src/state-parser.ts, src/state-index.ts, src/state-watcher.ts

## Decisions Made

[Any parsing or debounce/event decisions, or "None".]

## Issues Encountered

[Or "None".]

## Next Step

Ready for 04-02-PLAN.md (daemon wiring and progress event consumption).
</output>
