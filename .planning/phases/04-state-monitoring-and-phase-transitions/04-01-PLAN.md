---
phase: 04-state-monitoring-and-phase-transitions
plan: 01
type: execute
depends_on: ["03-01", "03-02"]
files_modified:
  - src/state/state-file-watcher.ts
  - src/state/state-parser.ts
  - src/state/types.ts
  - src/state/index.ts
domain: node-cli
---

<objective>
Build a robust `STATE.md` watcher and parser that converts file changes into a structured in-memory representation the rest of the daemon can consume.

Purpose: Enable the daemon to observe GSD state changes in real time by reacting to `STATE.md` edits rather than polling or guessing.
Output: A reusable file-watching utility, a resilient `STATE.md` parser with validation, and a typed state snapshot API exposed from the state module.
</objective>

<execution_context>
/mnt/c/Users/jrm22n/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/config/index.ts
@src/logger/index.ts
@src/state/index.ts

**Tech stack available:** Node 18+, TypeScript, chokidar for file watching, zod for parsing/validation, pino for logging.
**Established patterns:** Centralized config module, pino-based structured logging, small focused modules exported via `index.ts`.
**Constraining decisions:**
- Phase 1–3 established CLI, lifecycle, and agent invocation; this plan must not change their public contracts.

**Issues being addressed:** Requirement in PROJECT.md for "State watching and phase transition detection (STATE.md, state parser)".
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce typed state model and parser for STATE.md</name>
  <files>src/state/types.ts, src/state/state-parser.ts, src/state/index.ts</files>
  <action>
  Define a TypeScript model that represents the current GSD state (phase number/name, total phases, current plan, status string, last-activity timestamp, git SHA, and any other stable fields present in `.planning/STATE.md`).
  Implement a resilient parser in `src/state/state-parser.ts` that:
  - Accepts a raw `STATE.md` string and returns a typed `StateSnapshot` object.
  - Tolerates additional sections, whitespace, and comments without failing.
  - Emits clear, structured errors for malformed or missing required sections, rather than throwing untyped exceptions.
  - Logs parse failures via the shared logger, including enough context to debug but not full file contents.
  Export a stable parsing API from `src/state/index.ts` (for example, `parseStateFile(contents: string): StateSnapshot`) that other modules can call without knowing about the underlying validation library.
  Avoid hard-coding formatting assumptions that would break if extra sections are added; instead, parse headings and key-value lines defensively.
  </action>
  <verify>
  - If a test runner already exists, add or update tests to exercise normal, partially-complete, and malformed STATE.md inputs and run the test command.
  - Otherwise, add a small Node script or dev-only function that imports `parseStateFile` with sample STATE.md content and logs the resulting object for manual inspection.
  </verify>
  <done>
  - `parseStateFile` returns a strongly-typed snapshot when given the current `.planning/STATE.md`.
  - Malformed or incomplete content produces a controlled, logged error rather than crashing the process.
  - No TypeScript or build errors are introduced.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement STATE.md file watcher with debounced change notifications</name>
  <files>src/state/state-file-watcher.ts, src/state/index.ts, src/config/index.ts</files>
  <action>
  Implement a `STATE.md` watcher in `src/state/state-file-watcher.ts` using chokidar (or the project's existing file-watching abstraction) that:
  - Watches the `.planning/STATE.md` file at the workspace root, using config to resolve the workspace path instead of hard-coding it.
  - Debounces rapid change events (for example, 100–300 ms) to avoid duplicate parsing on single saves.
  - Reads the file on startup to emit an initial snapshot, then emits new snapshots whenever the file changes successfully.
  - Uses the `parseStateFile` API from Task 1 for all parsing, handling parse failures by logging an error and skipping emission (not crashing the daemon).
  Export a public API from `src/state/index.ts` (for example, `createStateWatcher`) that accepts a callback like `(snapshot: StateSnapshot) => void` and returns a stop function for cleanup in daemon shutdown.
  Ensure watcher errors (permissions, missing file) are surfaced via structured logs and do not cause unhandled promise rejections.
  </action>
  <verify>
  - Run the CLI/daemon in a local environment where `.planning/STATE.md` exists.
  - Manually edit `.planning/STATE.md` to change the phase or status fields and observe logs to confirm that new snapshots are parsed and delivered once per save.
  - Intentionally corrupt a copy of STATE.md (for example, remove the "Phase" line) and confirm that the watcher logs a parse error but keeps the process alive.
  </verify>
  <done>
  - On startup, the watcher emits an initial snapshot that matches the current file.
  - Subsequent edits to `.planning/STATE.md` result in debounced snapshot emissions with updated values.
  - Error scenarios are logged and do not terminate the process.
  </done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] TypeScript compilation (or the project's build step) succeeds.
- [ ] The watcher emits at least one snapshot when `.planning/STATE.md` is edited manually.
- [ ] The daemon remains stable when given malformed STATE.md content (no unhandled exceptions).
</verification>

<success_criteria>

- Typed `StateSnapshot` model and parser exist and are exported from the state module.
- A chokidar-based watcher monitors `.planning/STATE.md` and delivers debounced snapshots.
- Other phases can depend on a stable `createStateWatcher` / `parseStateFile` API without caring about file system details.
  </success_criteria>

<output>
After completion, create `.planning/phases/04-state-monitoring-and-phase-transitions/04-01-SUMMARY.md`:

# Phase 4 Plan 1: STATE.md Watcher and Parser Summary

**Robust STATE.md parsing and watching is in place, exposing a typed state snapshot stream to the daemon.**

## Accomplishments

- Implemented typed state model and parser for `.planning/STATE.md`.
- Added a debounced file watcher that emits snapshots on change without crashing on errors.

## Files Created/Modified

- `src/state/types.ts` — State snapshot types.
- `src/state/state-parser.ts` — Parser implementation for `STATE.md`.
- `src/state/state-file-watcher.ts` — File watcher for `STATE.md`.
- `src/state/index.ts` — Public state API exports.

## Decisions Made

- Centralized `STATE.md` parsing and watching behind a dedicated state module.

## Issues Encountered

- [Document any parsing edge cases or watcher issues, or "None".]

## Next Step

- Ready for `04-02-PLAN.md` to consume watcher events for phase/plan advancement and progress events.
</output>

