---
phase: 04-state-monitoring-and-phase-transitions
plan: 02
type: execute
depends_on: ["04-01"]
files_modified:
  - src/lifecycle/state-adapter.ts
  - src/lifecycle/phase-transition-engine.ts
  - src/events/progress-emitter.ts
  - src/events/types.ts
  - src/daemon/index.ts
domain: node-cli
---

<objective>
Wire STATE.md snapshots into the lifecycle engine so that phase/plan advancement and progress events are driven by the parsed state instead of ad-hoc logic.

Purpose: Ensure the daemon advances phases and plans deterministically based on the single source of truth in `STATE.md`, while emitting structured progress events for logging and status reporting.
Output: A small adapter from `StateSnapshot` to lifecycle state, a transition engine that reacts to snapshot changes, and an event emitter for progress/transition events used by the daemon and status server.
</objective>

<execution_context>
/mnt/c/Users/jrm22n/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/04-state-monitoring-and-phase-transitions/04-01-PLAN.md
@.planning/phases/02-lifecycle/02-01-SUMMARY.md
@.planning/phases/02-lifecycle/02-02-SUMMARY.md
@.planning/phases/03-agent-integration/03-01-SUMMARY.md
@.planning/phases/03-agent-integration/03-02-SUMMARY.md

@src/state/index.ts
@src/lifecycle/index.ts
@src/daemon/index.ts
@src/logger/index.ts

**Tech stack available:** Node 18+, TypeScript, existing lifecycle state machine, event emitter or pub/sub patterns, pino logging.
**Established patterns:** Lifecycle orchestrator from Phase 2, agent invocation from Phase 3, centralized logger and config, small modules re-exported via `index.ts`.
**Constraining decisions:**
- Lifecycle states and transitions defined in Phase 2 must remain the source of truth for behavior; this plan should adapt STATE.md into those existing concepts rather than inventing a parallel model.
- Progress events should be structured and machine-readable for both logs and a future status server.

**Issues being addressed:** PROJECT.md requirements for "State watching and phase transition detection (STATE.md, state parser)" and for progress events to support status visibility.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map StateSnapshot into lifecycle state and detect transitions</name>
  <files>src/lifecycle/state-adapter.ts, src/lifecycle/phase-transition-engine.ts, src/lifecycle/index.ts</files>
  <action>
  Implement a small adapter in `src/lifecycle/state-adapter.ts` that converts the `StateSnapshot` produced by Phase 4 Plan 1 into whatever internal lifecycle state representation the orchestrator uses (for example, enums like New, PlanningPhase, ExecutingPlan, PhaseComplete, Failed).
  In `src/lifecycle/phase-transition-engine.ts`, implement a transition engine that:
  - Keeps track of the previous `StateSnapshot` (or derived lifecycle view) and the current one.
  - Computes diff-like information (for example, phase number changed, plan index changed, status changed) and classifies them into high-level events such as "phase started", "plan started", "plan completed", "phase completed", "run failed".
  - Ignores no-op updates where nothing meaningful changed (e.g., only timestamps).
  - Delegates to existing lifecycle/state-machine helpers wherever possible instead of duplicating logic.
  Export a minimal public API (for example, `deriveLifecycleState(snapshot)`, `detectTransitions(prev, next)`) from `src/lifecycle/index.ts` so the daemon can plug it into the daemon loop without circular dependencies.
  Ensure the adapter and engine handle unknown or out-of-range phase/plan values gracefully by logging warnings and avoiding undefined behavior.
  </action>
  <verify>
  - Add or update tests (if a test runner exists) to feed pairs of synthetic `StateSnapshot` objects and assert that the correct transition events are produced.
  - Manually exercise the adapter with a small script that imports `detectTransitions` and logs events for a few hand-crafted snapshot sequences.
  </verify>
  <done>
  - Given two snapshots representing different phases/plans, `detectTransitions` emits the expected sequence of high-level events.
  - Invalid or unexpected snapshot values are logged and do not crash the process.
  - No changes are required to existing lifecycle consumers beyond calling the new adapter/transition helpers.
  </done>
</task>

<task type="auto">
  <name>Task 2: Emit structured progress and transition events from the daemon</name>
  <files>src/events/types.ts, src/events/progress-emitter.ts, src/daemon/index.ts, src/logger/index.ts</files>
  <action>
  Define event types in `src/events/types.ts` representing progress and transition events (for example, `PhaseStarted`, `PlanStarted`, `PlanCompleted`, `PhaseCompleted`, `RunFailed`) with fields that include phase number/name, plan number, timestamps, and any relevant metadata.
  Implement a progress emitter in `src/events/progress-emitter.ts` (using Node's EventEmitter or a minimal custom pub/sub) that:
  - Exposes a way to subscribe to events from other parts of the system (status server, logs).
  - Provides helper functions (for example, `emitPhaseStarted`, `emitPlanCompleted`) that take strongly-typed payloads and emit the underlying event objects.
  In `src/daemon/index.ts` (or the main orchestration loop), wire together:
  - The `createStateWatcher` from Plan 1,
  - The `detectTransitions` helper from Task 1,
  - And the new progress emitter so that each incoming `StateSnapshot` is diffed against the previous one and translated into one or more progress events.
  For now, log each emitted event to the existing logger in a structured way (for example, `logger.info({ event }, "progress event")`) so progress can be inspected even before the HTTP status server exists.
  Design the event API so that Phase 6 (Status Server) can reuse it without refactoring (avoid hard-coding logging concerns into the emitter itself).
  </action>
  <verify>
  - Run the daemon and manually edit `.planning/STATE.md` to simulate phase and plan changes (for example, from Phase 3 to Phase 4, or plan counts).
  - Observe logs to confirm that meaningful progress events are emitted exactly once per logical transition (not on every file write).
  - If tests exist, add coverage that ensures event objects are well-formed and that emitter subscribers receive events in the expected order for a simple scenario.
  </verify>
  <done>
  - Transition events are emitted whenever phase/plan-related fields in STATE.md change.
  - Logs show structured progress events that can later be consumed by a status server without redesign.
  - The daemon shuts down cleanly, disposing of the state watcher and removing event listeners to avoid leaks.
  </done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] The daemon uses the new transition engine and progress emitter without compile errors.
- [ ] Editing `.planning/STATE.md` to represent different lifecycle stages produces the correct sequence of progress events in logs.
- [ ] No duplicate or spurious events are emitted for unchanged snapshots.
</verification>

<success_criteria>

- STATE.md snapshots drive lifecycle transitions via a clear adapter and transition engine.
- Progress and transition events are emitted as structured objects and logged consistently.
- The system is ready for Phase 5 (Crash Detection &amp; Recovery) and Phase 6 (Status Server) to consume progress information without invasive changes.
  </success_criteria>

<output>
After completion, create `.planning/phases/04-state-monitoring-and-phase-transitions/04-02-SUMMARY.md`:

# Phase 4 Plan 2: Phase/Plan Advancement and Progress Events Summary

**Lifecycle transitions are now driven by STATE.md snapshots and produce structured progress events for the daemon and future status server.**

## Accomplishments

- Implemented adapter and transition engine from `StateSnapshot` to lifecycle events.
- Wired a progress emitter into the daemon to emit structured phase/plan events.

## Files Created/Modified

- `src/lifecycle/state-adapter.ts` — Maps snapshots into lifecycle state.
- `src/lifecycle/phase-transition-engine.ts` — Detects transitions between snapshots.
- `src/events/types.ts` — Progress/transition event types.
- `src/events/progress-emitter.ts` — Central progress event emitter.
- `src/daemon/index.ts` — Integrated watcher, transition engine, and emitter.

## Decisions Made

- Treat STATE.md as the single source of truth for phase/plan advancement, with lifecycle transitions derived from snapshots.

## Issues Encountered

- [Document any edge cases or logging concerns discovered, or "None".]

## Next Step

- Phase 4 is complete; the system is ready for Phase 5 to build crash detection and resume logic on top of the state and progress events.
</output>

