---
phase: 04-state-monitoring-and-phase-transitions
plan: 02
type: execute
depends_on: ["04-01"]
files_modified:
  - src/state-watcher.ts
  - src/daemon.ts
domain: node-cli
---

<objective>
Drive phase/plan advancement and progress events from STATE.md snapshots so the daemon and future status server have a single source of truth and structured progress visibility.

Purpose: Lifecycle and progress are derived from STATE.md via the state watcher; no ad-hoc phase/plan logic outside the watcher.
Output: StateWatcher emits typed progress events (phase_advanced, plan_advanced, phase_completed, goal_completed); daemon wires the watcher and logs structured progress events; shutdown cleans up the watcher.
</objective>

<execution_context>
/mnt/c/Users/jrm22n/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-state-monitoring-and-phase-transitions/04-01-SUMMARY.md
@.planning/phases/02-lifecycle/02-01-SUMMARY.md
@.planning/phases/02-lifecycle/02-02-SUMMARY.md
@src/state-watcher.ts
@src/daemon.ts
@src/logger/index.ts

**Tech stack:** Node 18+, TypeScript, EventEmitter, pino.
**Established patterns:** Lifecycle from Phase 2; agent invocation from Phase 3; state parser and watcher from 04-01.
**Constraining decisions:** Lifecycle states from Phase 2 remain the source of truth; progress events must be structured and machine-readable for logs and status server (Phase 6).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Emit phase/plan transition events from StateWatcher</name>
  <files>src/state-watcher.ts</files>
  <action>
  Ensure StateWatcher (or the module that owns snapshot diffing) compares previous vs current StateSnapshot and emits typed progress events: phase_advanced (fromPhase, toPhase, phaseName), plan_advanced (phaseNumber, fromPlan, toPlan), phase_completed (phaseNumber, phaseName), goal_completed when progress indicates 100% or equivalent. Emit only when meaningful fields change (phase, plan, status, progress); ignore no-op updates (e.g. timestamp-only). Handle out-of-range or missing phase/plan values by logging and not emitting invalid events. Export ProgressEvent (or equivalent) types so daemon and status server can consume them without relying on string event names only.
  </action>
  <verify>Unit tests or manual script: feed two snapshots (e.g. phase 3→4, plan 0→1) and assert the correct event sequence; no duplicate events for identical snapshots.</verify>
  <done>Snapshot diffs produce exactly the intended progress events; types are exported; no-op updates do not emit.</done>
</task>

<task type="auto">
  <name>Task 2: Wire StateWatcher in daemon and log structured progress events</name>
  <files>src/daemon.ts</files>
  <action>
  Ensure the daemon creates StateWatcher with config (stateMdPath from workspaceRoot + '.planning/STATE.md', debounceMs, logger). Subscribe to state_changed, phase_advanced, plan_advanced, phase_completed, goal_completed. For each event log a structured message (e.g. logger.info({ event }, 'progress event')) so progress is visible in logs and the same event shape can be consumed by the status server later. In the daemon shutdown path (or when leaving the goal loop), call watcher.stop() and remove listeners to avoid leaks and unhandled events after shutdown.
  </action>
  <verify>Run daemon with existing .planning/STATE.md; edit STATE.md to change phase or plan; confirm logs show one structured progress event per logical transition. Stop daemon and confirm no watcher-related errors or leaks.</verify>
  <done>Daemon uses StateWatcher for the active goal loop; all progress events are logged in a structured way; watcher is stopped and listeners removed on shutdown.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] Daemon builds and runs with StateWatcher wired.
- [ ] Editing STATE.md to reflect phase/plan changes produces the correct progress events in logs.
- [ ] No duplicate or spurious events for unchanged snapshots; shutdown cleans up watcher.
</verification>

<success_criteria>

- STATE.md drives phase/plan transition events via StateWatcher.
- Daemon logs structured progress events suitable for Phase 6 status server consumption.
- Phase 4 complete; ready for Phase 5 (crash detection/resume) and Phase 6 (status server).
  </success_criteria>

<output>
After completion, create `.planning/phases/04-state-monitoring-and-phase-transitions/04-02-SUMMARY.md`:

# Phase 4 Plan 2: Phase/Plan Advancement and Progress Events Summary

**[One-line: lifecycle transitions and progress events driven by STATE.md and wired in the daemon.]**

## Accomplishments

- StateWatcher emits typed phase/plan transition events from snapshot diff.
- Daemon wires watcher and logs structured progress events; clean shutdown.

## Files Created/Modified

- src/state-watcher.ts, src/daemon.ts

## Decisions Made

[Any event shape or logging decisions, or "None".]

## Issues Encountered

[Or "None".]

## Next Step

Phase 4 complete; ready for Phase 5 (Crash Detection & Recovery).
</output>
