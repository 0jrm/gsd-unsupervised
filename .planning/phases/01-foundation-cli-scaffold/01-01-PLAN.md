---
phase: 01-foundation-cli-scaffold
plan: 01
type: execute
depends_on: []
files_modified: [package.json, tsconfig.json, src/cli.ts, bin/unsupervised-gsd]
---

<objective>
Initialize the Node.js project and create the CLI entry point with argument parsing.

Purpose: Establish the project skeleton that all subsequent phases build on — package config, TypeScript compilation, directory layout, and the user-facing CLI interface.
Output: A runnable `unsupervised-gsd` CLI binary that parses flags and prints help, ready for config/goals/daemon wiring.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@goals.md

**Constraining decisions:**
- Stack: Node.js, ESM modules
- No build frameworks — TypeScript compiled directly with tsc
- CLI daemon that reads goals.md and accepts --parallel flag
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Node.js project with TypeScript</name>
  <files>package.json, tsconfig.json, src/.gitkeep, bin/unsupervised-gsd</files>
  <action>
Create package.json with:
- name: "unsupervised-gsd"
- type: "module" (ESM)
- bin: { "unsupervised-gsd": "./bin/unsupervised-gsd" }
- scripts: { "build": "tsc", "start": "node dist/cli.js", "dev": "tsc --watch" }
- engines: { "node": ">=18" }

Install dependencies:
- commander (CLI argument parsing)
- zod (config validation)
- pino (structured logging)
- pino-pretty (dev log formatting)

Install dev dependencies:
- typescript
- @types/node

Create tsconfig.json:
- target: ES2022
- module: Node16
- moduleResolution: Node16
- outDir: ./dist
- rootDir: ./src
- strict: true
- esModuleInterop: true
- declaration: true

Create directory structure:
- src/ (source root)
- bin/unsupervised-gsd (shell shim: #!/usr/bin/env node, imports ../dist/cli.js)

Create .gitignore with node_modules/, dist/, *.tgz.
  </action>
  <verify>npm install exits 0, npx tsc --noEmit exits 0 (once src/cli.ts exists), bin/unsupervised-gsd is executable</verify>
  <done>package.json valid with all deps, tsconfig compiles, directory structure exists, bin shim is executable</done>
</task>

<task type="auto">
  <name>Task 2: Create CLI entry point with commander</name>
  <files>src/cli.ts</files>
  <action>
Create src/cli.ts as the main entry point using commander:

Define program with:
- name: "unsupervised-gsd"
- description: "Autonomous orchestrator for Cursor agent + GSD framework"
- version: read from package.json (use createRequire or inline)

Options:
- --goals <path> : Path to goals.md file (default: "./goals.md")
- --config <path> : Path to config JSON file (default: "./.autopilot/config.json")
- --parallel : Enable parallel project execution (default: false)
- --max-concurrent <n> : Max concurrent projects when parallel (default: 3)
- --verbose : Enable verbose/debug logging (default: false)
- --dry-run : Parse goals and show plan without executing (default: false)

Action handler: For now, log parsed options and exit. The daemon loop (Plan 03) will replace this stub.

Export a main() function that calls program.parse(). The bin shim calls main().

Ensure the file compiles and the CLI responds to --help and --version.
  </action>
  <verify>npm run build succeeds, ./bin/unsupervised-gsd --help shows all options, ./bin/unsupervised-gsd --version prints version</verify>
  <done>CLI binary responds to --help with all documented flags, --version prints correct version, unknown flags produce error</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm install` succeeds without errors
- [ ] `npm run build` compiles TypeScript without errors
- [ ] `./bin/unsupervised-gsd --help` shows all options
- [ ] `./bin/unsupervised-gsd --version` prints version from package.json
- [ ] `./bin/unsupervised-gsd --parallel --goals goals.md --verbose` logs parsed options
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript compilation errors
- CLI responds correctly to all flags
- Project structure follows ESM conventions
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-cli-scaffold/01-01-SUMMARY.md`
</output>
