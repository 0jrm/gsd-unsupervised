---
phase: 02-lifecycle
plan: 02
type: execute
depends_on: ["02-01"]
files_modified: [src/orchestrator.ts, tests/orchestrator.lifecycle.test.ts]
domain: node
---

<objective>
Verify and harden the orchestrator's command sequence and phase/plan loop behavior.

Purpose: Ensure the orchestrator drives the GSD command sequence in the correct order, correctly skips already-present artifacts (PROJECT/ROADMAP), and correctly discovers/executes plans from `.planning/phases/`.
Output: A focused orchestrator lifecycle test suite plus any small fixes required for determinism and correctness.
</objective>

<execution_context>
.gsd-framework/workflows/execute-plan.md
.gsd-framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-lifecycle/02-01-SUMMARY.md
@docs/CONTEXT-FOR-MODEL.md
@src/orchestrator.ts
@src/roadmap-parser.ts
@src/lifecycle.ts
@src/state-parser.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an orchestrator lifecycle test that asserts command order</name>
  <files>tests/orchestrator.lifecycle.test.ts</files>
  <action>Create or extend `tests/orchestrator.lifecycle.test.ts` using Vitest and a temp workspace directory (similar patterns to existing `tests/*.test.ts`). Use a stub AgentInvoker that records every `GsdCommand` it receives and returns `{ success: true }`. Provide minimal `.planning/ROADMAP.md` with at least 2 phases and create matching `.planning/phases/NN-name/NN-01-PLAN.md` files plus optional summaries to simulate executed/unexecuted. Assert the orchestrator issues commands in strict order: (a) `/gsd/new-project` only when `.planning/PROJECT.md` absent; (b) `/gsd/create-roadmap` only when `.planning/ROADMAP.md` absent; (c) `/gsd/plan-phase N` for each phase; (d) `/gsd/execute-plan <path>` for each unexecuted plan discovered (plan has PLAN but no SUMMARY). Also assert that when a phase directory is missing, orchestrator skips execute-plan for that phase and continues planning the next phase.</action>
  <verify>npm test -- orchestrator.lifecycle passes</verify>
  <done>Test proves command ordering and plan discovery behavior for both "initialized" and "fresh" workspaces</done>
</task>

<task type="auto">
  <name>Task 2: Make any small orchestrator fixes required by the test (determinism + correctness)</name>
  <files>src/orchestrator.ts</files>
  <action>If the new test reveals incorrect ordering, missing skips, or nondeterministic behavior, apply minimal fixes in `src/orchestrator.ts`. Keep behavior aligned with `docs/CONTEXT-FOR-MODEL.md`: orchestrator treats GSD as black box, executes plan-by-plan (not execute-phase), and uses SUMMARY existence to decide executed vs unexecuted plans. Avoid large refactors; focus on correctness and stable tests.</action>
  <verify>npm test passes (full suite)</verify>
  <done>Orchestrator behavior matches expected command sequence and all tests pass</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm test passes
- [ ] orchestrator lifecycle test asserts command order and plan execution selection
</verification>

<success_criteria>

- All tasks completed
- Orchestrator command sequence is covered by at least one end-to-end-ish unit/integration style test
- No regressions in existing tests
  </success_criteria>

<output>
After completion, create `.planning/phases/02-lifecycle/02-02-SUMMARY.md`:

# Phase 2 Plan 2: Orchestration loop Summary

**[Substantive one-liner — e.g. orchestrator lifecycle tested for correct command ordering and plan selection]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `tests/orchestrator.lifecycle.test.ts` — Lifecycle/command ordering coverage for orchestrateGoal
- `src/orchestrator.ts` — (If changed) Minimal fixes for correctness/determinism

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase 2 complete; ready for Phase 3 (Agent Integration)
</output>
