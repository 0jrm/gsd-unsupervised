---
phase: 02-lifecycle
plan: 01
type: execute
depends_on: []
files_modified: [src/lifecycle.ts, src/lifecycle.test.ts]
domain: node
---

<objective>
Harden and verify the GSD lifecycle state machine (GoalStateMachine) used by the orchestrator.

Purpose: Make lifecycle transitions explicit, testable, and robust so orchestration cannot silently enter invalid states.
Output: A covered GoalStateMachine with transition validation tests and any small safety improvements needed.
</objective>

<execution_context>
/mnt/c/Users/jrm22n/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@docs/CONTEXT-FOR-MODEL.md
@src/lifecycle.ts
@src/orchestrator.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GoalStateMachine unit tests for transitions and errors</name>
  <files>src/lifecycle.test.ts</files>
  <action>Create `src/lifecycle.test.ts` using Vitest. Cover: (1) initial phase is `new`; (2) valid transitions per `LIFECYCLE_TRANSITIONS` succeed; (3) invalid transition throws with helpful message including current phase, target, and allowed set; (4) `fail()` moves to `failed` and sets error; (5) `isTerminal()` true for complete/failed. Keep tests behavior-focused (don’t snapshot whole objects unless stable).</action>
  <verify>npm test -- lifecycle passes</verify>
  <done>Lifecycle transitions, invalid-transition error, and terminal states are covered by tests</done>
</task>

<task type="auto">
  <name>Task 2: Verify getNextCommand behaviors for early lifecycle states</name>
  <files>src/lifecycle.ts, src/lifecycle.test.ts</files>
  <action>Ensure `getNextCommand()` returns the expected commands for the early lifecycle states: `new` → `/gsd/new-project`, `initializing_project` → `/gsd/create-roadmap`, `phase_complete` (when `currentPhaseNumber < totalPhases`) → `/gsd/plan-phase (currentPhaseNumber+1)`. Add/extend tests for these, including that `planning_phase` and `executing_plan` return null (or remain consistent with the orchestrator owning that loop). Avoid changing the orchestrator’s command loop here—this is about correctness and clarity in the state machine.</action>
  <verify>npm test passes; tests explicitly assert getNextCommand outputs for key phases</verify>
  <done>getNextCommand contract is tested and consistent with orchestrator behavior</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm test passes
- [ ] lifecycle tests cover invalid transitions and getNextCommand for early phases
</verification>

<success_criteria>

- All tasks completed
- Tests exist for GoalStateMachine transitions, terminal state, failure, and getNextCommand
- No regressions in existing test suite
  </success_criteria>

<output>
After completion, create `.planning/phases/02-lifecycle/02-01-SUMMARY.md`:

# Phase 2 Plan 1: Lifecycle state machine Summary

**[Substantive one-liner — e.g. lifecycle state machine covered with transition/getNextCommand tests]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `src/lifecycle.test.ts` — Added unit tests for GoalStateMachine transitions and command selection
- `src/lifecycle.ts` — (If changed) Small hardening / clarified contract

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 02-02-PLAN.md (Command sequence and orchestration loop)
</output>
