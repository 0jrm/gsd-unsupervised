---
phase: 07-wsl-bootstrap
plan: 01
type: execute
depends_on: []
files_modified:
  - src/config/wsl.ts
  - src/config/paths.ts
  - src/bootstrap/wsl-bootstrap.ts
  - src/cli.ts
  - src/cursor-agent.ts
  - README.md
domain: node-cli
---

<objective>
Implement WSL-aware bootstrap and path resolution so the Unsupervised GSD CLI can reliably locate Cursor, clip.exe, and the workspace when running under WSL 2.

Purpose: Make the CLI robust in WSL environments by centralizing platform/WSL detection and path normalization, eliminating ad-hoc path handling.
Output: WSL detection module, shared path resolution utilities, bootstrap applied at CLI startup, and documentation for WSL behavior and overrides.
</objective>

<execution_context>
/mnt/c/Users/jrm22n/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-status-server/06-01-SUMMARY.md

**Assumptions:**
- Project is a Node/TypeScript CLI with existing daemon/CLI entrypoint (Phases 1–6 complete).
- WSL usage is primary; Windows host is available at `/mnt/c`.
- Cursor agent and clip.exe are on the Windows side or PATH; resolution order is env override → config → WSL-aware default.

**Key goals from roadmap (Phase 7 - WSL Bootstrap):**
- Setup script and path resolution for WSL (Cursor, clip.exe, workspace paths).
- Handle WSL-specific quirks so other phases treat paths abstractly.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add WSL and platform detection helpers</name>
  <files>src/config/wsl.ts</files>
  <action>
Create a small `src/config/wsl.ts` module that exposes:

- `isWindows(): boolean` — true when process.platform === "win32".
- `isWsl(): boolean` — true when running under WSL: check process.platform === "linux" and either (a) one of WSL_DISTRO_NAME, WSL_INTEROP is set, or (b) /proc/version contains "microsoft" or "wsl". Read /proc/version only when needed; on read error or ambiguity, return false.
- `getWindowsRoot(): string | null` — returns "/mnt/c" when isWsl() is true, null otherwise.
- `toWindowsPath(wslPath: string): string | null` — for paths under /mnt/&lt;drive&gt;/ convert to Windows style (e.g. /mnt/c/Users/foo → C:\Users\foo): strip /mnt/, uppercase drive letter, replace / with \. For non-/mnt/ paths or invalid input, return null. No new dependencies.

Keep detection conservative so vanilla Linux is not mis-detected as WSL.
  </action>
  <verify>
- Run project type-check/build; no new errors.
- In this workspace, isWsl() returns true when under WSL; toWindowsPath("/mnt/c/Users/test") returns a Windows-style path; getWindowsRoot() returns "/mnt/c" under WSL and null otherwise.
  </verify>
  <done>
- src/config/wsl.ts exists and exports isWindows, isWsl, getWindowsRoot, toWindowsPath.
- WSL detection and path conversion behave correctly under WSL; project still builds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add path resolution and WSL bootstrap module</name>
  <files>src/config/paths.ts, src/bootstrap/wsl-bootstrap.ts</files>
  <action>
Create or extend `src/config/paths.ts` that uses the WSL helpers and centralizes:

- `getCursorBinaryPath(config: AutopilotConfig): string` — prefer process.env.GSD_CURSOR_BIN if set; else config.cursorAgentPath or "cursor-agent". When isWsl() and the value is a WSL path under /mnt/, convert with toWindowsPath for use with Windows binary; otherwise return as-is.
- `getClipExePath(): string | null` — when not WSL return null. When WSL: use process.env.GSD_CLIP_EXE if set; else return "C:\\Windows\\System32\\clip.exe" (or equivalent). Treat clipboard as optional; callers must skip clipboard features when null.
- `getWorkspaceDisplayPath(workspaceRoot: string): { wslPath: string; windowsPath: string | null }` — wslPath is workspaceRoot; windowsPath is toWindowsPath(workspaceRoot) when WSL, else null.

Create `src/bootstrap/wsl-bootstrap.ts` that:
- Exports `applyWslBootstrap(config: AutopilotConfig): ResolvedEnvironment` which returns { isWsl, cursorBinaryPath, clipExePath, workspace: { wslPath, windowsPath } } using the path helpers.
- No side effects beyond reading config and env; used at CLI startup to resolve environment once.
  </action>
  <verify>
- Type-check/build passes. From project root, a small script or REPL that imports paths and wsl-bootstrap confirms getCursorBinaryPath, getClipExePath, and getWorkspaceDisplayPath behave as specified under WSL, with env overrides taking precedence.
  </verify>
  <done>
- src/config/paths.ts and src/bootstrap/wsl-bootstrap.ts exist; path resolution and applyWslBootstrap match the above contract; env overrides work.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire WSL bootstrap into CLI and document</name>
  <files>src/cli.ts, src/cursor-agent.ts, README.md</files>
  <action>
Integrate WSL bootstrap into the main CLI and agent invoker, and document behavior.

- In the CLI entrypoint (e.g. src/cli.ts): call applyWslBootstrap(config) at startup (or before first use of agent/clip paths). Use the resolved cursor path and clip path from the bootstrap result; do not hardcode "cursor-agent" or "clip.exe" in orchestration code.
- In the Cursor agent invoker (e.g. src/cursor-agent.ts): use the centralized Cursor binary path (from config or bootstrap) when spawning the agent process, so WSL vs native behavior is determined by paths.ts and wsl.ts only.
- In README.md: add a "WSL Support & Paths" (or "WSL Bootstrap") section that explains: the CLI auto-detects WSL and resolves Cursor and clip.exe via config helpers; users can set GSD_CURSOR_BIN and GSD_CLIP_EXE to override; clipboard is optional and the CLI works without it.
  </action>
  <verify>
- Build/type-check passes. CLI --help (or equivalent) runs under WSL without crashes or path-related errors. README section accurately describes WSL behavior and env vars.
  </verify>
  <done>
- CLI and agent invoker use bootstrap/path utilities; no hardcoded Cursor or clip paths in orchestration. README documents WSL support and overrides.
  </done>
</task>

</tasks>

<verification>
Before declaring Phase 7 Plan 01 complete:
- [ ] TypeScript build or type-check succeeds.
- [ ] CLI help/no-op command runs under WSL without new errors.
- [ ] WSL path conversion and Cursor/clip resolution behave as specified, with env overrides respected.
</verification>

<success_criteria>

- WSL detection and path conversion are implemented and used in this workspace.
- Cursor and clipboard paths are resolved via a single configuration layer with env overrides.
- CLI entrypoint and agent invoker use these utilities; WSL-specific logic stays in config and bootstrap.
- README includes a clear WSL bootstrap / path resolution section.
  </success_criteria>

<output>
After completion, create `.planning/phases/07-wsl-bootstrap/07-01-SUMMARY.md`:

# Phase 7 Plan 01: WSL Bootstrap Summary

**WSL-aware detection and path resolution added so the CLI reliably locates Cursor and clip.exe when running under WSL.**

## Accomplishments

- Implemented WSL/platform detection and path conversion in src/config/wsl.ts.
- Centralized Cursor binary, clip.exe, and workspace path resolution in src/config/paths.ts.
- Added applyWslBootstrap in src/bootstrap/wsl-bootstrap.ts and wired it into CLI startup and agent invoker.
- Documented WSL support and override env vars in README.

## Files Created/Modified

- src/config/wsl.ts — WSL detection and toWindowsPath / getWindowsRoot.
- src/config/paths.ts — getCursorBinaryPath, getClipExePath, getWorkspaceDisplayPath.
- src/bootstrap/wsl-bootstrap.ts — applyWslBootstrap.
- src/cli.ts, src/cursor-agent.ts — use bootstrap/path utilities.
- README.md — WSL Support & Paths section.

## Decisions Made

- Conservative WSL detection (env + /proc/version); no new dependencies.
- Env vars GSD_CURSOR_BIN and GSD_CLIP_EXE override resolved paths; clipboard optional when null.

## Issues Encountered

- [Document any edge cases or "None".]

## Next Step

- Phase 7 complete; ready for next phase or refinements.
</output>
